# Décisions techniques — index

Une décision = **un fichier** dans [`docs/decisions/`](decisions/). Ce fichier n'est plus
que leur index : une ligne par décision, **de la plus récente à la plus ancienne**.

**Écrire une nouvelle décision** : créer `docs/decisions/<ancre>.md` (titre `#` de niveau 1,
nom en kebab-case, numéro d'issue s'il y en a un) **et** ajouter sa ligne en tête de la liste
ci-dessous. Jamais d'insertion dans un fichier existant : c'est précisément ce qui supprime
les conflits de fusion que ce fichier produisait — tous les lots convergeaient sur sa ligne 1,
à raison de trois conflits par jour. Ne re-fusionnez pas les fichiers en croyant simplifier.
`tests/test_index_decisions.py` échoue si une décision n'a pas sa ligne, ou l'inverse.

**Les ancres restent ici.** Des centaines de liens `docs/technical_decisions.md#<ancre>`
existent hors du dépôt — commentaires d'issues GitHub, non réécrivables. Chaque ligne porte
donc l'ancre d'origine de sa décision, alias compris : un vieux lien atterrit sur la bonne
ligne, qui pointe vers le fichier. Une ancre ne se renomme ni ne se supprime.

La version d'avant la découpe (30/08/2026, 18 404 lignes, 1 054 Ko) est figée sous
[`docs/archive/`](archive/technical_decisions-avant-decoupe-20260830.md) : une archive, jamais l'autorité.

---

- `2026-09-11` <a id="acteur-du-roster-850"></a>[Un membre de roster est collecté sous l'acteur que son roster nomme, jamais re-deviné par son nom (#850)](decisions/acteur-du-roster-850.md) — le run `34575181245` a laissé **6 membres de roster sans profil**, donc absents de **5 fiches de groupe AN**, alors que le roster AMO30 nommait leur acteur. Le roster le transmettait, mais **encodé dans une URL** (`source`) que rien ne relisait, et la collecte le re-devinait depuis le slug par la **correspondance par nom** : échec sur la ponctuation que le slug avait perdue (O'Petit, K/Bidi, M'jid El Guerrab, Morel-À-L'Huissier), refus — légitime — devant deux homonymes (Béatrice Descamps, Jean-Louis Masson). **La mesure a précédé le code, et elle a changé le lot** : aucune attribution au mauvais acteur dans le corpus publié — **0 écart** sur les 1 892 entrées membres des 28 fiches AN, en vérifiant contre AMO30 que l'acteur de chaque profil appartient à un organe du groupe qui le liste, sans passer par la table dont 16 entrées sur 1 174 seulement ont une origine vérifiée ; le défaut est de **couverture**, pas de véracité, et il n'y avait rien à purger. L'entrée de roster porte désormais `acteur_ref` **en clair**, `process_candidat` le transmet pour un membre de roster et pour lui seul, et `fetch_identite_officielle_par_slug` le prend **avant** toute résolution — sauf s'il **contredit la table committée**, auquel cas rien n'est collecté et les deux acteurs sont nommés (la règle de #757). **Effet mesuré avant de pousser** : sur les 1 140 entrées de la vraie liste de roster, 1 134 ont une entrée de table et **toutes** s'accordent avec le roster ; les 6 qui n'en ont pas sont **exactement** les six introuvables. Le correctif ne change le comportement que pour eux. Suite complète à **4 510**, 0 échec.
- `2026-09-11` <a id="agents-sans-comptes-volatils"></a>[`AGENTS.md` ne porte plus de compte qu'un run déplace](decisions/agents-sans-comptes-volatils.md) — trois comptes d'`AGENTS.md` étaient faux le jour où on les a relus, et aucun par négligence : **481 profils, dont 13 candidats déclarés et 468 membres de roster** (#630) quand `main` en porte **1 035, dont 32 et 1 003** ; **158 fichiers de décision** quand il y en a **285** ; un portail à **quatorze blocs** quand #845 venait de lui ajouter la §4c. Chacun était juste à sa date. Le défaut n'est pas la mesure, c'est son **emplacement** : `AGENTS.md` est chargé à chaque session et lu comme l'état présent, alors qu'un run déplace les deux premiers et qu'un lot déplace le troisième. Question posée par la propriétaire, 11/09/2026 : ces chiffres ont-ils leur place dans un fichier censé être stable ? Non. Ils sont retirés, la règle qu'ils illustraient reste — deux populations, un champ qui les distingue, des outils qui impriment la ventilation —, et le §8 interdit désormais d'en rajouter. **Rien n'est perdu** : les chiffres de #630 vivent dans `populations-profils-portees-par-les-outils-630.md`, et le compte à jour se lit en une commande.
- `2026-09-11` <a id="cle-de-fusion-qui-depend-dun-champ-volatil-827"></a>[Une clé de déduplication ne doit dépendre d'aucun champ qui apparaît (#827)](decisions/cle-de-fusion-qui-depend-dun-champ-volatil-827.md) — #827 a donné un `source_url` aux explications de vote, et le run `34503639092` les a publiées **deux fois** : `_interv_key` était une cascade **exclusive** qui prenait l'URL avant le contenu, or ces entrées n'ont **pas** d'`intervention_id` — leur clé était le contenu avant, l'URL après, donc la fusion additive a ajouté au lieu de reconnaître. **1 462 doublons** sur 4 profils. **Le correctif évident aurait détruit 88 % du corpus** : indexer chaque entrée sur toutes ses clés, contenu compris, fusionnerait les **902 457** entrées à identifiants différents qui partagent déjà une clé de contenu, les interventions en mode thème-seul n'ayant ni sujet ni texte — la mesure a précédé le code et l'a interdit. `source_url` est donc **retiré de la cascade** : l'identifiant quand il existe, le contenu sinon, ni l'un ni l'autre n'apparaissant après coup ; les **3 073** entrées sans identifiant portent **toutes** un sujet ou un texte, **zéro** exception. La purge (`scripts/purger_doublons_interventions_827.py`) garde l'exemplaire **avec** le lien et ne passe que sur le pivot — #729 exige les deux étages, mais le brut n'a rien, l'enrichissement s'appliquant au profil pivot. Après purge : **1 611** explications, le compte d'origine, **1 462** liées, **0** doublon. **Un chiffre corrigé en route** : le premier constat annonçait 1 962 doublons dont 430 `debat` — faux, la mesure groupait sur le contenu **sans distinguer les entrées à identifiant** et comptait des interventions distinctes du même débat ; **0** entrée à identifiant est publiée deux fois. Un témoin lit le corps de `_interv_key` et refuse le retour de `source_url` : le défaut n'apparaît qu'au run **suivant** celui qui pose le champ. Suite complète à 4 453, 0 échec.
- `2026-09-11` <a id="fiche-de-lignee-836"></a>[Une lignée n'est pas la somme de ses maillons (#836)](decisions/fiche-de-lignee-836.md) — l'interface ne publiera qu'**une fiche par lignée** de groupe parlementaire (décision de la propriétaire, 10/09/2026), et cette fiche n'existait nulle part : ce lot en pose le schéma et la composition. **Elle ne s'obtient pas en additionnant les maillons** — mesuré sur la lignée socialiste `NG:15 → SOC:15 → SOC:16 → SOC:17`, la somme donne **170 membres** pour **96** personnes et **20 524** scrutins de cohésion pour **16 420**, les maillons n'étant pas disjoints ; et les `amendements_agreges` ne se dédoublonnent **pas du tout** au niveau des fiches, un amendement cosigné comptant une fois dans chaque maillon sans que rien ne dise que c'est le même — d'où un **recalcul** depuis `profiles[].amendements`, législature par législature pour ne pas rouvrir le défaut que #821 et #825 viennent de fermer. Trois régimes, donc : ce qui s'**unit** sur une clé (membres sur `membre_id`, cohésion sur `scrutin_id`, mandats sur `(categorie, label)`), ce qui se **recalcule**, et ce qui ne s'agrège **pas** — `position_politique` est publiée par législature par l'Assemblée (#686) et les réunir produirait un jugement que personne n'a porté (§2 règle 1), donc chaque maillon garde la sienne. L'ordre des maillons se lit sur `succede_a`, jamais sur les dates, et un cycle est **refusé** plutôt que parcouru. L'identifiant de lignée est **déclaré** et non dérivé : par la racine il changerait le jour où un maillon antérieur est ajouté — MoDem, Horizons, LIOT et UDR restent à déclarer (#815) —, par le maillon le plus récent il changerait à chaque législature, et un identifiant qu'une collecte déplace casse les liens du site. Les fiches vivent dans `pivot_data/lignees/`, à part des maillons : le contrôle de perte raisonne par collection, et deux types de documents dans un même répertoire est le défaut que #630 a payé. **Ce lot ne livre pas le script de génération ni le câblage CI** — la brique est là, mesurée sur la lignée socialiste réelle, et ce qui l'appelle reste à écrire.
- `2026-09-11` <a id="fiche-de-lignee-ui-329"></a>[La page de groupe devient une fiche de lignée, construite en maquette avec la propriétaire (#329)](decisions/fiche-de-lignee-ui-329.md) — l'interface publie **une fiche par lignée déclarée** (#836), et plus une par législature : 13 pages au lieu de 30 fiches, l'adresse d'un groupe vient de son `lignee_id` et une adresse de fiche par législature (`/groupes/AN-SOC-17`) mène à sa lignée. La page a été **construite en maquette avec la propriétaire le 11/09/2026** — seize versions, vingt fils d'annotation —, et chaque arbitrage y est venu d'un rendu, jamais d'un texte. **`sync-data.mjs` ne rechaîne plus `succede_a`** : il trouvait 13 lignées, maillon pour maillon les mêmes que les 13 déclarées, mais seulement parce qu'aucune scission n'était encore écrite. La page lit une **projection de build** (`scripts/vue-lignee.mjs`, 6,9 Mo pour les 13, 1,1 Mo au plus pour une lignée, contre 5 Ko à 7,7 Mo de fiche de lignée et jusqu'à 11 Mo de maillons), calculée par les **mêmes règles** que le navigateur importe (`utils/groupe.js`, `utils/lignee.js`) : Node exige l'extension `.js` dans les imports, Vite l'accepte, et c'est tout ce qu'il a fallu changer. Deux recalculs y sont **vérifiés, pas crus** : l'effectif jour par jour retombe sur `effectif.a_la_date_de_reference` pour les **28 maillons AN sur 28**, et les amendements par commission retombent sur `amendements_agreges.par_type_deposant` pour les **56 couples (maillon AN, type de déposant) sur 56** — un type qui s'en écarte n'est pas servi. Six sections — qui sont-ils · sur quoi ils ont pris la parole · ce qu'ils ont proposé · ce qu'ils ont voté · avec qui ils votent · ce qu'on n'a pas pu lire —, la dernière réduite à ce que chaque fiche signale d'elle-même, le reste du corpus renvoyé à `/couverture` (#328), la navigation par période de la fiche candidat maillon par maillon, et les sept règles de forme appliquées : le paragraphe en méthodologie derrière cinq ancres, la limite en pied de section. **Retirés, chacun pour sa raison** : « Ce que cette fiche ne dit pas » et les sept notes (règle 2), la ligne « déposés par le gouvernement : 0 » et la comparaison des amendements groupe contre groupe (règle 1), les instances et fonctions (donnée fausse, #853), les grandes lois (relecture). **Un écart assumé avec la fiche candidat** : la frise de lignée garde des motifs de posture le jour où celle du candidat perd les siens (#328), parce qu'elle ne porte qu'un encodage. Coût : `sync-data` passe à 59 s et 1,2 Gio de RSS à froid. Suite complète à **4 525**, 0 échec.
- `2026-09-11` <a id="generation-fiches-lignees-836"></a>[Les dix lignées sont écrites, et une mesure corrige le motif du recalcul (#836)](decisions/generation-fiches-lignees-836.md) — #836 avait posé le schéma et la composition, sans ce qui les appelle : `pivot_data/lignees/` était vide. Ce lot livre `generate_lignee_profiles.py`, son step CI, la §4c du portail, la collection `lignees` du contrôle de perte, et les **dix `lignee_id`** déclarés dans `raw_data/groupes_reels.json`. **La mesure a corrigé le motif** : la somme des maillons et le recalcul depuis les profils coïncident **exactement sur neuf lignées sur dix** — `88 709 → 60 897` amendements distincts sur la seule socialiste, et `20 524 → 16 420` scrutins sur la seule socialiste également. Le ×2,82 de l'issue datait d'**avant #821** ; depuis que chaque maillon ne compte que sa période, deux maillons de législatures différentes portent des `amendement_id` disjoints **par construction**, et le seul recouvrement du corpus vient des deux maillons d'une **même** législature, `NG:15` et `SOC:15`. Le recalcul n'est donc pas un correctif, c'est ce qui **vérifie à chaque run** que la propriété tient — et la seule voie correcte le jour où deux maillons se partagent à nouveau une législature. L'identifiant est **déclaré** sur chaque entrée de `groupes[]`, la chaîne reste écrite **une seule fois** dans `succede_a`, et un contrôle refuse qu'elles se contredisent : c'est cette porte qui tient la **scission** `AD`/`DR` tant que sa forme n'est pas tranchée (#815). Deux défauts trouvés en écrivant l'appelant : `recalculer_agregats` publiait `nb_amendements_sans_identifiant` là où toute la chaîne lit `nb_sans_identifiant`, sans `signatures` ni `taux_adoption` — **elle n'était couverte par aucun test** —, et elle chargeait tous les profils d'une lignée avant d'agréger, soit l'OOM de #377 un étage plus haut sur les **660** couples de la lignée macroniste. Coût mesuré : **104 s et 1 453 Mio de RSS** pour les dix, **37 Mo** sur disque en écriture compacte contre 53 indentée. Suite complète à **4 492**, 0 échec.
- `2026-09-11` <a id="index-decisions-genere-840"></a>[L'index des décisions est généré, il ne s'édite plus (#840)](decisions/index-decisions-genere-840.md) — `docs/technical_decisions.md` était maintenu à la main et **tous les lots y écrivaient au même endroit**, une ligne insérée en tête : deux branches parallèles conflictaient systématiquement — **quatre rebases en une heure** le 10/09/2026, sur des lots qui ne se recouvraient pas, et une résolution manuelle dans l'interface GitHub ne pouvait pas régénérer `decisions-par-module.md`, donc les tests tombaient ensuite sans que rien n'ait été mal fait. L'index est désormais **produit** depuis les fichiers de `docs/decisions/`, chacun portant sa date et son résumé `> **En bref** — …` : un lot n'écrit que **son propre fichier**, que personne d'autre ne touche, et le conflit devient **structurellement impossible** au lieu d'être seulement rare. **La migration est prouvée sans perte** — index régénéré comparé champ par champ à l'ancien sur les 279 décisions : **0** date changée, **0** résumé altéré, **0** ancre perdue, une ancre **gagnée** (`gouvernement-textes-statut`, qui existait dans son fichier sans être indexée) et 14 titres devenus plus précis, l'index ayant simplifié `(#683, lot 4)` en `(#683)`. Le résumé vit **dans** la décision et non à côté, parce que la lecture chronologique du projet a de la valeur pour qui ouvre le fichier ; les alias d'ancres deviennent de **vraies** ancres HTML, si bien qu'un lien `decisions/<nom>.md#<alias>` atterrit enfin quelque part. **Écarté** : insérer en fin plutôt qu'en tête — deux ajouts au même endroit conflictent de la même façon —, et une stratégie de merge dans `.gitattributes`, qui ferait disparaître le conflit **en perdant une ligne en silence**.
- `2026-09-11` <a id="lignees-modem-horizons-liot-815"></a>[Les lignées MoDem, Horizons et LIOT, et un tri qui plantait sur un seul député (#815)](decisions/lignees-modem-horizons-liot-815.md) — trois lignées de plus, déclarées sur l'archive AMO30 republiée le 11/09/2026 à 02:34 : **MoDem** `DEM:15 → DEM:16 → DEM:17`, **Horizons** `HOR:16 → HOR:17`, **LIOT** `LIOT:16 → LIOT:17` — **7 fiches**, effectifs 67, 58, 42, 36, 44, 22 et 26, soit **178 personnes distinctes**, dont **37** ont déjà un profil publié et **141** sont à collecter par le run. Le MoDem de la XVe est l'**union de deux organes** — `MODEM` jusqu'au 23/09/2020, `DEM` dès le 24 —, le motif `SOC`/`SOC-A` de la XVIe : sans l'union, trois ans sur cinq disparaîtraient. **La mesure a trouvé un défaut que le run aurait payé** : `fusionner_intervalles`, posé par #809 la veille, triait des couples `(début, fin)` et levait `TypeError` dès que deux mandats de même début avaient l'un une fin, l'autre aucune. **Un seul couple de tout l'index GP** porte cette forme — Stéphane Lenormand, LIOT-17, `2024-07-19 → 2025-02-28` puis `2024-07-19 → ouvert` —, aucun des 21 groupes jusque-là déclarés ne la portait, et c'est pourquoi rien ne l'avait vu ; déclarer LIOT-17 sans corriger aurait fait tomber le roster du run. La clé de tri ne compare plus jamais une fin ouverte à une date. **Non déclarés, et ce n'est pas un oubli** : `LT` (XVe) comme prédécesseur de LIOT, `AGIR-E` (XVe) comme prédécesseur d'Horizons — deux jugements de filiation que personne n'a portés ; l'identifiant de lignée étant déclaré, les ajouter plus tard ne le déplacera pas. UDR, scission, attend toujours sa forme. Suite complète à **4 503**, 0 échec.
- `2026-09-11` <a id="periodes-jetees-par-le-filtre-809"></a>[Un champ traverse la chaîne, ou il n'existe pas (#809)](decisions/periodes-jetees-par-le-filtre-809.md) — #809 a câblé `mandat_periodes` de bout en bout — roster, appartenances, membre de fiche, compteur de présence, schéma, validation — et le champ n'a **jamais atteint une seule fiche** : `filter_roster_by_sigle` est une **projection par clés** qui recopie chaque entrée champ par champ, et le nouveau n'y figurait pas. Le run `34538350163` a régénéré les 23 fiches de groupe, `date_reference.origine` y a bien pris sa nouvelle valeur (#808 passait par un autre chemin), et **zéro membre ne portait `periodes[]`**. C'est la règle que `docs/regles/fusion-et-index.md` énonce depuis #492, #639, #641, #696, #710 et #718 — un champ ajouté n'atteint pas tout seul un consommateur qui projette — appliquée cette fois non à une fusion mais à un **filtre**. **Les tests de #809 ne pouvaient pas le voir** : ils appelaient `appartenances_depuis_roster` directement, en sautant le filtre, et décrivaient donc la chaîne telle que le lot l'imaginait. Le correctif est d'une ligne ; le test qui l'accompagne part du roster et va jusqu'à l'appartenance, filtre compris, et vérifie sur la chaîne réelle que `REN-16` rend bien ses **196 membres avec périodes, dont 8 à périodes multiples**.
- `2026-09-11` <a id="teintes-des-institutions-328"></a>[Trois teintes pour quatre institutions, et la sarcelle réservée au Sénat (#328)](decisions/teintes-des-institutions-328.md) — une seule institution a une couleur officielle réutilisable (l'Union, `#003399`) ; une fois ce bleu pris, le cercle ne loge plus trois autres familles sans parenté, donc le Sénat sort du système de couleurs et la sarcelle `#169E9E` lui est réservée pour le jour où il sera rebranché.
- `2026-09-10` <a id="amendements-agreges-dans-la-periode-821"></a>[Un agrégat de fiche ne compte que la période de la fiche (#821)](decisions/amendements-agreges-dans-la-periode-821.md) — `_aggregate_amendements` parcourait l'`amendements[]` des membres **sans aucun filtre** : `LR-16` publiait 159 274 amendements dont **32 277 seulement** déposés sous la XVIe, et **20 501 venaient de la XVIIe**, c'est-à-dire d'après la dissolution du 9 juin 2024, sous un groupe qui n'existait plus — 20 % dans la période pour `LR-16`, 26 % pour `SOC-16`, 46 % pour `RN-16`, si bien que **les chiffres publiés n'étaient même pas comparables entre eux** ; c'est le filtre de #403 pour les votes (« un scrutin serait attribué à un groupe qui n'existait pas au moment du vote »), jamais transposé, **quatrième occurrence de la semaine** après #657, #817 et #683 ; la législature se lit sur l'identifiant (`AMANR5L16…`), donnée **structurelle** jamais déduite d'une date, un identifiant qui ne la porte pas est **retenu** et compté à part (§2 règle 5), et le filtre ne s'arme que si l'appelant nomme une législature ; **une signature n'est pas un amendement, et le compteur a dû l'apprendre** — la première version nommait « amendements » ce qui s'incrémente une fois par signataire : **657 996 signatures écartées pour 126 997 amendements distincts** sur `LR-16`, la confusion même que #643 a corrigée et que §6 verrouille, défaut vu en lisant la sortie réelle et non en relisant le code. Effet vérifié en rejouant la génération : 159 274 → **32 277**, exactement le chiffre mesuré indépendamment avant d'écrire une ligne, et `taux_adoption` bouge de 0,0424 à 0,0354 — il était lui aussi calculé hors période. **Non corrigé ici** : `tags_thematiques_agreges` a le même défaut (26 % des interventions de `REN-16` dans la période) mais `tags_thematiques` est une liste de chaînes **sans provenance** — un lot à part. **Écarté** : filtrer sur l'appartenance réelle, parce que #809 a établi que ces bornes sont une enveloppe. Suite complète à 4 376, 0 échec.
- `2026-09-10` <a id="bloc-sombre-mort-retire-328"></a>[Le bloc sombre de « En bref » est retiré (#328)](decisions/bloc-sombre-mort-retire-328.md) — `.cp-gc` portait le seul thème sombre de tout `src/`, sans effet possible sous `color-scheme: light` ; le lot institutionnel devait le doubler, il le retire.
- `2026-09-10` <a id="colonnes-repliables-en-bref-328"></a>[Les colonnes de « En bref » se replient, et le Sénat l'est d'entrée (#328)](decisions/colonnes-repliables-en-bref-328.md) — le repli d'une colonne par le lecteur n'est pas le retour des onglets : tout est ouvert au départ sauf le Sénat, on ne peut jamais tout replier, et une colonne repliée reste nommée par sa puce.
- `2026-09-10` <a id="controle-perte-nomme-les-echanges-823"></a>[Le contrôle de perte nomme ce qui a disparu (#823)](decisions/controle-perte-nomme-les-echanges-823.md) — `audit_diff_profils` relève chaque liste par un **entier** : une entrée remplacée par une autre garde le même compte, et l'échange est **invisible** ; le run `34454305520` l'a payé — `tags_thematiques` montait de 43 811 à 53 581 sur les 953 profils, **aucune perte**, pendant que `REN-16` et `EPR-17` perdaient chacun **une** étiquette d'union et bloquaient le commit, or une union ne perd un élément que si aucun membre ne le porte plus : l'échange naissait à l'étage profil, où rien ne le voyait, et ne se manifestait qu'à l'étage groupe, **où il ne naissait pas** ; le run n'ayant rien committé, sa sortie n'existait plus et l'étiquette **n'a jamais pu être nommée**. Une collection déclare désormais `listes_nommees` — `tags_thematiques` et `tags_thematiques_agreges`, **trois champs** — dont le relevé garde les valeurs : les entrées disparues sont nommées, y compris à compte égal et **même quand le compte monte**, le cas le plus trompeur. Coût mesuré et non supposé : **5 911 valeurs distinctes** pour 53 183 entrées sur les 1 035 profils publiés, 2,6 Mio qui ne quittent jamais la mémoire — le même traitement sur `amendements` coûterait des millions de clés, d'où le choix de déclarer plutôt que de généraliser. **Non bloquant, délibérément** : `tags_thematiques` est un champ DÉRIVÉ recalculé à chaque run (§4), un échange y est le fonctionnement normal — #825 vient d'ailleurs de diviser par deux les agrégats des 21 fiches, volontairement ; ce lot n'ajoute pas un verrou, il ajoute une **explication**, la perte de compte restant bloquante. `_identites()` rend **`None` et non un ensemble vide** sur une liste non nommable, et aucun constat n'est produit si un seul des deux côtés l'est : ne pas savoir n'est pas un fait (§2 règle 5). **Non tranché** : sortir `tags_thematiques` des listes stables, que cette décision rend moins urgent sans le régler. Suite complète à 4 435, 0 échec.
- `2026-09-10` <a id="dossiers-legislatifs-sous-le-formulaire-817"></a>[Les dossiers du roster passent sous une case, le motif qui les écartait est tombé (#817)](decisions/dossiers-legislatifs-sous-le-formulaire-817.md) — `extract-roster-groupes` portait `--skip-dossiers-legislatifs` **en dur** depuis #357 au motif que « les textes portés ne sont consommés par aucun agrégat de groupe », or c'est le motif même que #657 avait démonté une liste plus tôt, et le commentaire du workflow portait les deux **côte à côte** : la justification périmée, puis sa réfutation sur le cas jumeau ; **17 profils sur 1 035** publiaient des `textes_portes`, tous `candidat_declare`, **zéro** membre de roster — `AN:DR` montrait 1 membre sur 86 —, alors que la collecte en rendrait **887 sur 1 021 pour 9 895 dossiers**, chiffre que l'issue n'avait pas mesuré ; le coût est de **3,0 s** de construction d'index et **57 Mio** de RSS, une fois par processus puis un `dict.get()` par membre, sur une archive que ce job **restaure déjà** (#505). Le drapeau passe sous `collect_dossiers_legislatifs` (défaut `false`) plutôt que d'être retiré, comme #657 l'a fait pour les interventions ; libellé relu à l'écran avant d'être écrit (§11), et la première rédaction — « roster members too » — écartée parce que les candidats déclarés sont collectés par `extract-an` indépendamment de cette case. **La couverture n'avait rien à changer** : `meta.collecte_ecartee` prime sur l'inférence et cessera simplement de nommer `textes_portes`, tandis que `DECISIONS_ROSTER` reste juste pour les 19 profils d'avant #539 — l'en retirer publierait « couvert » sur des listes vides. `tests/test_ci_inputs_workflow.py` a repris ce lot en **exécutant** le bash du workflow : il refuse une expression `${{ }}` dans le script, la première rédaction en portait une, deux tests sont tombés et ils avaient raison. Suite complète à 4 423, 0 échec.
- `2026-09-10` <a id="identifiants-groupes-et-successions-multiples-815"></a>[Un `groupe_id` porte toujours sa législature, et `succede_a` est une liste (#815)](decisions/identifiants-groupes-et-successions-multiples-815.md) — préalable à la continuité des groupes XVe→XVIIe : **sept identifiants sur dix-huit ne portaient pas leur législature** (`AN:REN`, `AN:SOC`, `AN:RN`, `AN:LFI`, `AN:LR`, `AN:EPR`, `AN:DR`), héritage de l'ère NosDéputés où `groupes_reels.json` ne décrivait qu'une seule législature — **le format est une convention du dépôt, pas un champ de la source ; c'est le contexte qui rendait le suffixe inutile** —, et ça cesse de tenir dès qu'un sigle revient : déclarer `LR` de la XVe rend `AN:LR` ambigu et `DR-17.succede_a` pointe sur un sigle à deux fiches ; **un test figeait l'incohérence** (« le constat, figé plutôt que corrigé »), remplacé par l'invariant vérifié sur **toutes** les entrées ; `succede_a` passe en **liste non vide**, parce qu'une fusion a deux prédécesseurs et qu'une **scission** — `AD` quitte `DR` le 11/09/2024 pendant que `DR` poursuit — n'est pas un remplacement ; **le fichier porte deux listes** (`groupes[]`, seule à décider de ce qu'un run écrit, et `correspondance_sigles_an.groupes`) et n'en corriger qu'une ne se voyait pas — c'est le test de recopie qui l'a montré ; l'ancienne forme est **refusée nommément** aux deux étages plutôt que tolérée, et **le contrôle du portail a dû suivre** : il testait `isinstance(succede_a, dict)` et, laissé tel quel, n'aurait plus rien vérifié — un garde-fou muet, pas tolérant. Effet **prouvé** en rejouant `generate_group_profiles.py` sur `DR-17` : `AN:DR:17` + liste, schéma OK ; le portail rend **7 échecs durs** sur le corpus committé, état d'avant-run déjà connu de LaREM XVe. **Écarté** : accepter les deux formes — deux écritures du même fait dans un fichier relu à la main, et un validateur qui ne peut plus dire laquelle est la bonne. Suite complète à 4 372, 0 échec.
- `2026-09-10` <a id="institution-dimension-de-la-fiche-328"></a>[L'institution est une dimension de la fiche, pas une section (#328)](decisions/institution-dimension-de-la-fiche-328.md) — quatre institutions lues dans les six sections de la fiche ; la chambre voyage sur le rôle et seul l'affichage la lit, le banc reste intact pour ses quinze consommateurs.
- `2026-09-10` <a id="lectures-du-depot-dans-les-tests-791"></a>[Un garde-fou posé sur `builtins.open` ne voit pas `pathlib` (#791)](decisions/lectures-du-depot-dans-les-tests-791.md) — `tests/conftest.py` refusait le cache du poste et le corpus publié, mais pas les `.json` de `raw_data/` qu’un run réécrit : trois tests du 08/09 sont passés au rouge **en local seulement** quand le run `34278343461` a écrit l’entrée d’Asselineau dans `correspondance_acteurs_an.json`, la CI ne matérialisant pas ce fichier ; **la piste de l’issue — étendre le filtre `builtins.open` — était insuffisante et la mesure le dit** : `Path.open()` appelle `io.open`, `builtins.open` en est une AUTRE référence, et sur les **144 tests** qui ouvraient un fichier du dépôt, **40** passaient par `builtins.open` contre **105** par `pathlib` — **le garde-fou de #721 avait donc le même trou depuis son écriture**, et un test le traversait en créant `.cache/amendements_an/17/` dans le dépôt pour y écrire. D’où trois portes coupées au lieu d’une ; ce qu’un run réécrit servi figé (`correspondance_acteurs_an.json` 64 ouvertures → **0**, `resolutions_candidats.json` 1 → **0**, le marqueur de cache 2 → **0**) ; et ce qui est une **configuration committée** — `groupes_reels.json`, `gouvernements_reels.json`, `candidats.json` — **déclaré** fichier par fichier (`pytest.mark.lit_reference_committee`, 18 fichiers de tests) plutôt que figé, parce que figer `test_repository_groupes_reels_json_is_valid` reviendrait à lui faire valider une copie. La déclaration n’est acceptée **que si le chemin est dans le `sparse-checkout`** : d’où l’entrée de `raw_data/candidats.json` (20 Kio) dans la liste blanche, cinq tests de validité qui se `skip`aient en CI et qui y tournent enfin — seul changement du lot dans `.github/workflows/`, réversible d’une ligne. Troisième mémo de module découvert à la mesure (le piège de #767) : `_MEMBRES_GROUPES_SUSPENDUS` est construit une fois par processus, si bien qu’un seul test lisait `groupes_reels.json` et onze consommaient sa lecture sans rouvrir de fichier — vidé aux deux bouts, le compte de lecteurs passe de 96 à **110**, le vrai chiffre. **41 tests** n’ouvrent plus aucun des trois fichiers disparus, **27** plus aucun fichier du dépôt ; aucun test supprimé ni désarmé. Suite complète 4 380 → **4 399 passés, 0 échec**.
- `2026-09-10` <a id="origine-date-reference-nomme-le-critere-808"></a>[L'origine de la date de référence nomme le critère, plus un événement (#808)](decisions/origine-date-reference-nomme-le-critere-808.md) — `date_reference.origine` valait `cloture_legislature` dès que toutes les appartenances d'un groupe sont refermées : critère de **calcul** exact, étiquette **fausse** pour deux fiches sur douze, dissoutes en cours de législature — `NG-15` s'arrête au 11/09/2018 quand la XVe se clôt le 21/06/2022, **3 ans 9 mois** d'écart, `EDS-15` 1 an 8 mois ; un lecteur y lisait « effectif à la fin de la XVe » là où la donnée dit « effectif au dernier jour d'existence du groupe » — le chiffre juste, la phrase fausse (§2 règle 2), soit le défaut de #653 un cran plus bas. La valeur devient **`derniere_appartenance_close`**, et l'avertissement publié — qui est la part réellement lue — nomme désormais le cas au lieu d'annoncer une clôture. L'ancien nom reste **accepté en lecture et jamais écrit** : les 14 fiches qui le portent ne seront régénérées qu'au prochain run, et le portail échouerait entre-temps sur des fichiers que personne n'a touchés ; les 2 fiches Sénat **gelées** ne portent aucun `date_reference`, donc le cas d'un nom à garder pour toujours ne se présente pas. **Écarté** : scinder en deux valeurs selon que la dernière appartenance coïncide ou non avec la clôture — il faudrait la date de clôture de chaque législature, pour une nuance que la date publiée donne déjà. Suite complète à 4 426, 0 échec.
- `2026-09-10` <a id="periodes-appartenance-809"></a>[L'appartenance publiée cesse d'être une enveloppe (#809)](decisions/periodes-appartenance-809.md) — `membres[].debut_dans_groupe`/`fin_dans_groupe` n'étaient pas les dates d'appartenance mais leur **enveloppe**, `_fusionner_periodes` recollant tous les mandats `GP` en une période : **33 membres de 8 fiches** étaient dits dans leur groupe pendant qu'ils étaient **au gouvernement**, 23 des trous coïncidant exactement avec une `fonction_gouvernementale` — Dussopt ministre du 21/05/2022 au 09/01/2024, sa fiche `REN-16` le disant présent sans interruption ; **re-mesuré ici** sur un corpus qui a gagné `LR-15` et `LAREM-15` : **48 des 1 604 couples** (acteur, groupe) ont plusieurs périodes et le plus long trou fait **1 110 jours**, pas 571. **Aucun compteur publié n'était faux** — 0 membre mal compté sur les 18 groupes — mais par **coïncidence de dates**, aucune date de référence ne tombant dans un trou : le défaut de #653 un cran plus haut, qui ne se corrige pas en espérant que ça dure. D'où `membres[].periodes[]`, et `_appartenance_couvre` exact par construction. **Le seuil de recollement ne se choisit pas, il se lit** : 67 trous d'exactement 1 jour, **0** entre 2 et 30, 48 au-delà de 31 — distribution bimodale, aucune zone grise, donc deux mandats séparés d'un jour sont un changement d'organe et au-delà une absence. L'enveloppe **reste publiée et reste le repli** pour les fiches d'avant ce lot, et la clé **n'apparaît que si la source l'a donnée** — l'écrire à `null` dirait « aucune période connue » sur un membre dont on connaît les bornes (§2 règle 5). `validate_profil_groupe` gagne sa **seule** validation de contenu d'entrée : les périodes détaillent l'enveloppe, elles ne la contredisent pas — une contradiction entre deux champs du même membre ne se rattrape nulle part en aval. **Écarté** : déclarer l'existence des trous sans les dater — 1 110 jours ne sont pas une approximation. Suite complète à 4 427, 0 échec.
- `2026-09-10` <a id="regle-de-lecture-en-methodologie-328"></a>[La règle de lecture quitte la fiche pour la méthodologie, parce qu'elle est désormais appliquée (#328)](decisions/regle-de-lecture-en-methodologie-328.md) — relue à l'écran, la section « ce qu'il a voté » portait **quatre énoncés de règle avant la moindre figure**, dont la dernière lecture écrite **deux fois** — en étiquette du chiffre et en phrase entière sous la figure. #711 avait exigé cette phrase sur la fiche parce que la méthodologie **annonçait** une règle que rien n'appliquait ; depuis #711 c'est le **code** qui l'applique (`isWholeTextVote` et le repli sur la date, dans `utils/lecture.js`), et une promesse non tenue ne peut plus réapparaître en silence — il faudrait retirer l'implémentation, et les tests qui la gardent échoueraient. **#711 est donc renversé sur la place de sa phrase, jamais sur son fond** : restent sur la fiche `LAST_READING_LABEL` — sans elle le lecteur compare des textes à des votes — et le renvoi vers `/methodologie#votes` posé sous la figure ; le raisonnement vit là où il s'argumente. C'est `DESIGN_SYSTEM` §7 règle 2 appliquée : l'étiquette est les deux mots, la phrase était le paragraphe. **Écarté** : une phrase courte de remplacement, qui ne dirait rien que l'étiquette ne dise et rouvrirait ce que le lot ferme. Trois tests suivent, chacun disant pourquoi.
- `2026-09-10` <a id="suite-independante-du-cache-local-767"></a>[La suite dit son monde au lieu d'en hériter (#767)](decisions/suite-independante-du-cache-local-767.md) — treize tests passaient ou échouaient selon qu'un répertoire de cache **local** existait, `.cache/acteurs_historique_an/` : **verts en CI** dont le checkout est vide, **rouges chez la propriétaire** au moment précis où elle vérifie qu'elle n'a rien cassé ; **ma première re-mesure était fausse** — j'avais lié `.cache` par un **lien symbolique**, or le garde-fou de #721 compare des chemins **résolus**, si bien que ma reproduction avait désarmé le contrôle qu'elle prétendait exercer, et j'ai annoncé « 1 échec, pas 13 » avant de retrouver les treize avec une vraie copie ; **le garde-fou n'était pas en cause** — il diagnostique en nommant le fichier lu et l'idiome à appliquer, et son commentaire dit qu'il ne redirige pas **exprès**, une redirection globale ayant été essayée puis écartée parce qu'elle casse les dix tests qui isolent par `chdir` ; d'où une fixture `autouse` **par fichier**, qui pointe `ACTEURS_HISTORIQUE_CACHE_DIR` vers un répertoire jetable **et vide le mémo de module aux deux bouts** — `_ACTEURS_HISTORIQUE_INDEX_MEMO` est un `dict` partagé qui, rempli par un test voisin, rend le vrai référentiel **sans rouvrir un fichier**, donc sans que le garde-fou puisse le voir. Vérifié **dans les deux mondes** : 4 372 passés cache présent, 4 372 passés cache absent — un correctif vert d'un côté et rouge de l'autre n'aurait fait que déplacer la dépendance. Ne traite pas #791, même famille sur `raw_data/*.json`, rencontrée trois fois aujourd'hui.
- `2026-09-10` <a id="tags-agreges-dans-la-periode-825"></a>[L'empreinte thématique d'une fiche est celle de sa législature (#825)](decisions/tags-agreges-dans-la-periode-825.md) — `aggregate_tags_thematiques` ne recevait ni législature ni période : l'empreinte d'une fiche de groupe était l'union des étiquettes de ses membres sur **toute leur carrière**, et les **21 fiches AN publiées** en perdent de 41 % à 86 % une fois filtrées — `REN-16` 4 317 → **2 187** (132 738 interventions écartées), `LAREM-15` 4 451 → **640**, `LR-16` 2 631 → **1 265** ; **l'issue croyait la chose impossible** — « `tags_thematiques` est une liste de chaînes, sans provenance : il n'y a rien à filtrer » — ce qui est vrai de la liste et faux du champ : depuis #710 il est **dérivé** des `interventions[]` par une fabrique unique, et rappeler `deriver_tags_thematiques` sur les interventions retenues suffit, **0 écart** entre la liste publiée et ce que la fabrique rend, mesuré sur les 1 035 profils publiés — ce qui referme du même coup la crainte de « rouvrir le repli documenté », lequel est **dans** la fabrique ; la législature se lit sur l'identifiant (`R5L(\d+)`, trois familles : séance, question écrite, Congrès) et **jamais** sur le `source.legislature` de l'entrée, qui manque sur 12 042 interventions et qui, sur 5 168 autres, porte un **terme européen** homonyme d'une législature française — le repli même que #432 a dû lever ; une entrée sans législature lisible est **conservée** et comptée (§2 règle 5), les 5 329 interventions européennes le sont sans effet mesuré puisque **0** ne porte de `theme_officiel` ni de `mots_cles` ; le filtre ne s'arme que si l'appelant nomme une législature, si bien que `parti_profile` garde l'agrégat de carrière. **Deux tests sont tombés et avaient raison de tomber** : ils posaient `tags_thematiques` à la main sur des profils sans aucune intervention — le défaut de #726, une fixture qui décrit le monde comme le code l'imagine. **Écarté** : porter la provenance sur chaque étiquette, qui changerait le schéma des 1 035 profils pour reconstituer ce que le calcul a déjà sous la main. Suite complète à 4 380, 0 échec.
- `2026-09-10` <a id="trois-fiches-de-continuite-815"></a>[Trois fiches, et six lignées qui remontent enfin (#815, lot 2)](decisions/trois-fiches-de-continuite-815.md) — `LR-15`, `GDR-17` et `ECOS-17` sont déclarées, et **trois liens qui ne coûtaient rien** — `REN-16 → LAREM-15`, `LFI-16 → FI-15`, `SOC-16 → SOC-15` — ferment trois lignées de plus sans collecter personne : six lignées remontent désormais, et `RN` s'arrête à la XVIe parce que le groupe **n'existait pas** à la XVe (seuil de constitution), fait publié `None` et non trou (§2 règle 5) ; organes relus dans AMO30 le 10/09 — `PO730934` LR 120 acteurs, `PO845514` GDR 18, `PO845439` ECOS 38 —, **84 personnes à collecter** et non les 116 annoncés la veille, la table de correspondance ayant grossi ; **deux erreurs de mesure corrigées avant d'écrire, de même forme — une valeur voisine, un résultat nul, aucune exception** : `est_mandat_de_transit(fin, constitution)` appelée à l'envers écartait **les 120 acteurs** de LR-15, et `sigles_an` porte le `libelleAbrev` (`ECOS`) et non le `libelleAbrege` (`EcoS`), si bien que le roster ne trouvait **aucun** membre — dans les deux cas c'est un test qui l'a montré, en mesurant 0 contre 38 attendus ; trois tests figeaient l'état d'avant (« les fiches de la XVIe ne succèdent à rien », « les cinq groupes de la XVIIe » deux fois) et le second compte désormais **le total de la table** plutôt qu'une constante. Rien n'est publié avant un run : `effectif_publie` reste null et le portail comptera trois fiches manquantes, régime normal déjà connu de LaREM XVe. Restent MoDem (91 sans profil), Horizons (45), LIOT (50) et UDR (16, une **scission**). Suite complète à 4 372, 0 échec.
- `2026-09-10` <a id="urls-explications-vote-europeennes-827"></a>[L'adresse est dérivée, l'existence est prouvée (#827)](decisions/urls-explications-vote-europeennes-827.md) — les explications de vote sont la seule matière du corpus européen où le candidat dit lui-même pourquoi il a voté, et elles paraissaient **sans aucun lien** : mesuré sur les 7 candidats déclarés, **0 des 1 801 entrées `WEXP`** porte une adresse contre **100 %** des neuf autres types d'activité, le trou est d'un seul type ; la référence est pourtant dans l'intitulé **1 530 fois sur 1 801**, mais **on ne peut pas la vérifier** — `www.europarl.europa.eu` est derrière un pare-feu anti-robot AWS qui rend `HTTP 202` et 0 octet *à l'identique* pour un document réel, pour une référence inventée et pour `CETTE-URL-N-EXISTE-PAS`, y compris avec un User-Agent de navigateur ; d'où la règle : **l'existence est établie sur `data.europarl.europa.eu`**, sans pare-feu, puis l'adresse publique est écrite — la dérivation n'écrit que l'adresse, jamais la preuve, et **1 508 explications (83,7 %) reçoivent un lien pour 0 lien mort**, 271 n'en ayant aucune faute de référence citée et 22 parce que le document n'existe pas, dont **17 transmis par une autre institution** et une coquille de la source (`A9-0390/2017`, terme 9 annoncé en 2017) ; **le cadrage initial de l'issue était sans objet** — il opposait « page lisible » (31,7 % des références) et « PDF seul » (66,9 %), or ces taux ne mesurent que les formats de distribution du portail : les six documents « PDF seul » testés **s'ouvrent tous** sur le site public ; équivalence « portail 200 ⟺ page publique » éprouvée sur **18 URL ouvertes une à une dans un navigateur** par la propriétaire, seule façon de franchir le défi anti-robot — 13 confirmées s'ouvrent, 5 contrôles négatifs échouent, **0 divergence**, 2015→2024, types `A`/`B`/`RC` — mais **éprouvée n'est pas surveillée** : 18 URL, pas 1 508, et le pare-feu interdit tout contrôle continu ; le portail, lui, **ne déclare son débit qu'en le refusant** — aucune en-tête sur un 200, puis `429` + `Retry-After: 60` : une première passe sans pause a rendu **1 320 réponses 429 sur 1 424**, mesure entièrement perdue, la reprise à 0,6 s ayant coûté 47 minutes et 13 blocages, d'où le cache disque et le respect du `Retry-After` ; `existe()` rend **`None` et non `False`** quand la question n'a pas pu être posée (§2 règle 5), et sans résolveur aucune adresse n'est écrite. **Écarté** : l'URL de distribution du portail (export Word, et segment `reds_iPlRp` variant par document, donc non dérivable), l'URI ELI (200 partout, en `rdf+xml` — une adresse pour machine), et dériver sans vérifier. Corrige aussi une docstring qui citait « 1 fois sur 190 » : exact **pour Bardella** (189 de ses 190 n'ont pas de référence), faux comme taux du corpus. Suite complète à 4 404, 0 échec.
- `2026-09-09` <a id="amorcage-du-marquage-par-la-fusion-691"></a>[La fusion défaisait le marquage de la collecte : un run vert qui n'a rien basculé (#691, lot 3c)](decisions/amorcage-du-marquage-par-la-fusion-691.md) — le run `34329085168` est **vert**, a committé 2 324 fichiers et **n'a basculé aucune tranche** : zéro suppression sous `raw_data/profiles/*/1[456].json`, aucun `derivee` committé, les 5,55 Go intacts ; **les shards avaient pourtant marqué** — vérifié dans l'artifact de `gabriel-attal` — et c'est la **fusion** qui a défait ce marquage : `merge_raw_dirs` lisait l'acteur par `_acteur_du_socle(out_dir, slug)`, le socle de **destination**, jamais marqué, donc `None`, donc aucun marquage, donc tranches réécrites ; le principe du lot 3b était juste — « la fusion ne résout pas d'identité, elle reconduit ce que la collecte a déclaré » — et l'implémentation lisait le mauvais fichier, la destination au lieu des **sources** ; **troisième run vert sans effet de la journée** après #786 (transport avalé par `continue-on-error`) et #788 (branche toujours `False`), et rien ne pouvait rougir puisque **ne pas marquer est un comportement légitime** — le défaut prudent posé exprès au lot 3b —, un non-effet et un défaut prudent étant indistinguables de l'extérieur ; le test ajouté ne vérifie donc pas une absence d'erreur mais un **effet** — un socle source marqué reste marqué après fusion et sa tranche disparaît du disque —, et il échoue sur le code d'avant ; **la bascule sera progressive** : `merge_raw_dirs` ne réécrit que les slugs présents dans les artifacts, chaque profil bascule le jour où sa collecte a réussi — plus lent qu'en une fois, et plus sûr ; **le run de TEST qui précédait portait le même défaut sans pouvoir le montrer** — son artifact était marqué, sa fusion l'a défait —, pour deux raisons dont la première n'est pas technique : **on n'a pas cherché l'effet** (la vérification a porté sur la liste annoncée, qui ne contenait pas « des tranches ont disparu »), et **le log ne le donnait pas à voir** puisque l'effet vit dans le commit et qu'un run de test ne committe pas ; `merge_raw_dirs` imprime donc désormais ce qu'elle a fait aux tranches — dérivées, en fichier, retirées ce run —, **uniquement** quand il y a quelque chose à dire (#510) et avec un compte de retraits **mesuré** socle avant/après plutôt que déduit du nombre de dérivées, un profil déjà basculé ne supprimant plus rien : **un mode de test ne vaut que ce que le log donne à voir**. 4 tests neufs, suite complète à 4 295, 0 échec.
- `2026-09-09` <a id="audit-compte-les-tranches-derivees-691"></a>[« Collecté = publié » compte une tranche dérivée dans l'archive (#691, lot 3a)](decisions/audit-compte-les-tranches-derivees-691.md) — le garde-fou de #511 lit la forme **sur le disque** (`if not dossier.is_dir(): return releve`), et une tranche dérivée n'a pas de disque à observer : dès la première, il rendait **0** — exactement ce que son propre docstring donne comme le défaut à éviter (« aveugle sur 96,7 % du volume ») ; il compte désormais **dans l'archive**, et le principe est préservé, ce qui autorise l'exception — l'interdit est de recopier le `nombre` que le manifeste **annonce** (#576, #579), l'archive n'étant pas le document contrôlé mais une source versionnée et close où le compte est **mesuré** ; un test l'établit en déclarant `nombre: 999` pour trois amendements réels ; `signatures()` et non `reconstruire_tranche()` — compter n'a besoin que de l'index par acteur, 10,5 Mo au lieu des centaines du store, sur un contrôle qui boucle sur tout le corpus, et un test vérifie que le store n'est pas ouvert ; une tranche **déclarée dérivée et non dérivable lève**, le taire ferait passer un déficit pour une absence ; **deuxième préalable déjà satisfait** — le sparse-checkout d'`extract-an` porte déjà `raw_data/amendements_an_figes` (ajouté pour #696) et les deux autres jobs lisant des bruts font un checkout complet, vérifié plutôt que supposé ; **troisième préalable laissé en arbitrage** : le `nombre` d'une tranche dérivée doit-il valoir le compte de la **collecte** (une divergence rend alors le profil **illisible**, déplaçant l'erreur loin de sa cause — le patron que #771 a payé 1 h 18) ou celui de l'**archive** (la lecture tient, mais il faut dire OÙ l'écart se signale, sans quoi il devient muet, #510) — les deux coïncident aujourd'hui, 854 tranches sur 854. **Rien n'est marqué, rien ne cesse d'être écrit, les 5,55 Go sont intacts.** 8 tests neufs, suite complète à 4 221, 0 échec.
- `2026-09-09` <a id="barre-candidats-ordre-et-mandat-328"></a>[La barre des candidats : ordre alphabétique, et ce que la fiche peut montrer (#328)](decisions/barre-candidats-ordre-et-mandat-328.md) — **le tri est fait à la source** (`sync-data.mjs`) et l'UI n'en refait aucun, deux tris pour une même liste étant deux listes qui divergeront ; il porte sur `nom`, **ce que le lecteur lit**, et non sur un patronyme reconstruit — découper « Le Pen » ou « Dupont-Aignan » demanderait une règle que la source ne donne pas —, avec `sensitivity: 'base'` pour que « Édouard Philippe » se range avec les E ; **une pastille grisée dit que la fiche ne porte ni mandat à l'Assemblée ni fonction gouvernementale**, donc ni vote ni intervention ni amendement à publier — un fait sur CE QUE LA FICHE MONTRE et jamais un rang (§2 règle 1), tenu par trois choses : la pastille reste cliquable et sélectionnable (ni `disabled`, ni retrait), son infobulle écrit ce que le grisé veut dire, et la teinte est celle des métadonnées (5,5:1, AA) et non une couleur de jugement ; **deux faits, aucun deviné** — `chambres` contient `"AN"` (champ dérivé, #493) ou un mandat de catégorie `fonction_gouvernementale` —, et **le Parlement européen n'est pas l'Assemblée** : quatre candidats y ont toute leur carrière et aucun intitulé ne le dirait ; mesuré sur les 30 candidats du manifeste, **16 ont l'un des deux, 14 n'ont ni l'un ni l'autre**, et **Ségolène Royal n'est pas grisée** malgré zéro vote publié parce que le critère porte sur ce qu'elle a EXERCÉ et non sur ce que nous avons COLLECTÉ ; coût mesuré : `sync-data.mjs` lit les 30 profils (39,8 Mo) et passe de 6 à **8,4 s** ; **alternative écartée** — les masquer comme une candidature déclinée, ce qui ferait porter à l'interface un jugement de pertinence et rendrait invisible un fait publiable. 9 tests neufs.
- `2026-09-09` <a id="constats-parltrack-perimes-683"></a>[Un constat « aucune donnée » ne survit pas à l'arrivée de données (#683, lot 3)](decisions/constats-parltrack-perimes-683.md) — trouvé en répondant à « la nouvelle pipeline ne va rien écraser ? » **avant** de lancer un run réel, et deux défauts auraient publié des affirmations fausses sans rien faire rougir : `unir_warnings` garde un message de l'ancien écrivain dont la famille n'est pas représentée par le nouveau — et un run qui **trouve** des données n'émet rien dans la famille « aucune donnée », si bien que `jordan-bardella` aurait publié « ParlTrack : aucune donnée trouvée pour le député européen 131580 » **à côté de 1 926 votes, 525 amendements et 255 interventions** ; et les deux constats de couverture du lot 2, qui portent des **compteurs**, n'avaient pas été déclarés dans `FAMILLES_WARNINGS` — vérifié : **12 000 et 18 709 publiés côte à côte** sur le même profil, exactement ce que cette table existe pour prévenir ; d'où `retirer_constats_parltrack_perimes`, sur le patron de `clean_stale_interventions` — une entrée écrite sous un régime révolu se retire **sur preuve** (une entrée portant `institution: "parlement_europeen"`) et non sur le succès d'un appel, qui ne survivrait pas au run suivant (#484) —, appelée en **toute fin** de `merge_pivot_profile` parce qu'appelée plus tôt elle jugerait sur un corpus incomplet, et **se levant d'elle-même** puisque l'enrichissement republie le constat dès qu'il ne trouve plus rien ; **écarté** : faire émettre un message positif dans la même famille pour chasser l'ancien — un avertissement qui annonce que tout va bien n'a pas de destinataire, et ferait de `meta.warnings` un journal de succès alors qu'il est le véhicule de « donnée manquante = donnée manquante ». **Quatrième fois de la journée** qu'un contrôle d'effet trouve ce qu'une suite verte laisse passer. Suite complète à 4 346, 0 échec.
- `2026-09-09` <a id="contact-et-comptes-publics-328"></a>[Le contact et les comptes publics : les mentions légales, et un rappel discret (#328)](decisions/contact-et-comptes-publics-328.md) — l'adresse devient `contact@empreinte-politique.fr` et les comptes LinkedIn et X sont ajoutés ; **les mentions légales font foi** — c'est la page que la LCEN désigne —, les comptes y prenant leur propre intitulé plutôt que d'être accrochés à la ligne de contact, un compte n'étant pas une adresse de contact éditeur au sens de l'article 6-III ; **le pied de page les rappelle en discret**, parce qu'une adresse qu'il faut chercher dans une page légale est une adresse qu'on n'écrit pas — cinq entrées à la teinte des métadonnées, qui passent à la ligne plutôt que de pousser le pied hors de la colonne ; **la citation datée de `licences.md` n'est PAS réécrite** — réécrire un enregistrement pour qu'il colle au présent rend les décisions illisibles a posteriori — mais elle porte désormais la note qui dit où trouver le texte qui fait foi, une adresse périmée recopiable de bonne foi étant pire qu'une adresse absente ; un test tient les deux bouts, et un autre vérifie `rel="noopener"` partout où `target="_blank"` apparaît. 4 tests neufs.
- `2026-09-09` <a id="cout-reel-du-run-de-test-792"></a>[Ce qu'un run de test coûte vraiment : 51 min, et pourquoi le mode ne peut pas faire mieux (#792)](decisions/cout-reel-du-run-de-test-792.md) — le mode de #792 a été exercé pour la première fois (run `34323318020`) et a fait ce qu'il annonçait — **2 shards au lieu de 27**, roster à **1 shard** plafonné à 8, quatre garde-fous verts, **aucun commit** — mais il a duré **51 minutes** quand j'en avais annoncé « ~20-25 », chiffre **jamais mesuré** ; la cause est structurelle : **`merge-and-pivot` fait la moitié du run (25,8 min) et ne peut pas être réduit**, puisqu'il refait les deux passes pivot, les agrégats et les quatre contrôles sur **tout le corpus** — c'est précisément ce qu'un run de test vient exercer, le réduire supprimerait ce qu'on cherche à vérifier —, et quatre autres jobs (amendements AN 7,6 min, UE 7,5, ParlTrack 6,8, matrice roster 6,8) ne dépendent pas du périmètre des candidats ; **gain réel : 1 h 15 → 51 min, un tiers et non les trois quarts**, portant sur la moitié qui échoue vite — une matrice mal calculée ou un artifact qui n'arrive pas (#786) se voit en 25 min contre 55, un défaut *dans* la fusion coûte le même temps qu'avant ; ce que ça change n'est pas l'utilité du mode (il venait d'exercer le chemin d'écriture de #691 sans rien committer, juste avant le run qui supprime 5,55 Go) mais **ce qu'on peut lui demander : il n'y a pas de boucle courte dans ce pipeline**, et la seule qui le soit reste le test unitaire ; l'alternative « aval seul » de #792 reste écartée pour la seule raison qui tenait — elle n'aurait pas vu #786 —, son coût annoncé étant faux lui aussi.
- `2026-09-09` <a id="couverture-europeenne-tranchee-683"></a>[La couverture européenne n'atteignait pas la fiche (#683, lot 4)](decisions/couverture-europeenne-tranchee-683.md) — constaté sur le **run `34377413730`**, vert et committé, en lisant les profils un par un : **3 profils sur 6** (`raphael-glucksmann`, `florian-philippot`, `lydie-massard`) publiaient leurs votes du Parlement européen **sans la borne de fraîcheur** arbitrée le matin même, et **6 sur 6** portaient « ParlTrack (fallback) : dumps absents ce run » à côté de 1 977 votes venus de ces dumps ; **le garde-fou de #602 n'a pas échoué, il manquait un cas** — `extract-ue-officiel` écrit une couverture sans volet européen, `merge-and-pivot` en écrit une avec, même jour et même rang, donc la règle 4 conservait la couverture déjà publiée et les profils le déclaraient sous « couverture divergente non tranchée » ; d'où une **cinquième règle, avant la non-décision** : à date et rang égaux, l'écrivain qui couvre **strictement plus de sources** l'emporte — un sur-ensemble strict n'est pas une contradiction, il dit tout ce que l'autre dit plus une source de plus —, avec la borne explicite et testée que deux jeux de sources simplement **différents** restent non tranchables, sans quoi la règle redeviendrait le « dernier qui parle a raison » que #602 a retiré ; la source se lit sur `couverture[].source`, **facultatif et dont l'absence signifie « Assemblée nationale »** (100 % des entrées d'avant ce lot), jamais sur le texte de la preuve ; et la reprise de la veille est étendue au **troisième** constat ParlTrack — l'énumération était incomplète, pas la règle. **Aucun des deux n'aurait fait échouer un run**, et aucun n'a été trouvé par un test : seulement en relisant les profils committés. Suite complète à 4 366, 0 échec.
- `2026-09-09` <a id="larem-xv-apres-691-779"></a>[LaREM XVe entre : la condition posée par #779 est levée (#779)](decisions/larem-xv-apres-691-779.md) — #779 avait scindé ce groupe **sur son coût de stockage** (227 profils, ~3,4 Go, pour un seul bénéficiaire) avec une condition explicite : #691 ; celle-ci est traitée — le run `34344178203` a retiré **854 fichiers de tranches** et `raw_data/profiles` est passé de **9,7 à 4,6 Gio** —, donc un membre de la XVe ne coûte plus que son socle ; l'organe a été **relu dans l'index AMO30 le 09/09**, pas repris de l'issue : `PO730964`, `libelleAbrev` **LAREM**, « La République en Marche », 2017-06-27 → 2022-06-21, `positionPolitique` **Majoritaire**, **367 mandats pour 343 acteurs distincts**, et **343 après filtrage des transits** — aucun transit sur cet organe, contrairement aux `NI` de la 16e (576) et de la 17e (577) ; **un chiffre de #779 était périmé** : 131 des 343 ont déjà une entrée de correspondance, il en reste **212** et non 227, le corpus ayant grossi pendant l'attente — le même effet que #777 avait constaté sur #706 ; le bénéficiaire direct reste **Attal seul** (268 votes sur la XVe, sa seule autre période comparable à ses 1 766 de la XVIIe), mais le vrai motif est celui que #777 n'avait pas nommé : les cinq fiches XVe publiées sont **toutes d'opposition**, et le site publie cette législature **sans sa majorité** ; **écarté** : publier avec les 131 déjà couverts — le taux de participation se calcule sur les membres *ayant un profil* et afficherait un dénominateur de 131 pour un groupe de 343, ratio dont le dénominateur ne nomme pas sa population (§2 règle 7), rien ne le bloquant techniquement puisque le seuil du portail est à 1 membre ; les 212 sans entrée reçoivent un slug fabriqué au roster (#527, #708 §3) puis leur entrée dérivée (#715), chemin qu'ont pris les cinq groupes de #777 dont `FI` publie ses **17 membres sur 17** ; le portail reste **rouge jusqu'au run** (`AN:LAREM:15: fichier manquant`), régime normal d'une publication configurée. Suite complète à 4 295, 0 échec.
- `2026-09-09` <a id="lecture-dumps-parltrack-683"></a>[Le lecteur des dumps ParlTrack n'avait jamais lu une ligne (#683, lot 1)](decisions/lecture-dumps-parltrack-683.md) — `iter_ndjson_zst` lisait en **NDJSON** des dumps que ParlTrack publie en **un seul tableau JSON, séparateur en tête de ligne** (`[{…}`, puis `,{…}`, puis `]`), format que la page /dumps décrit en toutes lettres depuis toujours ; `json.loads` échouait sur chaque ligne et l'échec partait dans un `except JSONDecodeError: continue`, si bien que **les 7 candidats déclarés à mandat européen publiaient « ParlTrack : aucune donnée trouvée » sur eux** — une panne présentée au lecteur comme un constat (§2 règle 5, patron de #484/#510) ; **la preuve dormait sur disque** — `.cache/parltrack/index_*.json` à **2 octets** (`{}`) en face de 182 Mio téléchargés à chaque run depuis un an — et **la suite était verte**, ses 27 fixtures étant écrites dans le format que le code imaginait (#726) ; d'où quatre choses et non une : `iter_dump_zst` au format publié, et surtout **`DumpParltrackIllisible`**, levée quand un dump porteur de lignes ne rend aucun enregistrement — le compteur porte sur les lignes et non sur la taille, un `[]` compressé pesant 12 octets non nuls, défaut trouvé par les tests du lot ; **un périmètre d'indexation obligatoire**, parce que réparer sans borner écrivait **2 675 293 entrées, ~0,5 Go** dans le cache téléversé à chaque run (le défaut ne s'en apercevait pas, l'index sortait vide donc gratuit), avec l'empreinte du périmètre **dans le nom du fichier** (#505) et `_perimetre()` qui rend l'**union** — jamais un index plus étroit que demandé (#510) ; **`role_signataire` cessé d'être `auteur_principal` pour tout le monde** — l'amendement `A9-0183/2023-18` de Bardella compte **30 signataires**, médiane 3, maximum 127 — et se lit désormais en trois cas, le quatrième restant **`null`** puisque l'ordre de `meps` ne reproduit celui d'`authors` que dans **91 %** des 1 257 735 amendements vérifiables ; **44 dates impossibles** (an 0302, an 2068) publiées `null` + `date_non_resolue` ; **deux dumps de plus** (`ep_votes`, `ep_mep_activities`, +58 Mio), liste centralisée dans `DUMPS_LUS` et lue par le YAML au lieu d'y être recopiée. Effet mesuré sur les dumps publiés, 78 s pour les sept : **7 828 amendements** et 12 textes portés, chiffres qui reproduisent exactement ceux de l'investigation du 04/08/2026 ; les deux nouveaux lecteurs rendent **100 017 positions de vote** en 36 s. **Deux chiffres annoncés le 09/09 sont corrigés** : la répartition des 21 356 signatures est de 7 828 déclarés / 13 528 roster, et non 10 013 / 11 343. Ne stocke ni votes ni activités — l'emplacement se décide après la maquette de fiche. Suite complète à 4 330, 0 échec.
- `2026-09-09` <a id="marquage-tranches-derivees-691"></a>[L'écriture marque les tranches closes, et le `nombre` vient de l'archive (#691, lot 3b)](decisions/marquage-tranches-derivees-691.md) — **le lot qui cesse d'écrire** : `partitionner(profil, acteur_ref=…)` marque `derivee` toute tranche de législature close que l'archive couvre, elle sort du dictionnaire rendu et `ecrire_profil_brut` la supprime **par le nettoyage des « non attendus » qui existait déjà**, sans une ligne de plus ; sans `acteur_ref` rien n'est marqué et le comportement est celui d'avant à l'octet près — `generate_all_profiles` le transmet depuis la table relue, `merge_profile` depuis le manifeste déjà écrit (**la collecte amorce, la fusion reconduit**) ; **le `nombre` vient de l'archive**, arbitrage de la propriétaire : les deux comptes coïncident aujourd'hui (854 tranches sur 854) et le choix porte sur le jour de la divergence — le compte de la collecte ferait lever `recomposer` et rendrait le profil **illisible**, déplaçant l'erreur très loin de sa cause (#771, 1 h 18), quand l'archive laisse la lecture tenir et fait déclarer l'écart **là où il naît**, par `nombre_collecte` écrit **uniquement** en cas d'écart (un champ qui n'apparaît que quand ça ne va pas se remarque, #510) ; **l'ordre devient par blocs** dès qu'une tranche est dérivée — elle est relue dans celui de l'archive (16 915 positions sur 16 915 différentes) — et ça ne coûte rien de publié, aucun consommateur ne lisant l'ordre, tandis que l'aller-retour octet pour octet reste vrai pour tout profil **sans** tranche dérivée ; **mesuré sur `mathilde-panot`** : **59,6 → 28,2 Mo (−53 %)**, seule la tranche 17 subsiste, **54 559 amendements relus pour 54 559**, ensemble identique, et **aucun `nombre_collecte`** ; **un OOM killer en route** — `signatures()` chargeait aussi le store alors qu'elle n'a besoin que de l'index par acteur (10,5 Mo contre plusieurs centaines), les mémos sont désormais séparés par fichier, et le test qui prétendait le couvrir ne vérifiait que l'absence d'appel à `reconstruire_tranche` : **un test qui vérifie l'appel n'a rien dit du coût** ; le premier run sous cette forme supprimera **5,55 Go**, la XVIIe (3,64 Go) restant écrite faute d'archive. 12 tests neufs, suite complète à 4 235, 0 échec.
- `2026-09-09` <a id="page-couverture-commune-328"></a>[Une page de couverture commune, et deux mentions de fiche allégées (#328)](decisions/page-couverture-commune-328.md) — suite de [`pourquoi-en-methodologie-328`](decisions/pourquoi-en-methodologie-328.md), qui avait sorti le raisonnement de la fiche : **160 des 239 entrées de couverture sont identiques sur les 27 fiches** — bornes AMO30, scrutins, Syceron, preuves mot pour mot —, et ce qui varie est minoritaire et mesurable (`fait_etabli` 35, `non_collecte` 26, `textes_portes hors_couverture` 19) ; elles partent sur **`/couverture`**, deux parties et pas davantage — la frise « ce que le dépôt porte » et le tableau « ce qui manque, et sur quelles fiches » ; **l'institution est le premier niveau** (Assemblée, Gouvernement, Sénat, Parlement européen, mandats locaux), ses listes en dessous, les champs au repli, et **la fiche d'origine est une teinte, jamais un niveau** : trois fiches portant le même fait sur la même période vivent dans le **même rail**, superposées en `mix-blend-mode: multiply` — l'inverse a été essayé et rejeté, une piste par origine mettant trois lignes là où il y a un fait ; **la scission parlementaire/gouvernemental est lue sur trois champs publiés** et jamais sur un intitulé (#639) — `categorie === 'fonction_gouvernementale'`, `role === 'initiateur_projet_de_loi'`, et la qualité `fonction` du compte rendu, qui nomme une fonction gouvernementale sur **6 201 des 21 725 interventions** : elles se scindent donc comme les textes portés, 15 524 sous l'Assemblée et 6 201 sous le Gouvernement ; **la borne dessinée est celle que la source déclare**, pas le minimum des dates rencontrées (qui la rendrait tautologique), et la hachure s'arrête au plus tôt des deux — mesuré : la borne des textes portés dit le **21/06/2017** quand le corpus en porte depuis **12/2010**, hachurer jusqu'en 2017 effacerait sept ans de faits ; **les deux mentions de fiche gardent leurs chiffres et renvoient le pourquoi** à `/methodologie#votes` et `/methodologie#interventions`, où les deux paragraphes existaient déjà mot pour mot ; **la projection est calculée au build** (`scripts/couverture-corpus.mjs` → `public/data/couverture.json`, 32 Ko), comme les comparaisons de groupe (#329), avec un cache sur les dates plutôt qu'un drapeau — lire les quatre index d'amendements coûte 17 à 28 s et le refaire à chaque `npm run dev` serait intenable ; **deux indexations fausses trouvées en écrivant** : `scrutins.json` porte une liste et `scrutins_dossiers.scrutins` une table vers une chaîne, les traiter en objets rendait tous les votes vides sans lever la moindre erreur ; **alternative écartée** — garder la section complète sur chaque fiche et n'ajouter que la page, moindre risque et défaut intact, une limite de source affichée sous le nom d'une personne se lisant comme une limite de cette personne. 14 tests neufs, suite complète à 4 309, 0 échec.
- `2026-09-09` <a id="paroles-par-periode-328"></a>[« Ce qu'il a dit » publie les mots, et les range par période politique (#328)](decisions/paroles-par-periode-328.md) — la section publiait **quatre listes de totaux de carrière** et un paragraphe de méthode : elle disait comment on sait, jamais ce qui a été dit, alors que le corpus porte **16 188 verbatims** du compte rendu pour 21 725 interventions (10 des 27 candidats déclarés) ; elle devient **période politique → nature → sujet → le fil**, avec le **même découpage que « Ce qu'il a voté » par construction** — `parolesParPeriode.js` appelle `bancALaDate` et `gouvernementALaDate` de `votesParPeriode.js`, deux sections qui découpent le temps pareil le faisant au même endroit ; **le fil reste fermé tant qu'aucun sujet n'est choisi**, non pour la longueur mais parce que la première phrase d'une liste ouverte est de fait une phrase mise en avant (§2 règle 1), et une fois ouvert il publie **toutes** les interventions du sujet, dans l'ordre ; **le sujet ne se lit pas au même niveau selon le type** — l'ordre du jour est un chemin « racine › étape › article » dont la grammaire change : une seule racine couvre **2 885 des 3 660** questions au gouvernement (« Questions au Gouvernement », un créneau de séance) quand ses 756 feuilles sont « Réforme des retraites » ou « Affaire Benalla », et c'est l'inverse sur l'examen d'un texte (105 racines qui nomment des textes, feuilles à « Suspension et reprise de la séance ») — **lire un niveau du chemin n'est pas rapprocher deux libellés** (#639) : « Motion de censure » et « Motions de censure » restent deux entrées ; **deux échelles, toutes deux nommées** — plafond de la fiche sur une période (un filtre retire de la masse, il ne redimensionne pas), plafond de l'ensemble sur « toutes les périodes », que le libellé déclare non comparable ; **`format` n'est jamais publié** bien que disponible sur 16 242 lignes, parce que c'est **notre** déduction (seuil de cinquante mots dans `parse_syceron.py`) et non un fait du compte rendu ; **la navigation devient partagée** (`NavigationPeriodes`) à la demande de la propriétaire, ce qui sort l'écouteur clavier de `window` (à deux sections, une flèche déplaçait les deux) et corrige un défaut livré — les deux flèches désactivées écrivaient « début de la période couverte », faux à droite ; **ce qui part** : le régime de qualité en pavé, `interventionsParNature`, la liste des fonctions, le bloc des questions au gouvernement ; **ce qui reste** : `regimeQualiteOrateur` et `directionQuestionsGouvernement`, que « En bref » consomme, et le raisonnement, passé en méthodologie sous l'ancre `#interventions` ; **les trous sont publiés** — 17 738/21 725 intitulés, 16 188 verbatims, 6 447 qualités, 264 sans date exploitable, et **5 483 en régime `theme_seul`** (Ruffin, Faure, Brun entiers), déclaré sous chaque entrée. 31 tests neufs, suite complète à 4 266, 0 échec.
- `2026-09-09` <a id="pied-de-site-et-section-6-328"></a>[Un pied de site, un badge qui dit ce qu'il fait, et §6 qui cesse de répéter le corpus (#328)](decisions/pied-de-site-et-section-6-328.md) — trois défauts de la même famille, tous mesurés sur la page rendue : **il y avait trois pieds** — accueil, explorateur, et **rien du tout** sur les pages statiques —, donc un contact absent de la méthodologie, des mentions légales et de la couverture ; un **seul composant** les remplace sur les quatre carcasses, **l'adresse écrite en entier** parce qu'elle se copie (coût mesuré et assumé : le pied passe de 66 à **148 px** partout), **les pages en mots et les comptes en icônes** — la forme dit la nature du lien —, chaque icône gardant son `aria-label` en toutes lettres et une cible de 30 px : elle remplace le libellé **à l'écran**, jamais pour un lecteur d'écran ni pour le doigt ; **le badge cesse de dire « vérifiée »**, un contrôle que nous ne faisons pas — et **« Source officielle » a été écarté sur mesure** : sur les 23 499 liens publiés des 32 fiches candidats, 19 fiches de groupe et 10 fiches de gouvernement, 22 988 pointent vers l'Assemblée ou le Parlement européen mais **511 vers nosdeputes.fr**, un tiers — le mot aurait été faux 511 fois, quand « Lien de source non publié » ne bouge pas parce qu'il parle de NOUS ; **§6 rend l'état daté et plus la borne**, dont le discriminant est l'**état** garanti par `couverture_profil._deriver` et non une reconnaissance du texte (#639) — partition exacte sur les 32 fiches, **180 entrées de borne sur 286, soit 6 413 des 8 328 mots rendus** ; **la mémoire des preuves devient celle de la SECTION, ce qui renverse #802**, dont la raison — ne pas faire disparaître la borne AMO30 de « Votes » — tombe avec la borne, et ce qui restait se répétait cinq fois pour rien : **700 mots sur la fiche Retailleau**, 140 par liste pour le seul certificat de suspension ; **deux affirmations retirées parce que fausses** — « rangés sous le même rôle « auteur » … aucun champ ne la porte », alors que `role` sépare projets et propositions sur **570 des 575** et que la phrase était publiée **deux fois**, et « dont la législature en cours », écrit en dur et vérifié nulle part (faux sur Retailleau) ; le **bloc Assiduité / Classement / 49.3 quitte la fiche**, « aucun classement » y apparaissant **trois fois** sur une même page ; mesuré après : Retailleau 870 → **237** mots, Tondelier 441 → **133**, Mélenchon 358 → **185**, et « aucun classement » 3 → 1, « taux de présence » 2 → 0 ; **alternative écartée** — replier les bornes derrière un `<details>`, zéro risque et défaut intact, la borne n'étant pas trop longue mais **au mauvais endroit**. 12 tests neufs, suite complète à 4 324, 0 échec.
- `2026-09-09` <a id="pourquoi-en-methodologie-328"></a>[Le « pourquoi » descend en méthodologie, la fiche garde la limite (#328)](decisions/pourquoi-en-methodologie-328.md) — un audit de la **page rendue** (titres de textes et de commissions exclus, ce sont des libellés de source) a mesuré **1 772 mots de notre copie** sur `jerome-guedj`, dont six blocs au-dessus de DESIGN_SYSTEM §7 règle 2 — le critère de « Ce qu'il a voté » à **58 mots** en tête — et deux modes d'emploi qui ne s'accordaient même pas sur le vouvoiement ; arbitrage de la propriétaire : le raisonnement va en méthodologie, la fiche garde la limite et le lien, et **chaque section de la fiche a son ancre, une seule** (`fonctions`, `propose`, `votes`, `ecarts`, `interventions`, `couverture`) — « Textes portés » et « Amendements » étaient deux sections pour un seul emplacement et sont réunies sous « Ce qui est proposé » ; **le critère des votes perd la règle de dernière lecture**, publiée SOUS la figure là où #711 la veut, et c'est cette répétition qui le portait à 58 mots ; **les trois refus gardent leur phrase et perdent leur `pourquoi`** (71 mots), qui n'est pas recopié — les deux pages rendent le même `STATED_REFUSALS` ; **les preuves de borne ne bougent pas**, ce sont des preuves et non de la copie ; mesuré après : **1 562 mots, 210 de moins**, aucun bloc au-dessus de 27, et un test refuse un critère au-delà de 22 mots comme un renvoi vers une ancre inexistante ; **alternative écartée** — raccourcir sans créer d'ancres, deux fois moins de travail et le coût déplacé sur le lecteur, qui rouvre douze sections pour retrouver la règle. 8 tests neufs.
- `2026-09-09` <a id="preuve-de-borne-dite-une-fois-328"></a>[Une preuve de borne se dit une fois par liste (#328)](decisions/preuve-de-borne-dite-une-fois-328.md) — un audit de verbosité de la fiche a trouvé autre chose que de la verbosité : la **même preuve de borne s'imprimait deux fois par ligne** dans « Ce qu'on n'a pas pu lire » — **148 mots en double** sur `jerome-guedj` et `marine-le-pen`, **197** sur `edouard-philippe` —, parce que `couverture_profil._deriver` écrit deux entrées par liste (« couvert depuis », « hors couverture jusqu'au ») et qu'une même borne explique les deux ; **le dédoublonnage ne pouvait PAS se faire à la source** : la première entrée porte une preuve différente dès qu'un fait « hors AN » est établi, et sur les 27 candidats déclarés, 135 listes portant au moins une preuve, **69 répètent la même mais 35 en portent de différentes** — supprimer la seconde effacerait un fait dans la moitié des lignes concernées ; la preuve reste donc sur chaque état et c'est l'affichage qui ne répète pas (`preuveDejaDite`), la marque disant « ne la répète pas » et non « cet état n'a pas de preuve » ; **le détail qui décide de la justesse** est que la mémoire est remise à zéro par liste — partagée, la borne AMO30 disparaîtrait de « Votes » parce que « Mandats et fonctions » l'a déjà écrite —, et un test le tient parce que c'est la seule façon de se tromper qui ne se voie pas à l'écran sur un profil ordinaire ; **aucun texte n'est touché**, la réduction des six blocs au-dessus de la limite de DESIGN_SYSTEM §7 règle 2 étant un lot séparé. 5 tests neufs, suite complète à 4 271, 0 échec.
- `2026-09-09` <a id="reconstruction-tranches-depuis-archive-691"></a>[Reconstruire une tranche d'amendements depuis l'archive figée (#691, lot 1)](decisions/reconstruction-tranches-depuis-archive-691.md) — `raw_data/profiles/` pèse **9,7 Gio dont 8,6 (89 %) de tranches d'amendements**, et **aucun champ n'est gros** : `co_signataires` fait **79,5 %** du poids parce qu'un amendement à 74 cosignataires est écrit dans **75 fichiers** (vérifié par `grep -rl`) portant chacun les 75 identifiants — **5 625 identifiants pour une information qui en compte 75**, et 74 cosignataires est la **médiane**, pas un cas extrême ; c'est la duplication que #431 a supprimée au niveau **pivot** (index partagé à 284 Mo) et jamais au niveau **brut** ; la donnée est pourtant déjà là **et déjà inversée** — `raw_data/amendements_an_figes/` porte 624 180 amendements en **38 Mo** gzippés *plus* un `index_par_acteur.json.gz` qui rend exactement `{uid, role_signataire}`, c'est-à-dire la forme que le pivot consomme depuis #431 ; **mesuré : 120 profils, 568 771 amendements, trois législatures, ZÉRO écart** champ par champ entre la tranche committée et sa reconstruction, les deux seuls champs dérivés étant ceux que #691 avait nommés (`role_signataire` vient de l'index, `legislature` du nom de fichier) ; le premier essai avait rendu **un** écart sur 16 915 — un résidu de conversion XML (`{"@xsi:nil": "true"}`) que la collecte normalise en `null`, recensé à **8 entrées sur 624 180**, assez rares pour passer inaperçues et assez réelles pour écrire un objet XML là où le corpus porte `null` (§2 règle 5) ; `mapping_pivot()` va de l'archive à la forme d'arrivée **sans passer par la tranche** — **alternative écartée** : le chemin naïf archive dédupliquée → tranche re-dupliquée → index re-dédupliqué, qui paierait la duplication en calcul après l'avoir ôtée du disque ; `None` ≠ `[]` (l'un dit « on ne sait pas », l'autre est un fait) et une archive à moitié présente est déclarée **absente** (#484) ; **ce lot ne supprime rien et rien ne l'appelle encore** — il publie l'inventaire des **sept** consommateurs du champ `amendements` du brut et laisse deux questions au lot 2 : le **filet de `merge_profile`** (la tranche committée est ce qui protège d'une collecte vide, or la collecte lit un cache CI non versionné) et l'**ordre** (ensemble identique, **16 915 positions sur 16 915 différentes**, quand le manifeste de #580 entrelace les tranches) ; portée : **5,55 Go** des 9,2 Go, la XVIIe étant vivante et sans archive. 16 tests neufs, suite complète à 4 202, 0 échec.
- `2026-09-09` <a id="run-de-test-perimetre-reduit-792"></a>[Un run de test : même workflow, périmètre réduit, jamais de commit (#792)](decisions/run-de-test-perimetre-reduit-792.md) — deux runs consécutifs ont coûté **1 h 13** et **1 h 15** pour révéler un défaut d'une ligne (#786, puis #788 : **71 minutes** pour apprendre qu'un argument manquait), la boucle « modifier → vérifier » n'ayant aucun cran entre le test unitaire (une demi-seconde) et le run complet ; un champ **en tête** du formulaire — un mode se lit avant les réglages qu'il modifie — réduit la matrice `extract-an` aux slugs saisis, plafonne le roster à **8 membres sur 1 shard** quand aucun plafond n'a été demandé, et **désarme le commit** ; **un champ pour trois effets, délibérément** : deux cases indépendantes autoriseraient « périmètre réduit ET commit », c'est-à-dire publier un corpus connu comme partiel, et c'est le seul des trois qui ne se rattrape pas ; **ce mode ne change ni le workflow, ni le code, ni l'ordre des jobs** — il réduit la matrice et rien d'autre, les quatre garde-fous d'avant-commit tournant normalement puisque c'est eux qu'un run de test vient exercer, et un mode qui divergerait du mode réel ne prouverait rien de ce qu'on lui demande (reproche qui ne se répare pas : on ne sait pas après coup ce qu'il n'a pas exercé) ; **il ne remplace pas les tests** — #788 devait être attrapé en une demi-seconde, ce mode existe pour ce qu'aucun test ne voit, l'orchestration ; un slug mal tapé ou gelé (#760) reçoit son `TEST_SLUG_INTROUVABLE` et un périmètre vide échoue **au job de matrice** (`TEST_PERIMETRE_VIDE`) plutôt qu'après une heure — sans quoi « tu as mal tapé » se lirait « rien à collecter » (#510, #771) ; le plafond roster réutilise la mécanique de #467 (`roster_limit > 0` force un shard, la limite valant par shard) et s'**annonce** à chaque run, un plafond implicite invisible étant un plafond qu'on oublie avoir ; **alternative écartée** : le mode « aval seul », même durée et **aveugle à #786** — sans job d'extraction, aucun artifact à transporter, donc aucun transport à éprouver. 16 tests neufs, suite complète à 4 184, 0 échec.
- `2026-09-09` <a id="stockage-europeen-dans-les-listes-existantes-683"></a>[L'européen entre dans les listes que la fiche a déjà (#683, lot 2)](decisions/stockage-europeen-dans-les-listes-existantes-683.md) — trois formes maquettées sur les données réelles de Bardella, la propriétaire tranche **« A enrichi »** : **pas de bloc européen parallèle**, un seul jeu de listes et chaque entrée qui sait d'où elle vient ; **six des sept natures européennes entraient déjà sans un champ de plus** (votes en `scrutin_id: null` + `scrutin_non_resolu` comme les amendements de #431, résolutions en `textes_portes` avec un rôle existant **apparié à `nature_texte`** (#689), interventions et questions en `interventions[]` où l'Assemblée range déjà les siennes), la septième — les **explications de vote**, seule matière où la personne dit elle-même pourquoi elle a voté — recevant un `type_detail` neuf ; **ce qui se publie est la position sur l'ensemble du texte** et rien d'autre, transposition de #711 — Bardella compte **607 scrutins sur le seul dossier `2018/0216(COD)`** et 93 % des intitulés sont procéduraux —, vérifié par la mesure : **1 007 de ses 1 137 dossiers ont exactement un vote retenu**, les pires cas étant des décharges où le Parlement vote réellement deux fois ; les écartés sont **comptés et déclarés** (§2 règle 5, #511) ; **le vocabulaire fermé des natures a dû devenir bilingue** — au passage à la XIe législature la source change de langue ET de tiret, la première version rendait **0 vote retenu sur 2024-2026** (5 012 scrutins pour Bardella) et **aucun test ne pouvait le voir**, toutes les fixtures étant au format d'avant : c'est la **borne de couverture** arrêtée au 19/10/2023 sur une personne siégeant toujours qui l'a révélé, troisième contrôle d'effet de la journée à trouver ce qu'une suite verte laissait passer ; **la couverture dit désormais quelle borne porte sur quelle part** — sans quoi 1 926 votes européens se publiaient sous « l'Assemblée nationale ne publie pas de scrutins avant la XIVe », preuve vraie portant sur rien (§2 règle 2) — l'institution étant lue **sur l'entrée** et la portée **mesurée sur les entrées**, jamais recopiée d'une page de source qui vieillit (#484) ; `COLLECTE_SANS_VERBATIM_SOURCE` ajoutée parce que « la source n'en publie pas » n'est pas « notre run ne l'a pas demandé » (#657) ; et **l'enrichissement remonte avant la dérivation de la couverture**, qui le suivait — un profil européen publiait sa couverture calculée avant l'arrivée de ses 525 amendements, et le chemin de collecte normal n'enrichissait pas du tout. Effet mesuré, schéma validé sur les 7 : **12 939 votes sur un texte**, 4 584 interventions, 397 textes portés. **Le chiffre de 375 annoncé en maquette est corrigé à 1 926** pour Bardella. Pas de fiche de groupe européenne, pas de wiring du périmètre (5 min 45 par run, mesurées). Suite complète à 4 343, 0 échec.
- `2026-09-09` <a id="tranches-derivees-lecteur-691"></a>[Le lecteur accepte une tranche dérivée, et le silence reste une panne (#691, lot 2)](decisions/tranches-derivees-lecteur-691.md) — le lot 1 laissait **deux questions ouvertes** présentées comme des obstacles ; les deux se sont résolues par la mesure et **dans le sens inverse** : (1) l'**ordre** n'en est pas un, `audit_diff_profils` relève une liste par un **entier** (`_listes` rend un `int`) et une perte est une **baisse de compte**, jamais une différence de séquence — le garde-fou a été construit pour voir disparaître des enregistrements, pas pour figer un ordre que rien ne publie ; (2) le **filet de `merge_profile`** est **renforcé** et non affaibli — `old` vaut aujourd'hui « ce qu'un run précédent a réussi à collecter », donc dépend de `.cache/amendements_an/`, un cache CI non versionné, quand `old` dérivé vaut l'**archive committée**, vérité entière d'une législature close à laquelle une collecte ne peut rien ajouter ; **mesuré exhaustivement : 854 tranches sur 854 couvertes** (77/77 en XIV, 270/270 en XV, 507/507 en XVI) ; `profil_brut` sert donc une tranche que le manifeste **déclare** dérivable (`derivee: true` + `acteur_ref`), avec trois propriétés qui répondent chacune à une manière de se tromper — **le manifeste doit le dire et le silence reste une panne** (chercher l'archive « au cas où » ferait lire une tranche **perdue** comme une tranche dérivée et republierait un profil amputé, #580), l'**acteur vient du manifeste** et non d'une jointure (`profil_brut` ne connaît pas la table de correspondance et n'a pas à l'apprendre pour relire un profil), le **`nombre` est un contrôle et non une source** (#576, #579) ; **un défaut trouvé en écrivant** : trois lecteurs indexaient le manifeste par `fichier[: -len(".json")]` recopié à l'identique, et une tranche dérivée n'ayant pas de `fichier`, l'un des trois aurait divergé en silence — `_nom_declaree` est la seule définition, comptée par un test ; **rien n'est encore marqué ni cessé d'être écrit**, le lot 3 devant traiter `audit_collecte_vs_publie` (qui lit la forme **sur le disque** et compterait « 0 amendement collecté »), l'`ordre` écrit par `partitionner`, et le sparse-checkout d'`extract-an` (#674) ; **alternative écartée** : déduire la dérivation de l'absence du fichier — plus court d'une clé, et ça supprime la seule différence entre « plus écrite » et « disparue ». 11 tests neufs, suite complète à 4 213, 0 échec.
- `2026-09-08` <a id="boucle-candidats-quatre-corrections-781"></a>[La boucle du périmètre a cassé quatre fois au même endroit (#771, #775, #781)](decisions/boucle-candidats-quatre-corrections-781.md) — une seule décision pour trois lots parce qu'ils n'ont qu'une histoire : #757 a fermé la boucle et elle a rouvert **trois fois**, sur le même geste — **borner une population à ce qu'on avait sous les yeux en l'écrivant** ; #771 résolvait ce qui est **neuf** quand la passe hors ligne a besoin de ce que **la table ne couvre pas** (1 h 18 de collecte, commit refusé), #775 réservait l'entrée à ceux qui ont un **profil publié** quand cinq candidats n'en auraient jamais eu sans entrée (verrou circulaire, troisième apparition de #715), #781 posait la seconde déclaration à l'écriture du **brut** et pas à la publication du **pivot** (cinq bruts sans pivot, commit refusé) ; le précurseur est #766, dont les trois entrées écrites **à la main** sont ce qui a fait diverger les deux populations de #771 ; corollaire de #771 : un fichier de résolutions s'écrit **même vide**, conditionner une étape sur la **présence** d'un fichier transformant « rien à ajouter » en « ne fais rien » ; garde-fous de #775 : `en_echec` (#484), `indetermine` qui ne vaut pas déclaration, un fichier absent qui ne déclare rien ; **ce que la série apprend** : deux fois sur quatre la prose du dépôt décrivait le piège — la décision de #757 prévenait que #715 était « borné à sa population », et le commentaire de `_normaliser_en_pivot` nommait le défaut mot pour mot —, et **un avertissement écrit ne protège pas celui qui l'écrit** ; ce qui a attrapé les défauts, ce sont les garde-fous **exécutables** (§5b, « collecté mais non publié » #511, la liste blanche du sparse-checkout, un banc refusant un `args` sans attribut) ; d'où la règle : **quand une règle décide d'une population, chercher sa jumelle avant de committer**, les tests vérifiant désormais la paire et non la place ; **non réparé** : les cinq de #775 restent non publiés pour une cause hors série — les artifacts d'extraction n'atteignent pas `merge-and-pivot` —, et le run vert l'est **parce que** la collecte n'arrive pas.
- `2026-09-08` <a id="cadre-fiche-repli-et-sommaire-324"></a>[L'en-tête rend 301 px, et un sommaire prend la marge que personne n'occupait (#324)](decisions/cadre-fiche-repli-et-sommaire-324.md) — l'en-tête restait collé **en entier** : marque plus trois barres, **357 px identiques sur tous les supports** (45 % de la hauteur d'un 1 280 × 800, 42 % d'un mobile), pour une fiche de **7 968 px** — neuf écrans avec un demi-écran utile ; au-delà de 180 px de défilement l'en-tête se réduit à **56 px**, soit **301 px rendus au contenu sur les cinq supports, mobile compris** (contenu au premier écran 371 → 672 px en 1 280 × 800) ; l'ordre des listes devient **candidats, groupes, gouvernements** — une fiche s'atteint par un nom ; un **sommaire de sections à gauche** tient en place, marque la section lue et sert de raccourci — à gauche parce qu'un sommaire s'y lit *avant* le contenu, et il **lit la page** (`[data-section]`) au lieu de recevoir une liste, donc il sert les trois types de fiche et accueille une section neuve sans rien savoir d'elle, l'ancre venant du **numéro** et jamais du titre (qui change avec la voix du texte) ; **le seuil de 1 440 px est une mesure, pas un goût** — la question « comment ça rend sur l'ordinateur de monsieur tout le monde » a changé la réponse : Statcounter France août 2026, 1 440 px couvre **39 % du parc de bureau nommé et 21 % de toutes les visites** contre 13 % à 1 600 px, et **un seuil à 1 600 px aurait exclu les écrans 1 600 × 900 eux-mêmes**, dont la fenêtre fait 1 585 px — frontière posée en pensant à des écrans, réglée sur des fenêtres ; le sommaire **ne coûte rien au contenu**, qui reste à 1 100 px partout ; **trois défauts trouvés en mesurant** — le sommaire restait vide (monté avant la fiche, dont les données arrivent en réseau : `MutationObserver`), retirer son `<nav>` faisait glisser le contenu dans sa colonne de 204 px, et surtout **vérifier un attribut n'est pas vérifier son effet** : une mesure lisait `element.hidden` et déclarait les barres masquées alors que `display: flex` l'emporte sur le `[hidden]` du navigateur — seule la capture l'a montré ; **alternative écartée** : le repli seul, sans sommaire, qui règle le vrai problème mais laisse une page de neuf écrans sans repère alors que le sommaire ne coûte rien. 12 tests neufs, suite complète à 4 147, 0 échec.
- `2026-09-08` <a id="divergences-avec-le-groupe-328"></a>[« Ses divergences » : une bande qui montre sans compter (#328)](decisions/divergences-avec-le-groupe-328.md) — la section publiait la seule **liste** des scrutins où la position d'une personne diffère de celle de son groupe : juste, et **muette pour dix des treize candidats déclarés** — trois ont une base réelle et zéro divergence (Attal 108/0, Le Pen 108/0, Ruffin 56/0), six n'ont aucune fiche de groupe ; **onze faits pour treize pages** ; une **bande** — un scrutin par colonne, dans l'ordre du temps — donne désormais à voir, **sans compter**, si la personne s'écarte et dans quel groupe ; **c'est §2 règle 7 qui décide de la forme entière** — « a voté contre son groupe 47 fois » y est nommé mot pour mot comme l'indice individuel interdit —, d'où trois conséquences : la bande fait lire **d'abord ce que fait le groupe** et la divergence en second (une variante en frise, qui ne montrait qu'elles, a été dessinée puis écartée parce qu'elle met le nombre au premier plan), le compte des scrutins communs **toutes natures** n'est plus affiché — il servait de dénominateur à une division que le lecteur faisait seul, et avec le **mauvais nombre** (2 831 chez Guedj quand la base est **172**) —, et la pastille du commutateur est **binaire** ; **trois faits, trois éléments, jamais deux faits sur le même** : socle coloré = ce qu'a voté le groupe, barre en encre = les membres qui n'ont pas suivi, point cerclé = ses divergences — deux encodages successifs coloraient la colonne par la position majoritaire tout en la dimensionnant par les votes qui ne la suivaient pas, et la propriétaire a nommé l'incohérence avant nous ; le point n'est **coloré que sur une divergence** (colorer les 172 repères faisait de la teinte une texture) et garde un signal de **forme**, deux gris de la palette étant proches ; **les étiquettes passent à gauche**, à la hauteur de leur rang, la gouttière étant payée par l'écart entre colonnes ramené à 1 px — mesuré, une colonne passe de **2,97 à 3,03 px**, la densité ne bouge pas ; « quorum non atteint » devient « **moins de la moitié du groupe s'est exprimée** » (règle vérifiée dans `group_profile.py`, seuil 0,5 sur les éligibles) et le scrutin **n'est pas écarté** — choisir les faits qui arrangent serait pire ; **trois vides, trois causes**, dont une seule est un fait sur la personne ; `ecartsAvecLeGroupe` quitte `profilCandidat.js` pour `utils/ecartsGroupe.js`, `fiches[]` porte les **dates** de `periode` au lieu des comptes, et la méthodologie gagne l'ancre `#ecarts` sans laquelle le renvoi menait à un contenu absent ; **reste ouvert** : les 7 fiches de groupe déclarées par #778 (XVe : FI, NG, SOC, EDS, GDR ; XVIe : ECOLO, GDR) ne sont **pas encore produites** — le jour où un run les écrit, la section gagne des bases pour Mélenchon, Ruffin, Attal et Faure sans qu'une ligne change. 19 tests neufs, suite complète à 4 128, 0 échec.
- `2026-09-08` <a id="groupes-xv-xvi-777"></a>[Sept groupes des XVe et XVIe entrent, LaREM en sort sur son coût (#777, #779)](decisions/groupes-xv-xvi-777.md) — #706 parlait de 2 groupes sans fiche puis de 4 ; remesuré sur les **27 candidats déclarés** avec `correspondance_sigles_an` pour autorité, c'est **19 couples touchés, 4 couverts, 15 sans fiche** — l'écart vient de la population, passée de 13 à 27, et le chiffre du ROADMAP porte lui aussi sur les 13 d'alors ; **le sigle ne se déduit pas, et deux cas sur huit l'ont prouvé** : le mandat dit « Ecolo - NUPES » quand l'organe s'appelle `ECOLO`, « GDR - NUPES » quand il s'appelle `GDR-NUPES`, d'où huit organes relus un par un dans l'index AMO30 ; **LaREM XVe sort sur une mesure** — 343 membres et 227 profils à collecter pour **un** bénéficiaire déjà publié, contre 72 pour les sept autres et six candidats servis —, et le chiffre qui tranche n'est pas le disque mais `merge-and-pivot`, seul job qui écrit : marge tombant de ×2,0 à ×1,5 sur son timeout de 60, quand un job tué par son timeout n'écrit rien (#498) ; **vérifié après coup**, le run `34241352524` a publié les sept fiches en **21,6 min**, marge ×2,8 ; reprise conditionnée à #691 ; sept gardes figeaient un nombre (« 10 entrées », « {16, 17} », « 456 membres ») et lisent désormais ce qu'elles vérifient — l'une cachait un `TypeError` sur `effectif_publie: null`, l'état normal d'une entrée non encore parue ; **ce que le lot n'a pas produit** : les 72 profils, le run n'ayant récupéré aucun artifact d'extraction.
- `2026-09-08` <a id="nom-des-resolutions-vient-de-lappelant-788"></a>[La seconde déclaration lisait un champ que le corpus ne porte pas (#788)](decisions/nom-des-resolutions-vient-de-lappelant-788.md) — le run `34264027824` a transporté ses artifacts (#786 corrigé : **723 profils fusionnés** contre 0) et s'est arrêté au garde-fou de #511, sur **les cinq mêmes** candidats — brut écrit, aucun pivot, « aucune source normalisable en --pivot-only » ; #781 avait posé la seconde déclaration à la place du pivot **en écrivant qu'elle devait être symétrique** de celle de l'écriture du brut, et elle ne l'était pas : l'une passe `nom`, venu de `raw_data/candidats.json`, l'autre passait `profile.get("nom")` — un champ qu'**aucun des 652 profils bruts ne porte**, le nom y vivant sous `identite.nom_complet` —, donc `None` → `""` → `False` **à tous les coups** ; le fichier de résolutions du run était pourtant juste (six entrées, `'François Asselineau' -> hors_an`…), et **la seconde déclaration n'a jamais pu être vraie**, morte à l'écriture, le lot qui la portait passant au vert ; le nom vient désormais de l'**appelant** aux deux places — par construction la clé du fichier de résolutions —, et la symétrie que #781 *décrivait* devient une symétrie que le code *tient* ; **les tests de #781 ne pouvaient pas la voir** : ils lisent le *source* et vérifient que la fonction **cite** `declare_hors_an_par_identifiant`, ce qu'aucun champ absent ne contredit — variante du patron de #726, en pire, puisque le code n'était pas exécuté ; ceux de ce lot **font tourner** la fonction sur un brut à la **forme du corpus** (pas de `nom`, un `identite.nom_complet`), échouent quand on remet l'état d'avant, et portent une **contre-épreuve** — même brut, nom d'appelant différent, pas de pivot — sans laquelle « corriger » en lisant `identite.nom_complet` passerait aussi ; `indetermine` ne vaut toujours pas déclaration (#757), un fichier absent rend le comportement d'avant, `en_echec` écarte toujours la branche (#484), les trois étant désormais vérifiés **en exécution** ; **alternative écartée** : lire `identite.nom_complet`, qui marcherait aujourd'hui et rejouerait le défaut — la clé est le nom de la **liste**, pas celui de l'identité collectée, et rien ne garantit qu'ils restent égaux. 6 tests neufs, suite complète à 4 168, 0 échec.
- `2026-09-08` <a id="six-emplacements-fiche-candidat-328"></a>[La fiche candidat passe à six emplacements, et ce qu'il a voté remonte avant ce qu'il a dit (#328)](decisions/six-emplacements-fiche-candidat-328.md) — trois arbitrages de la propriétaire, et ce qu'ils coûtent : **« les gouvernements dont il a été membre » est retiré** (avec le composant, `buildGouvernements`, `loadGouvernements`, le titre dans les trois voix, la fonction `Barre` et **26 règles CSS**) — la fiche d'un ancien ministre ne télécharge plus ses fiches de gouvernement, jusqu'à quatre chez Attal, et le bilan collectif n'est pas dépublié pour autant, les pages `/gouvernements/:id` le portent ; **l'ordre rapproche les trois emplacements d'actes législatifs** — proposer, voter, s'écarter — et renvoie la parole après eux, sans rien changer au principe de la trame (une suite d'actes, jamais une hiérarchie) ; **« ce qu'il a voté » ne garde que la figure validée sur maquette**, l'axe des années et la note « un membre du gouvernement ne vote pas » partant ensemble — leur raison d'être était de protéger un `0` d'être lu comme une absence individuelle (§2 règle 3), ce qui n'a de sens que sur un axe **continu**, et la vue par période n'affiche aucune période sans vote, donc plus aucun zéro à mal lire ; **ce qui ne part pas** est la moitié qui compte — les quatre branches de vide (quatre causes qu'aucune figure ne remplace), les dénominateurs du repli (§2 règle 7) et les deux phrases de règle, que #711 veut **à côté du chiffre** et dont `test_derniere_lecture_711.py` garde la présence ; elles passent seulement **sous** la figure, trois blocs de texte avant un graphique faisant lire la légende à la place du fait ; **alternative écartée** : garder l'axe des années sous la vue par période — deux axes sur la même population avec deux métriques différentes, quand un lecteur suppose qu'ils comptent la même chose ; **aucun test ne gardait les figures retirées** (zéro occurrence de `parAnnee`, `cp-annee` ou de leurs libellés dans `tests/`), et les 20 classes CSS mortes ont été établies en confrontant chaque nom aux `.jsx`, gabarits `cp-annee--${…}` compris — `cp-gouv-tete/-nom/-periode` survivent, « ce qu'il a proposé » les réutilise, le préfixe ne suffisait pas à décider ; `trame-profil-candidat-328` décrit toujours sept emplacements et **n'est pas modifiée** : une décision se remplace, elle ne se réécrit pas. Suite complète à 4 099, 0 échec.
- `2026-09-08` <a id="transport-artifacts-panne-ou-absence-786"></a>[Une panne de transport n'est pas une source absente (#786)](decisions/transport-artifacts-panne-ou-absence-786.md) — le run `34241352524` est **vert et n'a collecté personne** : ses quatre téléchargements d'artifacts sont partis dans la même seconde et ont tous reçu un **403 « secondary rate limit »** sur `ListArtifacts`, que `download-artifact` déclare *non-retryable* ; `continue-on-error: true` a fait le reste et la fusion a écrit **0 profil** là où le run d'avant, même code, en écrivait **636** — les **40** artifacts AN et **8** artifacts roster étaient pourtant publiés et non expirés, dont les cinq candidats qu'attendait #775 ; **aucun garde-fou ne pouvait le voir**, ils mesurent tous la collecte APRÈS la fusion et sans profil brut il n'y a ni perte à constater (#460) ni collecté-non-publié à signaler (#511) — un vert **par absence**, le motif que #484 nomme déjà : *une panne rend le même vide qu'une absence* ; un step de contrôle s'insère donc **entre les téléchargements et la fusion**, seul point du run à tenir les deux termes — l'**inventaire** de ce que les jobs ont publié et le **disque** — et trois issues en sortent : rien de publié → silence (le repli de #412 §2.1 tient), publié et arrivé → rien à dire, publié et vide → **panne**, rattrapée par `gh run download` puis bloquante ; le rattrapage emprunte volontairement l'**autre chemin** (`gh` passe par l'API REST quand l'action interroge `results-receiver`, celui qui a rendu le 403), recolle à plat ce que `gh` range par sous-répertoire, et retente trois fois — la limite qui a produit l'incident a duré **moins de trois secondes** ; **deux asymétries assumées** : `parltrack-dumps` est rattrapé mais jamais bloquant (repli déclaré, portail §5), et un inventaire illisible **n'échoue pas** — « je ne peux pas vérifier » n'est pas « il n'y a rien » (§2 règle 5), d'où un `inventaire()` qui rend `None` et non une liste vide ; les quatre `continue-on-error` **restent**, ce lot ne les remplace pas, il sépare la source muette de la collecte perdue ; **alternative écartée** : retirer les `continue-on-error`, qui rendrait bloquante toute source n'ayant rien produit — ce que #412 §2.1 refuse — et un `retry` sur l'action, qui rejouerait la requête que la limite vient de refuser. 15 tests neufs, suite complète à 4 162, 0 échec.
- `2026-09-08` <a id="votes-par-periode-politique-328"></a>[« Ce qu'il a voté » range les positions par période politique, et ne totalise plus une carrière (#328)](decisions/votes-par-periode-politique-328.md) — la section publiait trois nombres justes et muets (« 123 pour, 34 contre, 11 abstentions »), alors qu'**un même vote n'a pas le même sens selon d'où il est émis** : 35 pour / 1 contre depuis la majorité en 2012-2014 chez Guedj, 16 pour / 6 contre depuis l'opposition en 2022-2023 — additionner détruit exactement ce que la répartition portait ; les positions de dernière lecture se découpent donc en **périodes politiques**, une nouvelle dès que le **banc** ou le **gouvernement en place** change, avec une barre divergente par matière et jamais un total par-dessus ; **deux repères parce qu'aucun ne couvre seul**, et leurs trous ne sont pas aux mêmes endroits — de 2012 à 2017 l'Assemblée publie le banc et le corpus n'a aucune fiche de gouvernement, depuis 2024 c'est l'inverse : banc **719 / 1 160**, gouvernement **916 / 1 160**, **banc ou gouvernement 1 160 / 1 160**, quand le banc seul en laissait **441 dehors** (dont les 132 positions de Marine Le Pen sur la XVIIe) ; cinq faits qualifient chaque vote, chacun de sa source — matière **711 / 1 160** (commission saisie au fond du dossier), origine **1 160 / 1 160** (mot ancré en tête de l'intitulé officiel, **pas** un rapprochement de libellés — #639), sort du texte **722 / 1 160** (`scrutins_dossiers.json`, #758, désormais servi au site) ; **quatre refus** : combler un repère manquant par celui de la période voisine (le titre écrit « banc non publié »), recalculer l'échelle sous un filtre — `porteeCommune` se calcule sur toutes les périodes **sans garde**, sinon 2 textes paraissent aussi larges que 37 et cocher un filtre n'apprend plus rien —, déduire la matière d'un intitulé, et totaliser par-dessus les périodes ; **l'encodage de l'origine a été arbitré sur maquette**, quatre variantes dessinées sur les mêmes données — rayure écartée (elle éclaircit la teinte d'un tiers et **disparaît sur un segment d'un ou deux textes**, donc sur les cas rares), creux seul (pèse trop peu), deux rangées (barres deux fois plus fines), voile seul (se lit « estimé ») —, retenu **voile 26 % + filet pleine saturation**, dont les défauts s'annulent ; `votesDuProfil` rend désormais `retenus` pour que le repli sur la dernière lecture n'ait **qu'une** implémentation (#711, AGENTS.md §6) ; le « pourquoi » des deux règles publiées passe dans la méthodologie — **leurs phrases restent sur la fiche**, parce que #711 est né d'une méthodologie qui annonçait la règle quand rien ne l'appliquait ; **reste ouvert** : le groupe majoritaire en face n'est pas nommé (#770), l'explication de vote ne se joint pas (214 entrées, 0 clé de dossier), et 449 positions restent en « matière non établie » — absence de source, jamais absence de commission, et la section l'écrit. 25 tests neufs, suite complète à 4 099, 0 échec.
- `2026-09-07` <a id="audit-des-rattachements"></a>[Un taux de rattachement se publie avec sa population, et ne bloque jamais](decisions/audit-des-rattachements.md) — le dépôt établit lui-même des liens qu'aucune source ne donne (amendement → texte visé → dossier → commission, scrutin → dossier #758, texte porté → commission), chacun mesuré **une fois** le jour de son écriture et plus jamais : un rattachement qui tomberait de 61 % à 12 % ne bougeait aucun compteur ; d'où une section « Rattachements reconstruits » dans `audit_pipeline`, **jamais bloquante** — et c'est ce qui la sépare d'`audit_integrite_referentielle`, qui vérifie une propriété **binaire** (une clé résout ou c'est un bug) sans aucun seuil, là où un taux bas est le plus souvent le **silence de la source** ; la **population est publiée avec le taux, toujours**, le même rattachement valant **4 %** sur les 17 762 scrutins et **61 %** sur les 697 textes en dernière lecture (§2 règle 7, §9) ; les **deux crans** de l'index des amendements sont mesurés séparément — viser un texte nommé, et ce texte porter un dossier — parce que les fondre masquerait celui qui décroche ; l'audit **ne recalcule pas** la population « sur l'ensemble d'un texte », que §6 pin à `lecture.js` (#711), et le déclare ; **ce que la première mesure fait sortir** : l'index de la **14e législature déclare 6 textes visés pour 578 référencés**, soit **122/61 036** amendements rattachés — 60 914 publiés sans titre ni dossier, trou antérieur au lot et jusqu'ici invisible, montré et non réparé ; **défaut de mon fait, corrigé et consigné** : le premier jet faisait calculer les rattachements par `build_report` — qui **assemble et ne recalcule rien** —, ce qui faisait scanner le **corpus vivant** aux trois appels de `test_audit_pipeline.py` (§3b) et portait la suite de 56 s à **185 s** ; le calcul appartient au CLI, et le dossier lu est dérivé de `--profiles-dir`. 14 tests, quatre mutations vérifiées échouantes.
- `2026-09-07` <a id="boucle-perimetre-candidats-757"></a>[La liste des candidats pilote le périmètre du run, elle ne le documente plus (#757)](decisions/boucle-perimetre-candidats-757.md) — #753 livrait la liste mais pas la **boucle** : une entrée neuve entrait avec `slug: null`, donc sans shard, donc jamais collectée, et le périmètre n'avançait que lorsqu'une main mintait le slug **et** écrivait l'entrée de correspondance qu'exige la §5b ; d'ici avril 2027 la liste bougera des dizaines de fois, et un périmètre suspendu à une corvée est un périmètre en retard (51 jours, 19 déclarés absents au lot précédent) ; la boucle se referme **dans le run** — job `rafraichir-candidats` sans `needs:` → artifact `candidats-a-jour` → matrice → correspondance écrite **hors ligne** avant le portail → commit de la liste **avec** les données —, forme déjà donnée deux fois au même problème par #518 (construction unique publiée en artifact) et #715 (correspondance écrite dans le run, parce que le remède manuel était inerte) ; **l'acteur AN se résout par un identifiant et jamais par un nom** : la correspondance par nom rapprocherait un candidat n'ayant jamais siégé d'un ancien député homonyme et publierait sur sa fiche les votes de quelqu'un d'autre (§2 règle 2), d'où la chaîne `article → wikibase_item → P4123 → PA…`, **validée 13/13** contre les entrées relues, positifs **et** négatifs (les 9 acteurs connus, les 4 `hors_an`), plus 3 recoupements sur Ruffin/Brun/Faure ; couverture des 19 entrées neuves : **8** avec acteur, **8** en fait négatif, **3** sans article qui restent **sans slug et nommées** — la file d'attente devient visible, ce que #539 reprochait à `slug: null` d'être muette ; trois issues distinguées (`ACTEUR`/`HORS_AN`/`INDETERMINE`) parce que « Wikidata ne dit rien de cette personne » et « Wikidata la décrit et ne lui connaît aucun mandat » ne sont pas la même affirmation, et **aucune panne ne rend `HORS_AN` par défaut** (le patron de #511 appliqué à une affirmation) ; l'entrée n'est écrite que si **deux sources s'accordent** — l'identifiant externe et le profil produit depuis AMO30 — et le fait négatif exige que les **deux se taisent**, un désaccord n'écrivant rien, nommant le slug et laissant la §5b bloquer ; troisième valeur d'`origine`, **`sourcee`**, parce que `derivee` signifie « n'établit rien » quand le slug sort de l'acteur, ce qui n'est pas le cas d'un `slugify(nom)` ; le réseau vit dans le job de tête **et nulle part ailleurs** (#524), et ce job **ne pousse rien** — `GENERATION_CODE_CHANGED_DURING_RUN` annulerait le commit —, son repli sur code 1 gardant la liste **committée** plutôt qu'une liste vide ; **prix assumé** : un déclaré entre dans le périmètre sans qu'un humain l'ait regardé, la garantie passant de « relue » à « sourcée et corroborée », arbitré sur l'asymétrie des modes d'échec — publier un candidat de trop se corrige en une ligne, ne pas publier un déclaré pendant six semaines est le défaut mesuré ; **coût CI** 13 → **29 shards** en série, +12 min de chemin critique dans le régime observé. 49 tests neufs, suite complète à 3 960, 0 échec.
- `2026-09-07` <a id="fiches-masquees-candidatures-declinees-761"></a>[Une candidature déclinée n'a plus de fiche dans l'interface (#761)](decisions/fiches-masquees-candidatures-declinees-761.md) — #760 avait sorti les déclinées du périmètre de **collecte**, donc leurs données cessaient d'être rafraîchies **pendant que leur fiche restait en ligne**, le pire des deux états ; `sync-data.mjs` **portait** déjà le `statut` dans le manifeste et ne s'en servait nulle part ; deux gestes, le second non cosmétique : le manifeste **exclut** les statuts masqués (`getCandidateProfile` rend `null` sans entrée, donc la fiche sort de la liste **et** de la navigation directe), et le profil **n'est pas copié** dans `public/data/profiles/` — celui qu'une exécution précédente y a laissé étant **supprimé**, faute de quoi la fiche « masquée » resterait servie à son URL, le script ne nettoyant pas son dossier de sortie ; **16 → 14** candidats au manifeste ; **masquer ne supprime rien de `pivot_data/`** (le `rmSync` ne vise que la sortie, et un test l'y confine — la même ligne pointée un cran plus haut supprimerait le corpus, #460/#470) et **ne retire personne d'un groupe** : les fiches de groupe sont bâties sur leur propre `membres[]`, et un député qui renonce à une candidature reste un député ; le masquage **se lève tout seul** si la personne redéclare ; **un seul ensemble, deux langages** — `STATUTS_MASQUES` (JS) et `STATUTS_GELES` (Python) disent « cette personne n'est plus candidate », et un test fait échouer la suite s'ils divergent, sur le patron de la table de sorts de #743 ; les séparer devra être une décision écrite, pas une dérive ; le masquage **se déclare** avec les slugs et jamais un compteur (#510) ; **ce que le lot ne fait pas** : `public/data/candidats.json` reste la copie intégrale de la liste éditoriale, déclinées comprises — aucun composant ne le lit, et cacher la trace d'une candidature qui a existé n'est pas ce que « masquer la fiche » veut dire. 9 tests neufs, suite complète à 3 987, 0 échec.
- `2026-09-07` <a id="fraicheur-dossiers-762"></a>[L'archive vivante se reprend au changement de semaine, les mortes jamais (#762)](decisions/fraicheur-dossiers-762.md) — `.cache/dossiers_an` porte une clé **hebdomadaire** doublée d'un `restore-keys`, et `ensure_dossiers_zip_downloaded` court-circuite sur `is_file()` sans qu'aucun appelant ne passe `force_download` : au changement de semaine le préfixe restaure le répertoire de la semaine d'avant, rien n'est repris, et il repart sous la clé neuve — **la rotation se désamorce elle-même**, exactement #749 appliqué aux dossiers, pour **cinq consommateurs** (`commissions_dossiers.json`, le statut des textes de gouvernement, `scrutins_dossiers.json`, `textes_dossiers_an`, `candidate_profile`) ; une étape reprend donc les archives **vivantes** quand `cache-hit != 'true'`, et **elles seules** — une législature dissoute ne produit plus d'acte, et reprendre les 15e et 16e coûterait **23 Mo par semaine pour un contenu identique** contre 9,8 pour la 17e ; l'option inverse de l'issue — purger les 33 Mo pour éviter « la machinerie qui distingue actives et figées » — est écartée parce que **cette machinerie existait déjà** (`ensure_dossiers_zip_downloaded` prend une législature et un `force_download`) : il n'y avait qu'un ensemble à déclarer, et il l'est **à côté des archives** plutôt qu'emprunté à `AN_SCRUTINS_LEGISLATURES_FIGEES`, les actives s'en déduisant par différence ; **défaut trouvé en relisant l'ordre des étapes** : sans la garde `!inputs.cold_start`, la reprise téléchargeait 9,8 Mo que le `rm -rf .cache` suivant effaçait ; le **dégât actuel est probablement nul** (session suspendue, comme #749) et un levier opérateur existait déjà — ce lot rend la reprise automatique. 12 tests, quatre mutations vérifiées échouantes.
- `2026-09-07` <a id="fraicheur-index-amendements-749"></a>[La rotation de clé hebdomadaire était toute la politique de fraîcheur, et son propre repli la désamorçait (#749)](decisions/fraicheur-index-amendements-749.md) — l'index amendements de la 17e n'avait plus été reconstruit depuis **18 jours**, et le gate le criait à chaque run ; aucun module n'avait tort séparément — #249 a fait de la **clé hebdomadaire** le seul mécanisme de péremption (le seuil de 7 jours de la §3d est documenté comme « aligné » sur elle), #250/#251 ne retéléchargent que sur cache absent ou corrompu, #253 a **explicitement rejeté** le retéléchargement inconditionnel, et #424 a ajouté `restore-keys` pour éviter un cache froid ; mis bout à bout, le repli restaure la semaine précédente, **le cache n'est donc jamais absent**, et la reconstruction n'a plus jamais lieu — le seul moment où le préfixe fut vide est le jour de sa création ; **le défaut est entre deux décisions, dans aucun des deux étages**, et l'alarme prévue pour ce cas sonnait sans destinataire ; ce qui l'a rendu invisible est un **log qui décrivait l'intention et non le fait** (« Construction de l'index… 642 acteurs » pour 0,28 s sans un octet téléchargé, les 642 étant un `len()` sur des noms de fichiers) ; correctif : `outputs.cache-hit` distingue seul une correspondance **exacte** d'un `restore-keys`, donc `--reconstruire-actives` purge les législatures **non figées** à chaque changement de semaine ISO ; `restore-keys` est **gardé** — le retirer viderait le cache entier et re-matérialiserait les figées, dont [[oom-reconstruction-amendements-figees]] a mesuré le coût (OOM à 6 Gio de RSS), pour 1,22 Gio par semaine au lieu de **283 Mo** ; un seuil d'âge dans le Python est écarté aussi : la politique de fraîcheur est dans la CI **exprès**, en dédoubler la source en ferait deux à tenir d'accord ; le prédicat de cache-hit est extrait et **nommé**, pour que le log et la décision ne puissent plus diverger ; dégât mesuré **nul** à ce jour (dernier amendement au 21/07/2026, cohérent avec la suspension des travaux), le coût commençant à la reprise de session. 10 tests, suite complète à 3 871, 0 échec.
- `2026-09-07` <a id="lien-profond-spa-pages"></a>[Un lien profond démarre l'application, pas la page d'erreur de GitHub](decisions/lien-profond-spa-pages.md) — mesuré en production au commit déployé `df5a9ba5` : `/` répondait **200**, `/candidats` et `/candidats/<slug>` répondaient **404**, et `/404.html` aussi ; l'application route côté client (`BrowserRouter`) quand Pages sert des **fichiers** et n'a **aucune règle de réécriture**, si bien que le site n'était navigable qu'en partant de la racine — un favori, un lien partagé, une entrée d'historique ou un simple **F5 sur une fiche** tombaient sur la page d'erreur de GitHub ; `dist/404.html` reçoit donc le contenu de `dist/index.html`, seul mécanisme disponible, et la copie est faite par le **build** (`scripts/spa-fallback.mjs`) et non par `deploy-pages.yml`, parce que le workflow ne fait que téléverser `dist/` et qu'un `dist/` publié autrement doit porter le même repli ; le repli **échoue bruyamment** si `index.html` manque ; **ce qui reste assumé** : le statut HTTP demeure **404** sur un lien profond — le lecteur voit la bonne page, un robot voit une erreur —, le corriger demandant un hébergement qui réécrit ou un routage par fragment qui abîmerait les URL ; défaut **ancien et sans rapport avec le lot du jour**, apparu avec le déploiement Pages et vu seulement parce qu'une fiche a été rafraîchie. 5 tests, trois mutations vérifiées échouantes, et le repli éprouvé contre un serveur reproduisant Pages.
- `2026-09-07` <a id="liste-candidats-declares-753"></a>[La liste des candidats se collecte, et un déclaré entre sans slug (#753)](decisions/liste-candidats-declares-753.md) — `raw_data/candidats.json` décide de **qui reçoit une fiche** (il dimensionne la matrice `extract-an` et porte la première passe pivot) et il était tenu à la main, `derniere_verification` à **51 jours** ; mesuré le 07/09/2026 : **30 déclarés** en ligne (19 au tableau principal, 11 en primaire) contre **13** chez nous, donc **19 absents** et **2 entrées périmées** — Wauquiez (`declare`) et Bardella (`pressenti`) sont tous deux sous « Candidats pressentis ayant décliné » ; le garde-fou censé le voir, `fetch_wikipedia_candidates.py`, n'était appelé par **aucun workflow**, n'avait **aucun test**, rendait 13 « candidats » dont **aucun n'était une personne** (« nationalité française », « droits civiques ») et **sortait en 0 sur un échec réseau total** — le patron de #511 ; trois défauts indépendants : mauvais article (la liste vit dans « Candidatures à… », l'article de l'élection ne fait que la transclure), regex de titre attrapant « **Conditions de candidature** », et requête Wikidata imposant une classe d'élection (`Q869519`) que `Q111594692` ne porte pas ; le remplaçant lit le **HTML rendu** parce que les primaires sont transcluses (`{{#section-h:}}`) et qu'un wikitext perd Mélenchon, Tondelier, Guedj et Royal — quatre personnes déjà publiées —, lit le nom dans le **texte** de la cellule et jamais dans son premier lien (Selma Labib et Benoît Mathieu n'ont pas d'article, leur premier lien est celui du **parti**), et retire trois parasites d'empilement `<br>` (clé de tri `{{TriNom}}`, `datasortkey`, `sup.reference`) ; **trois anomalies bloquent l'écriture** (page illisible, section introuvable, zéro candidat) et aucune n'est un seuil, une ligne illisible étant comptée et nommée comme `ROSTER_SANS_SLUG` (#527) tandis qu'un en-tête de colonnes se reconnaît à sa **structure** (que des `th`) et non à son libellé ; le point qui décide de tout : un nouveau candidat entre avec **`slug: null`**, ce qui le tient hors de `prepare-an-matrix` donc hors publication donc hors du hard fail §5b (#525) — **le blocage de #715 était un défaut et il est réparé** (`70626a02`, 02/09/2026, a bien committé les 160 profils et leurs entrées `origine: derivee`), mais son correctif est **borné à sa population** : `slugs_fabriques()` ne lit que `rosters_bruts.json`, or `groupes_reels.json` ne configure que **5 groupes par législature** (ni Écologistes, ni GDR, ni LIOT, ni MoDem, ni Horizons), d'où **3** des 19 déjà collectés comme membres de roster et **16** qu'aucune passe dérivée ne rattraperait — et l'étendre serait **faux**, le slug d'un candidat sortant de `slugify(nom)` et non de son acteur, donc un **rapprochement** à relire (#525, #715 §2) ; une entrée qui n'est plus déclarée est **signalée, jamais modifiée** (la section lue ne distingue pas un retrait d'un déplacement) ; Wikidata sort du dispositif (`P3602` rend **1** personne sur 30) et l'adaptateur officiel n'est pas écrit — `data.gouv.fr` rend **0** jeu 2027, le CC publie 2017 et 2022 sous `other-pd` et la liste passera par trois régimes (déclaratif → parrainages → officiel) ; **`decline` entre dans `statuts_possibles`** — le fichier n'avait aucune valeur pour dire qu'une candidature est abandonnée, d'où 51 jours de `declare` faux —, l'entrée étant **conservée** (son slug est publié, #460/#470) et la valeur n'étant **jamais posée par le script** ; coût **nul aujourd'hui** (19 entrées sans slug, `prepare-an-matrix` en retient toujours 13 ; référence : run `34053322456`, 13 shards, **606 s cumulés**, 27 à 69 s, moyenne 46,6 s) et **dissymétrique demain** : 3 des 19 sont déjà collectés comme membres de roster (Ruffin, Brun, Faure — le slug ne fait que basculer `provenance`), 16 sont à collecter de zéro, soit +19 shards **en série** (`max-parallel: 1`, seuil d'avertissement de 16 franchi) ≈ **+15 min** de chemin critique, plafond dur 19 × 5 min = **+95 min** ; côté dépôt (9,0 Go de `raw_data/profiles`, pack git **2,95 Gio**, médiane roster **10,8 Mo**), les bornes sont **0 à ~173 Mo bruts** selon combien des 16 ont une carrière AN — ce que dira la table de correspondance, et ce lot charge un peu plus **#678** et **#691** ; effet de bord **déclaré et non corrigé** : `parti_profile.py` créera 19 fiches de parti à `nb_candidats_avec_pivot: 0`. 24 tests sur une capture réelle réduite aux quatre formes qui cassent une extraction naïve, dont trois qui confrontent le fichier réel à sa propre liste de statuts.
- `2026-09-07` <a id="perimetre-collecte-candidatures-declinees-760"></a>[Une candidature déclinée sort du périmètre de collecte, sa fiche reste publiée (#760)](decisions/perimetre-collecte-candidatures-declinees-760.md) — #757 avait fermé la boucle du périmètre sans en border la **sortie** : `prepare-an-matrix` retenait tout candidat à slug résolvable, donc une candidature déclinée gardait son shard — **2 sur 29** au 07/09/2026 (Wauquiez, Bardella), et la part ne fera que croître, l'article des candidatures comptant déjà **24** personnes sous « ayant décliné » ; un prédicat unique (`src/perimetre_candidats.py`) appliqué aux **deux** endroits qui décident du périmètre — la matrice et `generate_all_profiles` —, parce que le filtre vivait en dur dans le YAML et qu'une seconde copie aurait divergé sans faire échouer aucune étape (le défaut que #518 a supprimé côté roster) ; **29 → 27 shards** ; **geler n'est pas supprimer** : le profil reste publié avec sa dernière collecte (régime des fiches Sénat gelées de #528 ; supprimer un fichier publié est une disparition qu'`audit_diff_profils` bloque), et rien n'est perdu à la fusion — ne pas collecter ne produit pas une collecte **vide**, ça ne produit **aucune** collecte ; le gel **se lève tout seul** si la personne redéclare, la boucle de #757 s'en charge ; **un statut inconnu est collecté** — `STATUTS_GELES` est le seul ensemble fermé, et le défaut penche vers collecter de trop plutôt que vers écarter en silence, parce qu'une collecte en trop coûte un shard et se voit, quand un candidat écarté par une valeur ajoutée ailleurs disparaît sans que rien ne le dise (#510) ; le gel **se déclare** là où le périmètre est calculé (`::notice::CANDIDAT_GELE`, avec le slug et le statut, jamais un compteur), et le module entre dans la liste blanche de sparse-checkout de la matrice — un chemin lu mais absent ne fait pas échouer le checkout, il rend un fichier absent (#674) ; **ce que le lot ne fait pas** : dépublier la fiche d'une candidature déclinée, question éditoriale laissée ouverte. 11 tests neufs + 3 sur le contrat de la matrice, suite complète à 3 978, 0 échec.
- `2026-09-07` <a id="purge-textes-portes-roster-747"></a>[Un profil de roster ne porte pas une liste que sa propre collecte déclare écartée — purge des 49 `textes_portes` résiduels (#747)](decisions/purge-textes-portes-roster-747.md) — l'atterrissage de #743 a montré **49 entrées** portant `sort: null` **et** `sort_non_resolu: null`, une absence sans cause que §2 règle 5 refuse et que le schéma promettait sans l'imposer, `validate_profil()` ne s'armant que sur un motif **non nul** ; toutes de provenance `roster_groupe`, **0 sur les 13 fiches publiées**, et toutes issues d'une fenêtre de trois jours — les 15 profils porteurs sont **tous les 15** dans le snapshot du 16/08/2026 (48 profils), collectés en mode plein avant que `a9f24d66` (#357) ne pose `--skip-dossiers-legislatifs` **en dur** sur `extract-roster-groupes` ; la fusion additive les conservait parce que la liste neuve est **vide et non incomplète**, donc les reports nommés de #689 et #743 n'avaient **aucune clé** sur quoi se poser — d'où la co-localisation exacte qui a servi de preuve, les 44 dossiers sans sort étant un sous-ensemble strict des 49 sans `nature_texte` que le gate comptait déjà ; **purge et non réparation**, décision inversée en cours d'arbitrage : la première recommandation pesait la **valeur** des faits sans regarder **qui les tiendrait à jour**, or la collecte de cette liste est coupée en dur pour cette population et un `sort` réparé sur un dossier `navette_en_cours` deviendrait faux **en silence** ; rien n'est perdu du corpus (**9** des 44 dossiers publient déjà un `statut` résolu sur une fiche de gouvernement, **34** sont dans `commissions_dossiers.json`) et le poids ne plaidait rien (**20,3 Ko**) ; **commit dédié plutôt qu'`allow_declared_losses`**, qui désarmerait le contrôle de perte du run entier ; les deux étages purgés mais **le critère ne les traverse pas** — `provenance` est un champ du pivot, et `collecte_ecartee` seul ne discrimine rien puisqu'un candidat déclaré est aussi un membre de roster : le critère brut seul aurait supprimé **71 dossiers sur 4 fiches publiées** ; l'invariant devient un contrôle, et il a fait tomber un **second chemin vivant** — `normalize_parltrack_dumps` publiait une entrée sans sort **ni** motif, défaut latent qu'aucune mesure n'aurait vu —, d'où un quatrième motif **`source_sans_sort`** ; ce que le lot ne fait pas : l'avertissement #689 tombe de 34 à **4** et non à 0 (prévision fausse, corrigée — 3 candidats déclarés restants), et `audit_integrite_referentielle` ne couvre toujours pas `textes_portes[].dossier_id`. 8 tests, suite complète à 3 858, 0 échec.
- `2026-09-07` <a id="rattachement-scrutin-dossier-758"></a>[Un scrutin ne dit pas quel texte il tranche : le lien se lit à l'envers (#758)](decisions/rattachement-scrutin-dossier-758.md) — la section « Ce qu'il a voté » devait montrer **sur quoi portent** les textes votés et **s'ils sont passés** : ni l'un ni l'autre n'était atteignable, `objet.referenceLegislative` étant nul sur **0/18 311** scrutins bruts et le seul sort disponible étant celui du *scrutin* (17 non-adoptés sur 697 textes, presque rien à montrer) ; le lien n'existe qu'**en sens inverse**, dans `actesLegislatifs[].voteRefs` du dossier — ce que `MOTIF_TEXTE_LIE_NON_SOURCE` écrivait déjà en renvoyant au rang 4 de #639, close depuis et donc sans porteur ; `pivot_data/scrutins_dossiers.json` publie le rattachement **et** l'issue du dossier, lus sur le **même arbre et les mêmes archives** que la commission au fond et que le statut des textes de gouvernement — troisième usage du même parcours, coût marginal nul côté réseau ; le lien **se lit et ne se devine jamais** (rapprocher par titre serait la jointure par libellé de #639), l'arbre irrégulier impose de le **marcher** plutôt que d'y suivre un chemin (six formes éprouvées), le statut est republié parce que les dossiers votés débordent les 725 textes de gouvernement mais sort de la **même fonction**, et `texte_lie_id` garde son sens de motion de censure ; **aucun cache disque**, et c'est la leçon de #749 — `.cache/dossiers_an` est restauré entre runs, un index dérivé posé là ne se reconstruirait jamais, et le parcours complet coûtant **1,7 s** le cache achèterait ces 1,7 s au prix d'une donnée qui vieillit en silence ; mesuré : **715 scrutins → 561 dossiers**, soit **423 des 697 textes (61 %)** dont **412 avec une commission (59 %)** — et chez Marine Le Pen, **22 textes votés contre ont été adoptés**, **3 votés pour ont été rejetés**, deux faits jusqu'ici impubliables ; limites déclarées : **39 % sans dossier**, XIVe hors archives, et le fichier n'existe pas tant qu'un run n'a pas eu lieu. 19 tests, trois mutations vérifiées échouantes.
- `2026-09-07` <a id="sort-des-textes-dans-la-fiche-328"></a>[Le sort d'un texte entre dans la fiche, à côté de son stade et jamais à sa place (#743 dans #328)](decisions/sort-des-textes-dans-la-fiche-328.md) — la demande était de **remplacer les étiquettes du sankey** par `textes_portes[].sort`, publié depuis #747 (423 entrées, 423 avec un sort) ; la mesure l'a décalée : **aucune barre n'est homogène en sort** — celle « non adopté » d'Édouard Philippe porte 2 navette, **1 adopté via 49.3** et 1 retiré, la barre finale « promulgué » de Gabriel Attal porte 6 CMP, 5 adoptés, **4 adoptés via 49.3** et 3 promulgués —, une étiquette ne pouvant nommer un sort qu'en mentant sur les autres, parce que la cascade a le **stade** pour axe et que le sort est **orthogonal** ; confrontation complète sur les 423 : **un seul désaccord**, le 49.3 de Philippe, où la figure écrit « non adopté » sur un texte adopté sans vote ; d'où le sort **par la liste** — deux faits, deux places, la pastille gardant le stade —, ce qui fait lire la barre « non adopté » de Jean-Luc Mélenchon comme **4 rejetés, 1 retiré, 1 en navette** là où elle donnait six textes indistincts ; le **49.3 nommé à côté de la figure et jamais dedans** (5 textes sur 414, quatre fondus dans « promulgué »), en **fait procédural** et en compte seul (§2 règle 4, §6) ; une **sélection par sort ne voile rien**, un segment agrégeant matière × porte ; le vocabulaire **déménage dans `utils/lecture.js`** — mêmes 9 valeurs que `KNOWN_STATUTS_TEXTE_GOUVERNEMENTAL`, `depose` et `rejete_49_3` déclarés bien qu'absents du corpus —, et un sort nul affiche son **motif**, jamais un défaut (§2 règle 5) ; premier test du dépôt qui compare **le frozenset Python au dictionnaire JS**.
- `2026-09-07` <a id="sortie-nommee-par-la-source-763"></a>[Le script pose `decline` quand la source nomme la cause (#763)](decisions/sortie-nommee-par-la-source-763.md) — trois lots **consommaient** `decline` (périmètre de collecte #760, masquage de fiche #761, rapport #753) et **aucun ne le posait** : les deux valeurs du fichier avaient été écrites à la main, soit trois consommateurs automatiques d'une valeur que seule une main pouvait produire ; simulation à l'appui — Wauquiez remis à `declare`, relance : signalé, statut inchangé, **toujours dans le périmètre** ; le script lit désormais les **deux sections qui nomment la cause** (« Candidatures retirées », « Candidats pressentis ayant décliné » — **26 personnes** mesurées le 07/09/2026) et pose `decline` sur les entrées qu'elles nomment ; **la frontière ne bouge pas d'un pouce** : « figure sous *ayant décliné* » est un **fait lu** et s'écrit, « ne figure plus parmi les déclarés » est une **absence** qui reste signalée sans rien changer — et c'est cette seconde moitié, celle qu'on aurait envie de sacrifier, qui empêche qu'un titre de section renommé décline tout le monde d'un coup ; trois garde-fous : on ne **crée** jamais une entrée depuis une section de sortie (Autain y figure et n'est pas chez nous), **`officiel` ne bascule pas** (il viendra du Conseil constitutionnel, et un article encyclopédique ne renverse pas un acte publié au JO), et **idempotence** ; une sortie nommée part en `::notice::` et non en `::warning::` — le warning reste pour ce qui demande une relecture —, et elle sort du bloc « à relire », que le rapport annonçait **faux** avant correction (« aucune section de sortie ne les nomme » pour les deux que les sections nommaient) ; la note cite le **titre** de la section à la lettre, pas sa paraphrase, avec l'état d'avant, la date et le lien ; **prix assumé** : une fiche disparaît du site sans qu'un humain l'ait décidé — symétrie de l'entrée automatique de #757, arbitrée séparément parce qu'ajouter une fiche se remarque moins que d'en retirer une ; un test de #753 a dû être **restreint** plutôt que supprimé, son invariant ne valant plus que pour les sorties sans cause nommée. 14 tests neufs, suite complète à 4 001, 0 échec.
- `2026-09-06` <a id="alignement-section-propose-ui-328"></a>[La cascade des textes portés entre dans l'UI, et la section s'aligne sur la maquette (#328)](decisions/alignement-section-propose-ui-328.md) — la PR #745 n'avait porté **qu'une pièce** de la maquette validée, le waterfall, laissant **cinq écarts** (cascade absente, ordre des cartes inversé, carte « Auteur d'amendements » en double, chapeau de section, en-tête du waterfall) ; ce lot les ferme : l'ordre suit les **deux populations** — les textes dont il est auteur ou rapporteur, puis les amendements déposés sur les textes **des autres** —, et la cascade est **portée telle quelle**, `d3-sankey` compris, mesurée avant d'être ajoutée (**+19,9 ko brut, +7,2 ko gzip**, dépendance de rendu **et** 394 lignes de mise en page **et** composant) parce que ce qu'elle apporte est load-bearing — nœuds, largeurs, et empilement contigu des **arrivées** — et que seul le **départ** est repris après coup, forcer aussi l'arrivée cassant la **convergence** que la fourche rend possible ; la mise en page vit dans `utils/` et **ne rend rien**, ce qui permet de vérifier hors navigateur la conservation des textes sur les **13 candidats déclarés** × 4 largeurs ; l'échelle des crans est **celle que le corpus remplit** — un cran n'existe que si un texte s'y arrête, ce qui écarte `inscrit_ordre_jour` (**0 des 423** textes) sans le coder en dur et garantit un **préfixe sans trou** ; la matière est celle de la chute (même table, même ordre, même teinte) parce que **les deux figures doivent colorier pareil**, l'absence restant une absence (§2 règle 5) ; la branche basse continue de ne parler que de **crans** et jamais de sort, `textes_portes[].sort` (#743) n'étant pas encore dans `pivot_data/` — le brancher est un lot à part ; un défaut corrigé au passage : sous trois flux, la maquette rendait une carte **vide** (Jérôme Guedj, 2 textes), l'UI écrit désormais pourquoi ; **coût assumé et déclaré** : la barre par législature emporte avec elle la **position déclarée du groupe** et le chapeau emportait sa raison — la donnée reste calculée, la carte est à réécrire.
- `2026-09-06` <a id="chute-des-depots-section-2-ui"></a>[La chute des dépôts remplace la barre par législature (§ « Ce qu'il a proposé »)](decisions/chute-des-depots-section-2-ui.md) — la barre segmentée **par sort**, une ligne par législature, répondait « combien et de quelle issue » et **rien sur le quand ni sur quoi** ; une **chute** — escalier cumulatif, une marche par **année civile** découpée par **matière**, barre de total qui repart du sol — la remplace, avec un bouton qui échange **amendements déposés** et **dossiers amendés** et un clic qui ouvre les dossiers de la matière, ceux dont **au moins un amendement a été adopté** marqués ; trois contraintes écrites sous la figure : **l'axe est le calendrier** (une année vide garde sa place, sa graduation et son palier — sept chez Laurent Wauquiez, huit chez Jérôme Guedj), **un escalier est additif** donc **aucune hauteur minimale** (1 dépôt sur 2 831 fait 0,15 px, et c'est le **palier** tracé au cumul exact qui garantit qu'aucune année ne disparaît), et **le domaine est l'union des deux mesures** jamais recalculé — fixé, il **montre** que **258 des 584 dépôts** de Laurent Wauquiez ne sont rattachés à aucun dossier ; une carte **« N amendements adoptés »** ouvre la rangée des blocs, **compte seul et rien en face**, porté sur **tous** les dépôts (la somme par dossier en perdrait **6** chez Jérôme Guedj et **25** chez Laurent Wauquiez) ; coût assumé : la série année × matière est accumulée dans la **passe unique** de `joinAmendements`, générateur qui ne se relit pas (#377, #431), et le lot **ouvre une palette catégorielle** que `DESIGN_SYSTEM.md` disait close — vrai du besoin d'alors (*marquer une ligne*), faux de celui-ci (*distinguer N matières*), sans solution sans teinte puisqu'une rampe d'encre les mettrait sur une échelle (§2 règle 1) ; **aucun rapport entre les deux mesures** (§6), le **gris** réservé à « matière non établie » qui est une absence et non une catégorie (§2 règle 5), et **aucune librairie de graphiques ajoutée** — le SVG est écrit à la main.
- `2026-09-06` <a id="sort-des-textes-portes-743"></a>[Un texte porté dit ce qu'il est devenu, et son sort ne se déduit jamais de son stade (#743)](decisions/sort-des-textes-portes-743.md) — `textes_portes[]` publiait dix champs dont **aucun ne disait ce qu'était devenu le texte** (ni rejeté, ni retiré, ni en navette), `stade_procedural` encodant une **progression** dont l'absence du cran suivant est un fait de la source à sa date et jamais une issue ; ce n'était pas un manque de source — les fiches de gouvernement le publient **depuis #184** (725 textes) et `schema_gouvernement.py` avait écrit que ce concept « n'existe pas encore côté pivot individuel » —, et la source est instruite depuis le spike #207 : le statut vit dans **`statutConclusion.fam_code`**, non dans `codeActe` ; d'où `textes_portes[].sort`, vocabulaire fermé lu **par la même fonction que les gouvernements** (`_determine_statut`, qui ne dépend pas de l'origine du dossier), accompagné quand il est `null` d'un `sort_non_resolu.motif` à trois valeurs **qui ne se réparent pas au même endroit** — `sans_decision` (état légitime), `fam_code_inconnu` (table à étendre), `archives_indisponibles` (panne du run) —, le schéma refusant la contradiction et le motif inventé, et `sort_49_3` n'étant **pas** republié puisque `adopte_49_3` le porte déjà ; mesuré sur les **472 entrées** : `navette_en_cours` 238 · `adopte` 127 · `adopte_cmp` 59 · `promulgue` 20 · **`rejete` 13** · **`retire` 8** · `adopte_49_3` 7, **0 sans sort**, et **464/464** dossiers retrouvés sans un seul `fam_code` inconnu ; **septième occurrence** de la famille #492/#639/#641/#696/#710/#718 pour le report nommé — mais **la moitié du câblage aurait été du code mort** : posé d'abord aux deux étages sur la leçon de #729/#730, la mutation a montré que décâbler le report **brut** fait échouer un test et le **pivot** aucun, parce que `merge_dossier_records` fait gagner l'entrée **neuve** là où `merge_lists_by_key` fait gagner l'ancienne — report pivot **retiré**, et la leçon écrite : **une règle générale ne dispense pas de vérifier qu'elle s'applique ici** ; le lot ne dérive jamais le sort du stade (4 tests sur 4 stades), ne touche pas la vue, et rien n'est publié avant un run. 27 tests, trois mutations vérifiées échouantes plus une quatrième restée verte — celle qui a fait retirer du code. Suite complète à 3 840, 0 échec.
- `2026-09-04` <a id="commission-saisie-au-fond-gouvernement-689"></a>[Un texte de gouvernement porte sa matière, ou dit pourquoi il ne la porte pas (#689)](decisions/commission-saisie-au-fond-gouvernement-689.md) — lire les lois d'un gouvernement **par matière** suppose une matière, et les `textes[]` des 10 fiches n'en portaient aucune ; l'inventer depuis les titres était exclu (§2 règle 1, et la clé tirée d'un libellé que #639 et #672 ont fait payer deux fois), mais **l'Assemblée en fournit une** — la **commission saisie au fond**, que `pivot_data/commissions_dossiers.json` publie (6 024 dossiers) depuis le commit de données `5de11422`, la ligne d'`AGENTS.md` disant qu'il « n'a jamais été produit » étant périmée et corrigée par ce lot ; chaque texte publie donc `commission_saisie_au_fond` (`{organe_ref, sigle, nom}` ou `null`) joint sur **`dossier_id`, jamais sur le titre**, et, quand elle est `null`, `commission_non_resolue.motif` d'un vocabulaire **fermé de trois valeurs qui ne se réparent pas au même endroit** — `depot_senat` (l'index est celui de l'AN, le Sénat est hors périmètre #528 : **jamais** réparable), `absente_de_l_index` (un vrai trou de l'index AN), `index_indisponible` (une panne du run) —, les confondre faisant lire une décision éditoriale comme un défaut ou une panne comme un constat (#726, #510) ; le schéma **refuse la contradiction** (porter les deux dirait « voici sa commission » et « voici pourquoi elle manque ») ; mesuré sur les 725 textes régénérés : **551 résolus (76,0 %)**, **174 `depot_senat`**, **0 `absente_de_l_index`** — compteur-témoin —, **0 erreur de schéma**, et surtout **381/381 des dépôts AN résolvent** contre 170/344 des dépôts Sénat, ce qui fait des 174 une limite de **périmètre** et non un trou de collecte (§2 règle 5) ; trois choix de mise en œuvre — la projection est explicite (le `type` d'organe décrit le référentiel, pas le texte), l'index est chargé **une fois pour dix fiches** (le coût que #392/#403/#467 ont payé trois fois au même endroit), et le défaut de la fonction reste `None` et non un chemin du dépôt (§3b, #721), un index **vide** valant un index absent ; le lot **ne touche pas la vue**, ne publie pas un « thème » mais une commission (l'appeler thème serait une interprétation), ne change ni les profils ni les fiches de groupe, et **rien n'est publié tant qu'un run n'a pas régénéré les fiches**. 14 tests, trois mutations vérifiées échouantes, suite complète à 3 754, 0 échec.
- `2026-09-04` <a id="fonctions-gouvernementales-section-1-328"></a>[Une fonction exercée ne l'est pas toujours au Parlement (#328)](decisions/fonctions-gouvernementales-section-1-328.md) — « Les fonctions exercées » ne montrait que **sept catégories, toutes parlementaires**, alors qu'un portefeuille ministériel est un siège occupé au même titre qu'une commission — intitulé, dates, durée — et que **6 des 13 candidats déclarés en ont exercé un** (65 des 641 profils publiés) ; `categorie: fonction_gouvernementale` couvre **trois natures** que le champ `fonction` sépare déjà pour `appartenancesGouvernementales`, et le filtre porte sur lui, **jamais sur le libellé** — reconnaître « Gouvernement (…) » dans l'intitulé aurait été la jointure par ressemblance de chaîne que #639 interdit : `membre` est l'**enveloppe** et reste à la frise (la publier doublerait chaque portefeuille d'une ligne qui ne dit pas ce qu'on y faisait — **17 enveloppes pour 18 portefeuilles** sur les 7 profils concernés), `en mission` a son **propre bloc** parce qu'un⋅e parlementaire en mission auprès d'un ministère **reste parlementaire** (la frise lui donne déjà sa piste), et le reste est le portefeuille ; **la marque ne s'applique pas** aux deux blocs gouvernementaux (`sansMarque`) — son dénominateur est le temps de mandat **électif**, et Édouard Philippe a été Premier ministre **trois ans sans siéger** : elle aurait affirmé « plus de la moitié » d'un tout dont il était absent, un ratio sans son dénominateur (§2 règle 7) ; **la couleur dit le BANC, pas la catégorie** — bleu de l'Assemblée, bronze du gouvernement, contour **tireté** pour la mission, soit les trois pistes de la frise et pas une teinte de plus, là où une teinte par catégorie en aurait demandé **neuf** et aurait fait perdre le banc sur un profil qui a connu les deux ; et la couleur **double** l'intitulé du bloc sans le remplacer, la section restant lisible en niveaux de gris ; effet de bord assumé : les deux teintes étaient écrites en littéral à **cinq endroits**, au point qu'un test surveillait que deux d'entre elles n'avaient pas divergé — **une valeur qu'un test doit surveiller pour rester unique est une valeur qui n'aurait pas dû être copiée**, elles vivent désormais sur `.cp-main` et `test_les_colonnes_prennent_la_teinte_de_la_frise` devient `test_la_teinte_d_un_banc_est_declaree_une_seule_fois` ; limite déclarée : **le lot ne retire pas la section « Les gouvernements dont il a été membre »**, qui continue de publier l'appartenance sous une autre forme — sa restructuration est un lot à part, décidé et non implémenté. 11 tests, neuf mutations vérifiées échouantes, suite complète à 3 812, 0 échec.
- `2026-09-04` <a id="mandats-gouvernementaux-en-commission-730"></a>[Le référentiel type l'organe, il n'y a rien à interpréter (#730)](decisions/mandats-gouvernementaux-en-commission-730.md) — huit mandats ministériels publiés en `commission` sur l'organe `Gouvernement`, gelés par la fusion additive puisque **aucun chemin de collecte ne les produit** (`GOUVERNEMENT` est volontairement hors de `_TYPE_ORGANE_TO_CATEGORIE`) ; **deux critères écartés par la mesure et non par principe** — le croisement (profil × période) **seul**, celui retenu à l'arbitrage, capture **1 036** mandats et non 8 (un ministre garde ses commissions), et le vocabulaire ministériel de #474 en capture **0**, `_normalise_fonction` étant **purement typographique sans troncature ni préfixe** et le rapprocher par préfixe étant la classification par libellé que #639/#718/#729 écartent ; **retenu : le typage du référentiel** — l'index d'organes AN type `Gouvernement` en `GOUVERNEMENT`, seul libellé du référentiel dans ce cas, donc on lit ce que la source déclare de l'organe —, **verrouillé** par le croisement de période, les deux rendant **8 entrées** chacun ; le sort se décide ensuite sur ce que le profil porte déjà : période **déjà couverte** → **retirée** (6), **non couverte** → **requalifiée** (2, `yael-braun-pivet` et `damien-abad`), la retirer effaçant sinon une période ministérielle réelle, et **rien n'est fabriqué** puisque période, libellé et fonction restent ceux de l'entrée ; « déjà couverte » exige un **recouvrement complet** et non un chevauchement — écrit d'abord avec un chevauchement, le lot rendait **8 retraits et 0 requalification** au lieu de **6 et 2** ; garde-fous : `--apply` explicite, **un index absent ne reconnaît rien** (pas de repli silencieux sur le libellé, §2 règle 5), appariement **sur le slug** `membres[].membre_id` et jamais sur un nom d'affichage (#487, #668 — un profil brut n'a pas de champ `nom`), idempotence vérifiée ; **rien n'a bougé dans `pivot_data/`** — il faut un run, qui **bloquera au contrôle de perte** sur `mandats` comme celui de #729, les 6 étant nommées d'avance. 18 tests, trois mutations vérifiées échouantes, suite complète à 3 802, 0 échec.
- `2026-09-04` <a id="purge-doublons-herites-729"></a>[185 doublons hérités retirés : l'outil existait, il n'avait jamais été relancé (#729)](decisions/purge-doublons-herites-729.md) — **aucun code** : `src/purge_mandats_dupliques.py` (#387) couvrait déjà le cas et n'avait pas été relancé depuis la régénération du corpus ; **185 doublons retirés sur 13 des 641 profils** (4 ignorés faute d'`acteurRef`, **9** faute d'extraction AN — déclarés, non instruits) ; le critère de #387 regarde **le profil** et non le corpus — une entrée héritée n'est retirée que si le même profil porte déjà son équivalent AN, libellé normalisé correspondant et période recouvrante —, ce qui le rend plus sûr que la détection par **libellé contradictoire** de l'instruction de #729, qui n'en voyait que **18** : le chiffre soumis à l'arbitrage était donc trop étroit, dans le sens le moins grave ; « doublon » se lit à la normalisation près, **l'AN nommant par le thème nu quand NosDéputés préfixait la nature** (`Groupe d'études polices municipales` en `commission` retirée, `Polices municipales` en `groupe_etudes` conservée) — vérifié après application sur les 185 relues depuis git : **155 semblent orphelines libellé brut à libellé brut, 0 avec `_normalize_label`**, la première mesure étant naïve et le noter évitant qu'on la refasse en paniquant ; **les 41 entrées des 13 libellés non arbitrables ne sont pas touchées** — leurs deux catégories sont héritées, NosDéputés se contredisait lui-même et aucun référentiel vivant ne tranche, elles restent marquées par `categorie_source` (#718) sans être accusées et #729 reste ouverte pour elles ; **#730 n'est pas couvert** (`gabriel-attal` et `yael-braun-pivet` rendent 0 doublon, faute d'organe AMO30 nommé `Gouvernement`) ; **rien n'a bougé dans `pivot_data/`** — il faut un run, qui **bloquera au contrôle de perte** sur `mandats`, liste stable surveillée : perte voulue, nommée d'avance, à déclarer par `allow_declared_losses` après comparaison du rapport aux 185.
- `2026-09-04` <a id="regles-par-domaine-737"></a>[`AGENTS.md` garde ce qui gouverne tout, `docs/regles/` ce qui gouverne un domaine (#737)](decisions/regles-par-domaine-737.md) — le fichier faisait **983 lignes** chargées à chaque session et grossissait à chaque lot ; l'hypothèse de départ « il mélange l'instruction et sa preuve » a été appliquée à dix puces et n'a rendu que **39 lignes**, les gains tombant de 11 à 0 — **seules 30 lignes sur 983 portaient un chiffre**, le reste était de l'instruction écrite longuement ; la mesure a montré l'autre levier : **78 % du fichier (738 lignes sur 921) ne sert qu'à qui touche un module donné**, et la §3 était le seul endroit où l'on savait qu'un agent lirait une règle, d'où l'accumulation ; d'où huit fichiers `docs/regles/` (§3a→`fusion-et-index`, §3b→`ci`, §3c→`gardes-avant-commit`, §3d→`roster-et-sources`, §3e→`interventions-syceron`, §3f→`portail-qualite`, §4→`schema-pivot`, §5→`champs-sensibles`) et **983 → 343 lignes** sans qu'une instruction disparaisse ; ce qui aurait cassé en silence : le dépôt cite « AGENTS.md §X » **596 fois dans 200 fichiers**, mais **~430 visent la §2 qui ne bouge pas**, et les sections déplacées continuent de résoudre parce qu'`AGENTS.md` **garde la ligne d'index de chacune** ; 28 tests le vérifient dans les deux sens, avec un compteur-témoin contre le relevé vide (#510), un plafond de 500 lignes et l'obligation que la §2 reste sur place — seule section qu'un agent doit avoir sous les yeux **sans savoir qu'il en a besoin** ; la scission a immédiatement cassé `test_ci_signal_identite_push_685`, qui lisait une règle de §3b dans `AGENTS.md` et lit désormais **les deux fichiers** : c'est le coût réel, **un test qui vérifie une règle doit lire le fichier où elle vit** ; coût assumé et non arrondi — un agent qui n'ouvre pas le bon fichier rate une règle, et le bénéfice n'est pas la longueur mais la **stabilité** ; les dix puces réécrites sont conservées, dont celle du `texte_vise` qui annonçait « 293 582 of 484 132 » **au présent et sans date**, vrai à l'écriture et **faux depuis #696** (remesuré : **0 sur 509 744**), citée deux fois comme un état courant par une autre session — **une mesure non datée dans un fichier d'instructions se lit comme un état courant** ; déplacement contrôlé par script, chaque nombre retiré devant exister dans une décision, **comparé nombre à nombre et non chaîne à chaîne** (anglais « 477 of 481 » contre français « 477 des 481 »). 3 777 tests, 0 échec.
- `2026-09-04` <a id="teintes-des-stades-en-bref-328"></a>[Une classe qui décide d'un fond ne se pose que sur un fond (#328)](decisions/teintes-des-stades-en-bref-328.md) — trois défauts de couleur vivaient en production dans « En bref », trouvés **à l'écran** par la propriétaire alors que la suite était verte à **3 735 tests** : aucun ne regardait une couleur ; tous les trois viennent du **même geste**, une classe qui décide d'un `background` posée sur autre chose qu'un fond — les `<em>` de légende portaient la classe du **segment de barre**, donc `var(--parl)` (#3f5166) passait derrière un texte `var(--muted)` (#8b8794), soit **1,9:1** là où AA demande 4,5:1, et « 15 promulgué » était illisible ; les cinq pastilles prenaient `background: currentColor`, c'est-à-dire la couleur du **texte**, donc **toutes la même** — une légende qui n'identifie plus rien ; et `.cp-gc-tete-col--gouvernement ~ * .cp-gc-part--promulgue` partait de l'en-tête pour descendre sur **tous ses frères**, si bien que la barre « 2 promulgué » de la colonne **À l'Assemblée** était brune, quatrième effet compris : seul `promulgue` basculant, la colonne du gouvernement affichait une **barre à deux familles** ; d'où une teinte **déclarée une fois** sur la classe du stade (`--teinte`) et **lue deux fois**, en fond par le segment et en pastille par la légende, le libellé gardant `--muted` ; la **famille** (`--t1`…`--t5`) vient de la **colonne**, portée par la cellule elle-même (`piste={c}`) et non déduite du voisinage — **aucune couleur ne se décide par voisinage dans une grille**, où l'ordre du DOM ne dit rien de la colonne rendue ; la rampe **monte** sur les cinq stades de `STADES_PUBLIES`, là où elle descendait puis remontait (`inscrit_ordre_jour` plus clair que `examine_commission`) avec **deux stades voisins de valeur identique** (`discute_seance` = `examine_commission` = `--parl-line`), et ce n'est **pas une échelle de valeur** : un texte promulgué n'est pas « mieux » qu'un texte en commission (§2 règle 1), c'est l'ordre que la source publie ; limites déclarées — **le contraste n'est pas mesuré par un test**, les cinq tests ajoutés lisent le code exécuté et vérifient les rôles, et le bloc sombre de `.cp-gc` **ne s'applique jamais** (`index.css` déclare `color-scheme: light`, rendu vérifié **au pixel près identique** sous préférence système claire et sombre) mais reste en place, verrouillé par un test existant. 5 tests, cinq mutations vérifiées échouantes, suite complète à 3 754, 0 échec.
- `2026-09-03` <a id="audit-champs-deplaces-726"></a>[Un audit qui lit un champ déplacé ne se tait pas : il crie 62 000 fois (#726)](decisions/audit-champs-deplaces-726.md) — `audit_pipeline.py` rend **62 705 lignes** dont **99 % sont deux faux constats**, chacun venant d'un champ qu'une décision de schéma a déplacé ou tari sans que l'audit soit rouvert : `sources[].synchro_le` des entrées `nosdeputes`, que **#529** a cessé de collecter tout en les conservant pour la clause ODbL (464 lignes, 100 % du bloc — 464 des **1 115** entrées `sources[]` publiées), et `cohesion_votes[].date`, que **#432** a déplacé dans `pivot_data/scrutins.json` (61 596 lignes, 98 % du rapport — **pas une** des 61 596 entrées ne porte la clé, ce n'est pas un `null` mais une absence de champ) ; ce n'est pas cosmétique, le second bloc enterrait le fait que le **tableau des plages temporelles par groupe était vide par construction depuis #432** ; d'où un troisième code `non_renseigne` séparé de `format_invalide` — une valeur **non-chaîne** reste fautive, une absence ne l'est pas (§2 règle 5) — **agrégé** au rendu quand les deux autres restent énumérés, et une résolution de la date **là où elle vit** via `scrutins_index.charger()`, qui rend le tableau réel (XVIe 2022-07-11 → 2024-06-07, XVIIe 2024-10-08 → 2026-07-21, les 2 fiches Sénat gelées sans `cohesion_votes` à `N/D`) et fait que **les 61 596 dates se résolvent toutes** ; trois motifs fermés (`MOTIFS_DATE_NON_RESOLUE`) **comptés et jamais énumérés** parce que « le scrutin n'est pas dans l'index » et « sa date est illisible » ne se réparent pas au même endroit ; un index absent est un **trou déclaré** (`index_disponible: False`) et un index **vide vaut absent** — `charger()` en rend un sur un fichier introuvable, le croire disponible aurait fait passer « le fichier manquait » pour « ces groupes n'ont pas de scrutin », la panne de #510 ; le défaut de la fonction reste `None` et non un chemin du dépôt (§3b, sujet de #721), la CLI portant `--scrutins` ; mesuré **62 705 → 653 lignes**, rapport groupes **~61 700 → 191**, plages renseignées **0 → 10 sur 12** ; la règle qui reste : **un audit est un consommateur comme un autre et rien ne le prévient qu'un champ a bougé** — les tests fournissaient eux-mêmes un `date` dans l'entrée, donc ils vérifiaient que la fonction lisait un champ que le corpus ne porte pas, et une fixture qui décrit le monde tel que le code l'imagine ne peut pas révéler qu'il a changé (#510). 3 684 tests, 0 échec.
- `2026-09-03` <a id="cache-du-poste-hors-des-tests-721"></a>[Six tests lisaient le cache du poste : la CI était verte parce que la machine est vide (#721)](decisions/cache-du-poste-hors-des-tests-721.md) — six tests rendaient **688** interventions là où leur fixture en attendait **1** : ils patchaient le **constructeur** (`_build_acteur_interventions_syceron_index`) mais pas `_read_cached_interventions_syceron_acteur`, qui **lit le cache avant de construire**, et les **onze** constantes de cache du dépôt valent `Path(".cache") / …`, donc un chemin **relatif au répertoire courant** — la racine du dépôt en local, le piège que `AGENTS.md` §3b nomme déjà ; **la CI est verte parce que son checkout partiel ne pose pas `.cache/`**, pas parce que le test est juste, et ils passaient en local depuis #719 parce que le cache du poste est périmé donc rejeté — deux raisons accidentelles, aucune bonne, et c'est ce qui a mis la puce à l'oreille (six tests devenus verts sans qu'on les touche) ; **rediriger les constantes a été essayé, mesuré et écarté** : dix tests cassent, ceux qui isolent déjà leur cache par `monkeypatch.chdir(tmp_path)` et écrivent sous un `.cache` relatif — une constante rendue absolue leur retire l'isolation et les envoie sur le réseau, où la coupure de #473 les arrête —, l'idiome existant étant bon, ce qui manquait était de **voir** ceux qui ne l'appliquent pas ; d'où un garde-fou dans `conftest.py` sur le patron de #473 : `builtins.open` filtré, toute ouverture sous le `.cache` **du dépôt** lève en nommant le fichier et en disant quoi faire, avec le test grossier sur la chaîne d'abord (`resolve()` à chaque ouverture coûterait cher pour un cas rarissime), `bytes` et descripteurs traités, et un `.cache` sous `tmp_path` qui ne déclenche rien ; le littéral `.cache` s'est fait prendre par `test_ci_perimetre_sparse_checkout`, d'où `_NOMMES_POUR_ETRE_REFUSES` — une exemption **explicite** plutôt qu'un chemin construit autrement qui aurait esquivé le relevé et masqué le garde-fou —, verrouillée par un test parce que le jour où `.cache` entrerait dans le sparse-checkout les tests liraient le cache du **runner** ; la propriété dont tout dépend (**les constantes restent relatives**, sans quoi `chdir` n'isole plus) est testée sur les 7 modules qui en portent, avec un **compteur-témoin** contre le trou muet de #510 ; limites déclarées — le garde-fou ne couvre que `open()` (un `Path.is_dir()` seul passerait, accepté : une existence lue ne sert pas de données fausses) et ne dit rien des écritures. 11 tests, deux mutations vérifiées échouantes, suite complète à 3 694, 0 échec.
- `2026-09-03` <a id="categorie-source-des-mandats-718"></a>[Un mandat dit quel référentiel a établi sa catégorie, et l'absence n'accuse personne (#718)](decisions/categorie-source-des-mandats-718.md) — **12 % des mandats `categorie: "commission"` des 13 candidats déclarés n'en sont pas** (27 sur 225), et la fiche publie ce total comme dénominateur (§2 règle 7) ; **le taux dépend de la population** — 12,0 % sur les 13 déclarés, ≈ 2,1 % sur les 641 profils ; l'instruction corrige le cadrage sur deux points : **le critère sourcé existait déjà** (`_TYPE_ORGANE_TO_CATEGORIE` tire la catégorie du `codeType` AMO30), le défaut étant du **résidu NosDéputés** que la fusion additive conserve, et les écarts comptent une **quatrième nature majoritaire** que l'issue n'avait pas vue — des mandats **réels** (groupes d'études, commissions d'enquête, `Bureau de l'assemblée nationale`) que NosDéputés aplatissait sous `commission` et que l'AN nomme autrement, si bien que la purge de #387 ne peut pas les apparier et que les supprimer perdrait une donnée que rien d'autre ne porte ; d'où `mandats[].categorie_source`, vocabulaire fermé (`an` | `europarl`) et **clé facultative dont l'absence dit « personne ne l'a établie »** — **il n'existe aucune valeur « héritée »**, ce serait une accusation que le corpus ne permet pas de porter (#486 : 29 des 511 `mandat_electif` publiés sont des entrées que la source ne sert plus), et `None` est refusé aussi parce qu'il dirait l'absence sous la forme d'un constat (§2 règle 5, même arbitrage que `interventions[].collecte` de #657) ; **sixième occurrence** de la famille #492/#639/#641/#696/#710, d'où `backfill_mandat_categorie_source` câblé **aux deux étages** — l'oublier au pivot aurait laissé le champ arriver au brut sans jamais atteindre la couche que `web/` lit, un correctif vrai et sans effet, trou trouvé par un test du lot et non par relecture ; effet **simulé** sur les 468 profils ayant correspondance relue et mandats AMO30 : **640 mandats catégoriels sans estampille sur 39 449 (1,6 %)**, dont **467 sur 11 775 `commission` (4,0 %)**, et **33 profils** dont le dénominateur baisse, de façon concentrée (`eric-poulliat` 65 → 27, vérifié : ses 38 entrées portent la signature NosDéputés, `type` en minuscules, et sont surtout des groupes d'études) ; **le lot ne touche pas la vue** (une autre session tient `profilCandidat.js`), donc le dénominateur reste faux à l'écran tant qu'elle ne compte pas les seules entrées établies, **il ne corrige aucune catégorie fausse** — ce qui le distingue de #729 (542 mandats sous deux catégories) et #730 (8 mandats ministériels en `commission`), tous deux ouverts — et il ne retire pas les 5 intitulés de navigation ; condition de retrait écrite : le champ disparaît quand plus aucune entrée n'est sans estampille. 15 tests, trois mutations vérifiées échouantes, suite complète à 3 709, 0 échec.
- `2026-09-03` <a id="fonctions-exercees-duree-de-siege-328"></a>[La section 1 devient « Les fonctions exercées », et son nombre est une durée de siège (#328)](decisions/fonctions-exercees-duree-de-siege-328.md) — la frise et le détail daté étaient montés **deux fois** sur la même page (le même composant `Frise`, pas une copie), et la section 1 les republiait à quelques centaines de pixels d'écart ; elle ne garde donc que ce que personne d'autre ne montre, et son titre le dit — « Expériences parlementaires » écarté parce qu'un bloc « Organismes extra-parlementaires » l'aurait contredit à l'écran. Le nombre affiché cesse d'être un **compte d'enregistrements** (27 entrées pour un siège continu de 5 ans 10 mois chez `jerome-guedj`, contre 4 entrées pour 2 jours à la commission des lois — le compte ne distingue pas les deux) et devient une **durée de siège en UNION d'intervalles**, jamais une somme : la fusion additive a laissé des doublons littéraux, et la somme donnerait 9,5 ans là où il y en a 5,8. Chaque catégorie montre ses **trois plus longues**, et une seule peut porter la marque — elle dépasse la **moitié du temps de mandat**, test de majorité sans constante, avec pour dénominateur l'**union des sièges électifs**, un vrai tout puisqu'on ne siège pas deux fois à la fois (§2 règle 7) ; le total des fonctions n'en serait pas un, treize groupes d'amitié simultanés faisant 33 ans sur une carrière de 19. Elle **sait se taire** : 4 fois sur les 13 blocs des deux profils de référence. La marque est **sans teinte** — aucune n'était libre, le jaune étant pris par la sélection, l'action et le badge de source, le vert et le rouge par les positions de vote, le bleu et le bronze par les institutions — donc un filet d'encre, lisible en niveaux de gris sans pictogramme de secours. Le **rôle** ne s'affiche que lorsqu'il distingue (`Membre` couvre 90,7 % des mandats de commission), ce qui fait apparaître que Guedj préside le groupe d'études « Longévité et adaptation de la société au vieillissement ». Les intitulés se coupent à **deux lignes** — une seule perdait trop sur des libellés de 200 caractères — et le « … » est un vrai bouton, posé seulement sur ce qui déborde. Le critère d'en-tête part, la règle descend en pied de section avec son lien vers la méthodologie. Deux défauts de collecte que la liste à plat noyait sont devenus visibles et ouverts à part : #729 et #730.
- `2026-09-02` <a id="conformite-index-syceron-719"></a>[Un index Syceron en cache est un parsage en cache, et l'existence n'y est pas la conformité (#719)](decisions/conformite-index-syceron-719.md) — le run `33652389393`, lancé avec `collect_interventions=true` pour appliquer #710, a **réussi** et n'a corrigé **aucun** des 2 041 sujets de créneau (chiffre pour chiffre, 14 817 interventions à sujet inchangées sur un corpus passé à 680 298) : les profils bruts qu'il venait d'écrire portaient **0** clé `sujet_code_grammaire` sur les 3 963 de `gabriel-attal` et les 2 376 d'`edouard-philippe`, parce que `.cache/syceron_an/<leg>/index_par_acteur` **n'est pas une archive mise de côté mais le produit de `parse_syceron`** — un index en cache est un *parsage* en cache, et sa clé `public-data-cache-an-<semaine>-interv-<empreinte>` dit quand, dans quel mode et sur quelles archives l'entrée a été écrite (#550), **jamais avec quel parseur** ; les deux gardes existantes ne couvraient pas le cas (#505/#510 refusent un index construit sur une archive absente ou n'ayant résolu aucun acteur, #550 une entrée écrite pendant une panne d'archive — aucune ne refuse un index correct écrit par un code périmé) ; la règle manquante existait pourtant **deux fois** — `AGENTS.md` §5 pour le cache d'amendements, #639 pour celui des scrutins, où accepter un store sans `type_scrutin` aurait publié 43 des 66 motions de censure en `vote_texte` — d'où `_syceron_index_qualifie`, qui lit un répertoire non qualifié **comme absent** et laisse l'appelant reconstruire : test sur la **clé** et non sa valeur (`sujet_code_grammaire` vaut légitimement `None`, règle de `_scrutins_store_qualifie`), **une seule tranche lue et c'est la plus petite** (une entrée tranche, et #628 interdit de charger une tranche de plusieurs Mio pour ça), refus par `continue` et non `return None` pour qu'un index complet périmé ne masque pas un index réduit conforme (asymétrie de #657 intacte), verdict mémoïsé par **chemin absolu** (piège de #377) et **oublié à la publication** — sans cet oubli, l'index qu'on vient d'écrire serait refusé et chaque acteur reparcourrait l'archive, 12,5 s et 3,8 Gio de pic par candidat (#510) ; **le lot ne corrige pas #710, il lève ce qui l'empêchait** — les 2 041 tombent au prochain run avec `collect_interventions=true`, et c'est là que se mesurera enfin la perte sur `tags_thematiques` (765 interventions, 93 couples annoncés sur 481 profils, à remesurer sur 641), le `allow_declared_losses` de #710 restant à ce jour **non vérifié** puisque les deux runs ont rendu « aucune perte bloquante » faute d'avoir rien corrigé ; **aucun `cold_start` n'est nécessaire** et il ne protégeait rien (le prochain champ ajouté au parseur serait retombé dans le même trou) ; condition de retrait écrite — la clé de cache cesse d'être un mensonge le jour où elle porte une empreinte du **code**, et d'ici là chaque champ neuf doit rejoindre `SYCERON_CHAMP_QUALIFICATION` ou passer inaperçu ; effet de bord mesuré et **déclaré non corrigé** : les 6 tests qui échouaient en local et pas en CI (688 interventions rendues pour 1 attendue) passent parce que le cache du poste est antérieur à #710 et donc refusé — un test dont le verdict dépend du cache du développeur reste un défaut, seulement devenu invisible. 13 tests, suite complète à 3 671, 0 échec.
- `2026-09-02` <a id="creneau-de-seance-nest-pas-un-sujet-710"></a>[Un créneau de séance n'est pas un sujet, et le discriminant reste structurel (#710, 02/09/2026)](decisions/creneau-de-seance-nest-pas-un-sujet-710.md) — `AGENTS.md` §3e réserve `sujet` au `code_grammaire` du point depuis #510, mais `TITRE_TEXTE_DISCUSSION` **n'est pas homogène** : c'est le titre du point d'**ordre du jour**, et l'Assemblée y inscrit tantôt un texte (« Droit à l'aide à mourir ») tantôt un **créneau de séance** (« Questions au gouvernement », « Questions orales sans débat », « Questions au Premier ministre »), d'où un faux thème dans `theme_officiel` puis `tags_thematiques` (§2 règle 8) ; deux causes mesurées sur les **61 109 points** des archives 16 et 17 — **`QPM_1_1` manquait au vocabulaire** (35 points, 629 paragraphes héritant du titre du créneau au lieu de « Parcoursup ») et **un créneau se reconnaît à ce que la source range dessous** (`_creneaux_de_questions` : un `TITRE_TEXTE_DISCUSSION` enfermant des points de question, **279 des 2 138** titrés), passe **préalable** parce que ces points sont des frères XML venant après lui en ordre de document ; **aucune liste de libellés**, et ce n'est pas un principe : la source publie *quatre* variantes typographiques du même créneau sur ces deux seules législatures, un filtre lexical en manquerait trois — défaut de #672 et de #639 —, et un test remplace le libellé par une chaîne sans rapport pour vérifier que le verdict ne bouge pas ; **cinquième occurrence** de la famille #492 / #639 / #641 / #696 : `backfill_sujet_seance` reporte trois champs nommés sur l'entrée déjà collectée, seulement ceux que l'entrée neuve porte (une entrée réduite au thème n'a pas de `sujet`, #657), **sans jamais toucher la clé de fusion** (#668), sur une preuve sourcée différente aux deux étages — la clé `sujet_code_grammaire` au brut, `source.type == "syceron"` au pivot, republier le code coûtant ~48 o sur **636 461** entrées réduites — et c'est le premier de la famille qui **retire** une valeur au lieu d'en poser une ; **`tags_thematiques` devient dérivé** (`schema_pivot.deriver_tags_thematiques`, comme `chambres` et `licence_donnees`) parce que `merge_pivot_profile` **unissait** ancienne et nouvelle liste, ce qui rendait tout tag immortel — le recalcul ne perd rien par lui-même, mesuré **39 782 = 39 782** couples (profil, tag) sur 0 profil d'écart ; effet mesuré, législatures 16-17 seules : **765 interventions publiées corrigées sur 81 des 481 profils** (282 perdent leur sujet, 483 reçoivent celui de la question réellement posée), **93 couples (profil, tag) retirés et 141 ajoutés**, et sur les 5 fiches AN « questions au premier ministre » tombe du **32e rang** de `AN:LR` et du 43e de `AN:SOC` ; **perte sur une liste stable**, donc `allow_declared_losses` au run de remédiation (#460/#470) ; **ce que le critère ne tranche pas est compté** — « Motion de censure » (32 points, 11 665 paragraphes ; 243 profils tagués), « Motions de censure » (192) et « Déclaration du Gouvernement et débat » (125) restent publiés, la source ne portant pour eux **aucune** marque structurelle (`@valeur` remplie sur 1 335 des 2 138 points titrés textes et créneaux confondus, `sousIntitule` à « 0 » sur les 2 138), et un `code_grammaire` absent ou inconnu ne devient **jamais** procédural par défaut, le critère étant positif des deux côtés (**0** point sans code sur 61 109) ; non mesuré et déclaré : la **XVe législature**, absente du cache (`.zip.part`), qui porte **255 993 des 651 695 interventions Syceron publiées (39,3 %)** — tous les chiffres ci-dessus sont des bornes inférieures —, `type_detail` qui garde son repli lexical hors périmètre, et **aucun run lancé** (jointure vérifiée : 388 751 / 388 751 entrées publiées retrouvées dans l'archive rendent exactement le sujet publié avant correction).
- `2026-09-02` <a id="derniere-lecture-retenue-711"></a>[La règle « dernière lecture » d'AGENTS.md §6 est implémentée, et c'est la DATE qui ordonne (#711)](decisions/derniere-lecture-retenue-711.md) — `AGENTS.md` §6 la publie depuis l'origine, le `DESIGN_SYSTEM` §6 la cite comme la voix de la maison et la page de méthodologie l'annonçait **au lecteur** ; **aucun code ne l'appliquait** — `isWholeTextVote` (#672) sélectionnait les votes sur l'ensemble d'un texte et s'arrêtait là. Mesuré au commit de données `f635cb60` sur les 17 748 scrutins publiés : **925** votes sur l'ensemble d'un texte, **697** textes une fois les lectures repliées, 187 textes votés plusieurs fois, donc **228 scrutins (24,6 %)** comptaient une lecture déjà comptée. Sur les **13 profils `candidat_declare`**, `gabriel-attal` passe de 150 votes à **111 textes** et de « 140 pour · 7 contre · 3 abstentions » à **« 103 · 6 · 2 »** — et ce qui disparaît **retire une affirmation fausse** : son « contre » de première lecture du 17/06/2025 sur le projet de loi de simplification de la vie économique, qu'il avait déposé comme Premier ministre, alors que la loi a été adoptée sur le **texte de la CMP** le 14/04/2026, scrutin où aucune position de lui n'est enregistrée (dire pourquoi il y manque publierait une absence individuelle, §2 règle 3). **Le regroupement par libellé est permis** parce que `regrouper-nest-pas-joindre-639` l'autorise **au sein d'une même source** et surtout parce que son **mode d'échec est sûr** — un intitulé mal replié *échoue à regrouper*, il ne rapproche jamais à tort, l'inverse exact de `texte_vise` (#696) où un appariement par libellé aurait **fusionné** des textes distincts. **La date ordonne, jamais le rang** : le rang n'est pas un champ, il n'existe que dans l'intitulé et il y manque **51 fois sur 925**, et **4 groupes** mettent les deux ordres en contradiction (le PLFR 2017 porte deux « première lecture » encadrant une « lecture définitive »). Le vocabulaire des mentions, **dupliqué et incomplet des deux côtés** (`lecture.js` et `utils/groupe.js`), est écrit une seule fois et les deux ancrages s'en déduisent : le cadrage en nommait 8, la mesure en trouve **16** dont **4 coquilles de la source**, **874 / 925** sont repliés et **51** ne le sont pas (49 sans parenthèse finale, 2 en `(art. 34-1 de la Constitution)`) ; les ordinaux **en chiffres** exigent le mot « lecture » derrière eux, sans quoi le `(2ème vote)` de `an:14:1086` souderait un scrutin annulé à son remplaçant, et le `(2)` du PLFR 2020 (2) distingue **deux textes**. Les **51 sans mention ne deviennent pas des premières lectures** (§2 règle 5) : ils gardent leur titre nu, qui *est* la clé, et 14 rejoignent ainsi un groupe — `an:14:594`, dernière lecture de la transparence de la vie publique, n'en porte aucune. La clé porte la **législature** : sans elle **10 groupes** souderaient deux textes homonymes de deux législatures, et compter deux fois est un manque quand souder serait une affirmation. **La dernière lecture se lit sur le CORPUS**, pas sur les votes de la personne — 120 textes contre 111 sur `gabriel-attal` — et un corpus absent se **déclare** (`derniereLectureDisponible`) au lieu de retomber sur une règle de repli muette (#510). **Ce qui reste faux est mesuré** : 9 groupes, **20 scrutins des 925, borne haute**, portent deux fois la même mention à des dates différentes dans une même législature — le sens de l'erreur reste le bon, le scrutin retenu est toujours un scrutin réel de la personne, ce qui se perd est un texte et jamais une position attribuée à tort. `audit_diff_profils` ne voit rien et **c'est correct** : aucun fichier de `pivot_data/` n'est touché, les 925 positions restent publiées et ce qui baisse est un **dénominateur affiché**, ni scalaire surveillé ni liste stable. Ni collecte ajoutée, ni rattachement au dossier rouvert ; `ecartsAvecLeGroupe` et `grandesLois` sont **volontairement** laissés non repliés. 36 tests, **12 mutations vérifiées échouantes**, dont deux fixtures renforcées après avoir passé au vert sur un module amputé.
- `2026-09-02` <a id="entree-derivee-correspondance-715"></a>[Une entrée dérivée gèle un slug fabriqué, elle ne prouve plus rien (#715)](decisions/entree-derivee-correspondance-715.md) — le premier run à profiter de la porte ouverte par #708 a collecté **160 profils neufs** et **n'a rien committé** : la §5b du portail exige une entrée de `raw_data/correspondance_acteurs_an.json` pour tout profil publié, et le remède qu'elle imprime est **inerte** — `build_correspondance_acteurs_an.py` part des profils **publiés**, que le commit bloqué empêche d'exister ; #708 a levé la circularité à la **collecte**, elle restait entière à la **table**, et le run n'est pas rejouable (`actions/checkout` fige le SHA au déclenchement d'un `workflow_dispatch`) ; la cause est que la table fait **deux métiers** dont un a disparu — pour les 481 slugs hérités de NosDéputés, l'acteur AN était à *découvrir*, et une découverte se prouve ; pour un slug **fabriqué depuis** l'acteur (`slugify(état civil AMO30)`), il n'y a rien à prouver, ce que deux mesures recoupent (**160 / 160** des slugs fabriqués sont collectés depuis `.../fiche/OMC_PA######` portant **exactement** leur `acteur_ref`, et **477 des 481** entrées committées portent cette même URL en `preuve`, **476** à la même date : produites en lot, seules les **10** entrées à écart ayant été arbitrées) ; ce que l'entrée apporte encore est **réel et unique** — le **gel** de l'identifiant, sans lequel un changement de nom d'usage déplacerait le slug au run suivant (#487, #668 ; 4 des 10 écarts de #525 en sont) — d'où une clé `origine` fermée (`relue` | `derivee`), **facultative en lecture** avec un défaut qui est un fait daté (la porte de fabrication n'existait pas avant), `ECARTS_CONNUS` ne pouvant pas la porter puisqu'une entrée dérivée n'a **aucun** écart — le validateur refuse les deux ensemble ; la passe `--completer-derivees` est **additive** (elle reconduit le document **brut**, pas la table normalisée, et ne réécrit pas le fichier quand il n'y a rien à ajouter), **hors ligne** (un second téléchargement AMO30 avant le commit ferait qu'une panne de source coûte un run valide, #524 ; l'état civil vient du pivot du run, lu par projection, #628) et **disjointe** de `construire()` ; trois filtres l'empêchent d'être un tampon — la table passe devant (#708 §3), le profil doit être **publié**, et son `identifiants.an` doit valoir **exactement** l'`acteur_ref` que le roster déclare, un désaccord n'écrivant rien et sortant en 1 (#540 sur le seul identifiant du dépôt) — l'autorité de « qui est fabriqué » étant `slug_origine` et jamais autre chose, ce qui laisse entier le refus de #525 §6 ; câblée dans `merge-and-pivot` **après** les pivots et **avant** le portail, avec la moitié qui manquait : `raw_data/correspondance_acteurs_an.json` entre dans le `git add` du workflow — il n'y était pas, et sans lui le gel serait une illusion, la panne que `AGENTS.md` §3a nomme déjà pour les index partagés ; la §5b publie désormais le nombre d'entrées `derivee`, qui est la **file d'attente de relecture** que #708 §8 nommait sans la rendre visible — un compteur, pas un seuil ; **la condition de retrait de #525 §7 est inchangée**, les 160 restent `derivee` jusqu'à relecture humaine (elle seule remplit `prenom`/`nom`, qu'AMO30 sépare et que la passe hors ligne laisse à `null`), et ce lot **ne relance pas** le run bloqué.
- `2026-09-02` <a id="les-grands-chiffres-fiche-candidat-328"></a>[« Les grands chiffres » : une frise qui commande deux colonnes appariées (#328)](decisions/les-grands-chiffres-fiche-candidat-328.md) — le bloc de tête de la fiche candidat, arbitré en maquette : la frise du parcours devient l'ossature et sa couleur relie chaque piste à sa colonne (bleu/bronze, le seul couple qui survive au daltonisme rouge-vert) ; la posture passe en **motifs** et distingue « non déclarée par l'Assemblée » de « non renseignée chez nous » ; les lignes sont **appariées** et ordonnées par degré d'engagement de l'acte, les tirets étant des faits (« un ministre ne vote pas », son siège est tenu par son suppléant) ; une concentration ne s'affirme que si un texte porte **plus que tous les autres réunis** — un P90 a été essayé et écarté parce qu'un percentile ne peut jamais se taire ; les dossiers se listent **par date**, jamais par volume ; l'empreinte thématique vient de la **commission saisie au fond**, sourcée, jamais d'un titre ; et la ligne « votes » est **retirée** après trois tentatives mesurées, parce que tout ce qui serait retenable y est soit interdit (§2 règles 3 et 7) soit inaffichable en un chiffre — c'est en la construisant qu'on a failli publier qu'Attal avait voté contre sa propre loi, ce qui a ouvert #711.
- `2026-09-02` <a id="mise-en-oeuvre-des-grands-chiffres-328"></a>[« Les grands chiffres » : la mise en œuvre, et les trois constats qu'elle corrige (#328)](decisions/mise-en-oeuvre-des-grands-chiffres-328.md) — le bloc de tête est écrit : `grandsChiffres()` et `pistesDuParcours()` dans `profilCandidat.js`, `GrandsChiffres` dans `CandidateProfile.jsx`, les sélecteurs `.cp-gc*` dans sa feuille. La vue de « L'essentiel » est retirée — section, `Point`, 25 sélecteurs CSS, 5 tests — mais **le vivier reste calculé et testé**, parce que la décision de la veille prévoit qu'un vrai résumé prenne la place libérée ; son retrait est à la charge de ce lot-là. Trois constats de la maquette sont corrigés par la mesure : le cas « ni l'un ni l'autre » vaut **3 des 13** et non 4 (`jordan-bardella` a un siège européen, donc une colonne), l'empreinte par commission saisie est **inerte** faute de `pivot_data/commissions_dossiers.json`, jamais produit, et la barre des textes ne publie **que ce qui est parvenu au moins en commission** (§6), en disant ce qu'elle écarte. Le rôle range le texte, jamais l'institution seule : replier `rapporteur` sur le Parlement afficherait « 3 propositions de loi » pour 2 propositions et 1 rapport. **20 tests, 9 mutations vérifiées échouantes** ; aucun runner JS dans le dépôt, donc ni mise en page, ni responsive, ni contraste, ni clavier.
- `2026-09-02` <a id="regles-de-forme-des-vues-326"></a>[Ce qu'une vue doit au lecteur : sept règles de forme, et ce que chacune a coûté (#326)](decisions/regles-de-forme-des-vues-326.md) — `AGENTS.md` §2 dit ce qui est **interdit**, `DESIGN_SYSTEM.md` dit la palette et le ton ; **ni l'un ni l'autre ne disait ce qu'une vue doit au lecteur** — mesuré le 02/09/2026, le mot « lecteur » n'apparaissait **pas une fois** dans les 208 lignes du design system. Les sept règles arbitrées en maquette sur la fiche candidat sont extraites du lot où elles étaient enfouies et rendues transversales, chacune **avec son coût**. La première est neuve et sans équivalent dans §2, qui ne dit jamais ce qui est **inutile** : *un chiffre dont le lecteur ne peut rien tirer ne se publie pas* — la répartition des votes a été retirée alors qu'elle était vraie, collectée et conforme aux huit règles, après instruction de **cinq pistes** dont deux tombaient sous §2 et une sur l'absence de rattachement sourcé (#639). Suivent : *le texte explicatif est un aveu d'échec* (ligne d'introduction réduite **trois fois**, jusqu'à cinq mots ; vaut aussi pour les noms, « Coup d'œil » et « L'essentiel » écartés) ; *seuls les nombres sont en gros* ; *chaque mesure compte contre son propre total* ; *aucun seuil arbitraire ne décide qu'il y a un fait* (un P90 sélectionne toujours 10 % des dossiers, donc il ne peut **jamais** se taire) ; *quand le volume ne distingue pas deux intentions, c'est la date qui range* ; *deux absences ne se confondent jamais*. Elles **s'ajoutent** à §2 sans l'amender — §2 est non négociable, celles-ci s'affinent — et `DESIGN_SYSTEM.md` gagne une §6 bis qui les énonce et renvoie ici. Appliquées à la seule fiche candidat : #329 et #330 ne sont pas relues à leur aune, et la règle 1 n'a pas de test, parce que « le lecteur ne peut rien en tirer » est un jugement.
- `2026-09-02` <a id="slug-fabrique-membre-de-roster-708"></a>[Un membre de roster sans correspondance relue reçoit un slug, et la collision reste un refus (#708)](decisions/slug-fabrique-membre-de-roster-708.md) — `build_roster_candidats_detaille` laisse tomber un membre sans slug **depuis toujours**, et c'était inoffensif tant que NosDéputés servait le roster avec ses slugs ; #527 a basculé la source sur AMO30, qui ne publie qu'un `PA######`, et l'exclusion est devenue **muette** — la cause n'était pas une table incomplète mais une **circularité**, `raw_data/correspondance_acteurs_an.json` étant construite depuis les profils **publiés** : pour avoir un profil il fallait un slug, pour avoir un slug il fallait un profil, et la table était donc **fermée**, ce que la mesure montre en creux (les **304 / 304** membres de la XVIIe qu'elle couvre avaient tous déjà siégé avant la XVIIe) ; `an_roster.resoudre_slugs` ouvre **une** porte — `text_utils.slugify(état civil AMO30)`, la seule fabrique de slugs du dépôt — avec la **table devant, sans exception**, ce qui est la garantie de #487/#668 (un changement de **nom d'usage** ne déplace pas l'identifiant d'une personne déjà collectée, et 4 des 10 écarts de #525 en sont) et sur l'index GP **entier**, jamais les groupes configurés ; trois motifs fermés refusent tout slug (`nom_absent`, `slug_deja_publie` — le slug visé appartient à **quelqu'un d'autre**, `hors_an` compris —, `homonymie_amo30`), le même slug porté par la **même** personne n'étant structurellement pas une collision puisque la table est lue d'abord ; **`ROSTER_SANS_SLUG` change de sens sans devenir faux** — il compte désormais ce qui reste *réellement* écarté et non un état que personne ne pouvait quitter : **0** sur les **1 662 acteurs de l'index GP sans entrée de table** (dont les 160 des 10 groupes configurés), une mesure du corpus et non une propriété du code —, et son miroir `ROSTER_SLUG_FABRIQUE` (en `notice`) nomme qui entre par une porte que personne n'a relue ; **fabriquer le slug d'un membre neuf n'est pas combler une entrée relue** (#525 §6 refuse de réécrire une preuve relue ; ici il n'y a **rien** à contredire) et la distinction tient parce que la **§5b du portail échoue en dur, seuil 0**, sur tout profil publié sans entrée : un slug fabriqué sert à *collecter*, jamais à *publier* ; **la condition de retrait de la table (#525 §7) est inchangée** — ce lot contourne son absence à l'entrée d'un membre nouveau, il ne la rend pas facultative ; **le coût est mesuré et non extrapolé** : les profils bruts étant partitionnés par législature (#580), un primo-député de 2024 pèse socle + une tranche = **6,8 Mio** en médiane (8,7 en moyenne) et non les 11,8 Mio de la médiane du corpus entier, donc **1,07 Gio** (médianes) à **1,44 Gio** (moyennes) pour les 156 entrants — **1,7 à 2,3 fois moins** que les « +1,8 à +2,5 Gio » qui circulaient (145 ne siègent qu'en XVIIe, 11 ont siégé avant, dont 3 en XIIe/XIIIe que le corpus ne couvre pas : borne déclarée) ; six mutations vérifiées échouantes (fabrication, règle de collision, priorité de la table, les deux compteurs, `slug_origine` à travers `filter_roster_by_sigle`) ; **aucun run n'a été lancé** — les 156 profils n'existent pas, et la couverture des 5 fiches de la XVIIe (305 / 461, `AN:SOC:17` à 41,4 %, #700) ne remonte que le jour où ils sont collectés **et** leur correspondance relue.
- `2026-09-01` <a id="coup-d-oeil-choses-nommees-328"></a>[Le coup d'œil publie des choses nommées, pas la forme d'une distribution (#328, 01/09/2026)](decisions/coup-d-oeil-choses-nommees-328.md) — sept retours de la propriétaire sur le seul bloc d'ouverture de la fiche candidat, livré par #684 : deux de ses cinq lignes étaient des **statistiques de distribution** (« 6 dossiers sur 34 concentrent 2 206 de ses 2 429 amendements »), et le défaut est de nature — un ratio de concentration décrit la forme d'une distribution, jamais ce sur quoi la personne travaille, donc aucune reformulation ne le sauve ; chaque point **nomme désormais une chose** (`agregerAmendements` rend `dossiers` et non plus `concentration`, `SEUIL_CONCENTRATION` supprimée avec la note qui la défendait), et un dossier n'est nommable que si la source le nomme — `textes[texte_vise].titre`, ou le `texte_vise` lui-même quand ce n'est pas une référence de source (**2 458 des 2 831** dépôts de Jean-Luc Mélenchon sont visés par « Système universel de retraite », clé d'aucune entrée `textes`), **jamais** la référence brute, le critère étant structurel (une référence ne contient pas d'espace) ; ce qui reste à découvert est **dit** — l'index de la XIVe n'a qu'**une** entrée `textes`, donc aucun des 12 dossiers de Xavier Bertrand ni des 3 d'Édouard Philippe n'est nommable (§2 règle 5) ; le défaut le plus grave était **arithmétique** — le point « questions » divisait le compte du premier sujet par TOUTES les questions, y compris celles sans sujet publié, ratio juste par accident tant que la couverture est totale et faux dès qu'elle ne l'est plus —, corrigé en `avecSujet` (compté sur l'ensemble, jamais sur la tranche de douze affichée), et de même sur la qualité d'orateur (`sourcees`) et les amendements (`depotsNommes`) ; **aucune caractérisation de la dispersion** (« sujets très divers » décrirait notre collecte en croyant décrire son travail), les nombres suffisant — 9 / 250 et 154 sujets distincts chez Mélenchon se lisent autrement que 28 / 215 et 102 chez Guedj ; les notes se trient sur une frontière — celle qui prévient un **contresens** reste (« questions reçues depuis le banc du gouvernement, pas des questions posées », « dont 13 projets de loi »), celle qui justifie **notre méthode** part (le pied de bloc « nous ne classons pas les textes par sujet », le seuil à 90 %, l'intitulé officiel) ; le cadre initiative/réaction étant parlementaire par construction, **deux gabarits sont refusés** et **seule la phrase d'introduction s'adapte**, sur un fait collecté — avoir été *membre* d'un gouvernement, **6 des 13** (un parlementaire en mission n'en est pas un, ce qui écarte les deux mandats « en mission » de Jérôme Guedj) ; **la mesure qui motivait le retour ne se reproduit pas** : la couverture des sujets de questions, donnée à 4 % pour Mélenchon le matin même, est **100 % sur les 7 profils qui en posent ou en reçoivent** au SHA `e40d0d3`, et le problème a changé de nature — dispersion (premier sujet de 3,6 % à 22,7 %) et non couverture ; un **seuil** décidant si un premier sujet « ressort » assez est écarté, sa valeur étant un arbitrage éditorial déguisé en mesure ; vérifié par rendu SSR réel des 13 profils, harnais hors dépôt.
- `2026-09-01` <a id="effectif-du-groupe-dans-le-temps-702"></a>[L'effectif d'un groupe dans le temps : `min_historique` et `max_historique` portent leur date (#702)](decisions/effectif-du-groupe-dans-le-temps-702.md) — les deux champs valaient `null` sur les **7 fiches** depuis l'origine du schéma ; ils publient désormais `{valeur, date}`, balayés à chaque `debut_dans_groupe` et au **lendemain** de chaque `fin_dans_groupe` (borne de fin inclusive, `_appartenance_couvre`), sur la **fenêtre de la fiche** — `periode.debut` → `periode.fin`, `date_reference.date` prenant le relais tant que la période est ouverte, jamais au-delà. Mesuré sur les 5 fiches AN de la XVIe : **REN 167 → 175**, **RN 87 → 89**, **LFI 74 → 75**, **LR 61 → 61**, **SOC 31 → 31** — l'écart `membres[]` / `a_la_date_de_reference` (24 sur REN) est de la **rotation**, pas une amplitude, et le lire comme telle aurait publié un chiffre trois fois trop grand. **Seuil 0** : une seule entrée sans `debut_dans_groupe` laisse les deux bornes à `null` avec son motif — ce membre n'est comptable à aucune date (#653), et les bornes obtenues sans lui sont des bornes inférieures (0 / 452 sur les 5 fiches AN, 14 / 15 et 4 / 5 sur les 2 fiches Sénat gelées, #516, qui gardent `null`). Limite nommée et non mesurée : `membres[]` ne porte qu'**un intervalle par membre** (périodes recollées, #526), donc un départ suivi d'un retour reste invisible et l'amplitude est un minorant. Trois formes lues (`null`, entier nu hérité, objet), une seule produite ; ni série, ni taux, ni classement (§2 règle 1).
- `2026-09-01` <a id="fiche-de-groupe-reprise-329"></a>[La fiche de groupe reprise de bout en bout : l'ordre des questions, pas celui du schéma (#329)](decisions/fiche-de-groupe-reprise-329.md) — la version livrée par #681 est **éditorialement irréprochable et structurellement inutilisable** : ses sections s'appellent « Cohésion de vote », « Empreinte thématique », « Amendements déposés », le vocabulaire du schéma et non les questions d'un électeur, et son fait le plus important — **sur 3 843 scrutins, la cohésion de `AN:SOC` n'est mesurable que sur 341** — était enterré en fin de section « Vérification » ; six sections dans l'ordre des questions (qui sont-ils · sur quoi ils travaillent · ce qu'ils proposent · comment ils votent · comment ils se situent · ce que la fiche ne dit pas), une seule focale à la fois, l'interne d'abord et la comparaison à la fin, le **quorum ouvrant la section des votes et non la page** ; les **absences ne franchissent plus l'écran sous aucun libellé** — « Sans trace de vote » était un libellé prudent sur une donnée interdite, et aucune largeur affichée ne se rapporte plus à `membres_eligibles` (§2 règle 3) ; les **fonctions exercées**, que rien n'affichait, sont réunies en quatre familles plus `autre`, **affiché avec ses intitulés d'origine** — 40 libellés distincts sur les 7 fiches, et la maquette en perdait un : 1 351 sièges simples publiés pour `AN:SOC` là où la fiche en porte **1 352**, un « représentant suppléant » rangé nulle part, tandis que `AN:REN` y révèle 9 intitulés ministériels ; sa prose se trompait aussi (« LFI en compte 3 » rapports contre 17 : avec la même règle des deux côtés, **9**), d'où le retrait de toute comparaison chiffrée entre groupes à cet endroit ; les convergences se publient en **trois natures** parce que « nuance » n'est pas « opposé » — SOC et RN ne sont opposés que **46** fois sur 231 mais en nuance **106**, là où un décompte brut afficherait « 152 divergences » —, ordonnées par nombre de scrutins comparables et jamais par accord, avec une quatrième nature `autres` mesurée à **0** et publiée pour que ce zéro reste vérifiable ; les **grandes lois** ne reconstruisent aucune clé de dossier (`texte_lie_id` nul sur **4 105 / 4 105** scrutins de la XVIe, `AGENTS.md` §4 l'interdit) mais regroupent les scrutins **dont l'intitulé officiel nomme le même texte**, ce que le compte publié dit mot pour mot, avec sa borne (51 intitulés n'en nomment aucun) — et la maquette annonçait un « top 8 » qui n'en était pas un, omettant trois textes mieux classés pour retenir le pouvoir d'achat à 67 ; un texte adopté sans vote n'y figure pas (le PLFSS rectificatif 2023, **172** scrutins, 3ᵉ de la législature, §2 règle 4) ; `position_politique` (#686) **n'est portée par aucune des 7 fiches** et le composant le déclare plutôt que de la simuler ou de la dériver d'un comportement de vote, `non_declaree` restant distincte d'un champ absent, et une fiche du Sénat disant qu'aucune source équivalente n'y est collectée ; la comparaison entre groupes passe par une **projection de build** (`scripts/comparaison-groupes.mjs`, **51 Ko contre 15,1 Mo** de fiches voisines, 2 415 positions au-dessus du quorum sur 19 832 entrées) — règle #628 appliquée au navigateur —, non bloquante : son absence se DIT, section par section ; réunie **par posture** et jamais alignée sur une échelle unique, sans aucun pourcentage (REN fait adopter **30 686** amendements, les quatre autres fiches **21 558** en en déposant **386 810** contre 142 143) ; vérifié par **rendu SSR réel** (`react-dom/server`, harnais hors dépôt) sur trois fiches et sur le chemin sans comparaison — ce qui ne couvre ni la mise en page, ni le responsive, ni le contraste, ni le clavier, le dépôt n'ayant aucun harnais de test JS.
- `2026-09-01` <a id="fiches-groupe-17e-legislature-700"></a>[Les 5 groupes de la XVIIe entrent dans `groupes[]`, et leur succession est déclarée comme une relecture (#700)](decisions/fiches-groupe-17e-legislature-700.md) — les 5 fiches de groupe publiées décrivent toutes la **XVIe**, close le 09/06/2024, alors que `correspondance_sigles_an` portait **déjà** les cinq groupes de la XVIIe, mesurés et relus le 26/08/2026 ; ce qui manquait tenait en cinq entrées du tableau **`groupes[]`**, le seul que le pipeline lise pour savoir quelles fiches produire, et la mention laissée dans la config — « la publication est le lot 1b » — désignait **#527**, qui a basculé la *source* du roster sur AMO30 et s'est fermée sans publier une fiche : elle est retirée, et un test échoue si elle revient ; **« 156 nouveaux profils » ne se reproduit pas** — `build_roster_candidats_detaille` laisse tomber un membre sans slug et un slug ne s'invente pas (#525), donc un run produit **0 profil brut neuf** : les 461 membres de la XVIIe rendent **304 slugs distincts** (`belkhir-belhaddad` siège dans deux groupes), **303 sont déjà dans le roster de la XVIe**, le seul propre à la XVIIe est `laurent-wauquiez`, et **304 / 304 ont déjà leur profil brut ET leur pivot** ; le roster de candidats passe de 452 à **453**, le poids ne bouge pas (`raw_data/profiles/` = **7,70 Gio** pour 481 slugs, médiane **11,8 Mio**, max **58,0 Mio**, garde-fou #580 à 80 Mio **par fichier**), et les 156 sans slug coûteraient +1,8 à +2,5 Gio le jour où quelqu'un les instruit (#691) ; `groupe_nom` est le `organe.libelle` **verbatim** de l'archive, remesuré en test sur la réduction `amo30_gp_leg16_17.zip` ; **`succede_a` passe sur la fiche** parce que `web/` ne lit que `pivot_data/`, **optionnel** comme #686 et #653, et il reprend le patron de #686 avec **une dissymétrie qui est le fait** : `position_politique` **exige** un `source_url`, `succede_a` l'**interdit** — l'Assemblée ouvre et ferme des organes (`PO800508` clos le 09/06/2024, `PO845425` ouvert le 18/07/2024), elle ne les chaîne pas, et c'est `etabli_par: "relecture_humaine"` (vocabulaire fermé à **une** valeur) plus `verifie_le` qui disent d'où l'affirmation vient, la preuve publiée étant les `sigles_an`/`organes_an` du prédécesseur, verbatim ; une succession qui ne résout pas est refusée **deux fois** (patron #485) — dans la table, **après** la boucle pour que le verdict ne dépende pas de l'ordre des entrées, et au **§4 du portail**, seuil 0, sur un `succede_a.fichier` qui ne désigne aucun document publié ; **`groupe_id` reste opaque** — `AN:EPR`/`AN:DR` sans suffixe, `AN:RN:17`/`AN:SOC:17`/`AN:LFI:17` avec, donc pas uniformément `<chambre>:<sigle>` —, mesuré tenable parce que **rien ne le découpe** dans le dépôt et figé par un test qui interdit un `split(":")` dessus ; les quatre garde-fous sont vérifiés un par un (le **§4b accepte** `non_declaree`, valeur publiée : 0 erreur dure sur les 10 fiches AN montées en `tmp_path` — rien à y corriger ; §5b, `audit_collecte_non_publiee` et `audit_diff_profils` sont sans objet, un fichier qui apparaît étant un gain) ; **ce qui reste à trancher** : la couverture des cinq fiches sera de **305 / 461 (66,2 %)** contre 99,1 % sur la XVIe, `AN:SOC:17` tombant à **29 / 70 (41,4 %)**, et `min_coverage_pct` valant **0** — désactivé depuis #193 faute de chiffres réels — une fiche à 41 % passerait le portail sans un avertissement en publiant une `position_majoritaire` par scrutin calculée sur 29 des 70 membres ; aucun run n'a été lancé, les cinq fiches n'existent pas encore.
- `2026-09-01` <a id="identite-du-push-et-declenchement-des-tests-685"></a>[L'identité du push décide si un workflow voit le commit de données — et depuis quinze commits, aucun ne le voit (#685)](decisions/identite-du-push-et-declenchement-des-tests-685.md) — `AGENTS.md` §3b promettait, au titre de #508, qu'« a deploy-key push emits a `push` event, so `tests.yml` really runs on data commits » : mesuré au 01/09/2026, **0 des 15** commits de données arrivés sur `main` depuis le premier run de `tests.yml` (20/08) ne porte de run de la suite, **11** d'entre eux postérieurs à #508, alors que le workflow a bien tourné en `push` sur tous les autres commits de `main` (124 runs) — ce n'est donc pas un incident mais l'état permanent ; la cause est prouvée par quatre mesures convergentes et par le journal du job `99566091830`, où `actions/checkout` pose `git remote add origin https://github.com/…` puis un `AUTHORIZATION: basic ***` au lieu du `git@github.com:` qu'il poserait si `ssh-key` était renseignée — le dépôt n'a **aucune** clé de déploiement (`GET /repos/{o}/{r}/keys` rend `[]`), le secret `DATA_PUSH_SSH_KEY` ne figure pas parmi ses 4 secrets, et le ruleset n'a ni `required_status_checks` ni acteur `DeployKey` : les **trois gestes manuels** de #508 §7 n'ont jamais été posés, la décision s'étant pourtant réservée (« tant qu'il n'a pas poussé, cette entrée décrit une intention ») avant que `AGENTS.md` ne promeuve l'intention en fait ; ce qui a rendu quinze commits invisibles n'est pas le repli vers le `GITHUB_TOKEN` — documenté et voulu — mais qu'il soit **muet**, le « rejet bruyant » de #508 ne parlant que sur un `GH013`, lequel suppose le check requis jamais rétabli, si bien que **chaque omission rend l'autre invisible** ; `merge-and-pivot` gagne donc un step qui **mesure** `git remote get-url origin` après un push abouti — le fait, là où `secrets.DATA_PUSH_SSH_KEY != ''` ne dirait que l'intention — et nomme la **conséquence** (« `tests.yml` ne tournera pas sur ce commit ») sur les deux canaux qui se relisent, annotation `::warning::` et résumé du job, **sans bloquer** puisque les trois gestes vivent hors du dépôt et qu'échouer priverait le site de ses données fraîches ; `tests/test_ci_signal_identite_push_685.py` **exécute** le fragment de shell extrait du workflow dans un dépôt local sous `tmp_path`, une fois en `https://` et une fois en `git@` (un signal qui crie ou se tait dans les deux cas n'apprend rien), garde un cas échouant si quelqu'un rendait le signal bloquant en passant, et un autre si `tests.yml` gagnait un `paths:` qui donnerait au commit de données une seconde raison de n'être pas couvert ; §3b cesse d'affirmer la garantie et dit qu'elle ne revient qu'avec #508 §7, **jamais en réécrivant la ligne** ; non établi et nommé : ce que les quinze commits non testés auraient révélé, et le comportement sous clé de déploiement, jamais observé dans ce dépôt.
- `2026-09-01` <a id="juxtaposition-position-groupe-328"></a>[Juxtaposer deux positions sourcées n'est pas mesurer un écart (#328)](decisions/juxtaposition-position-groupe-328.md) — §2 règle 7 interdisait « Individual-vs-group gaps », ce qui, lu à la lettre, interdisait d'afficher la position d'un membre à côté de celle de son groupe sur un scrutin donné ; or ce que la règle protège est nommé dans le code — `compute_ecarts_cohesion_internes` calcule des « écarts de cohésion/participation individuels **vs moyenne du groupe** », c'est-à-dire un **taux par personne rapporté à une moyenne**, dont `taux_participation` est exactement le taux d'assiduité que la règle 3 interdit déjà ; la règle distingue donc l'**indice** (jamais public, `--rapport-interne`) de la **juxtaposition d'un scrutin sourcé** (publique, jamais comptée ni notée), avec le garde-fou dans la règle même — « a voté contre son groupe 47 fois » est le même indice par un autre chemin — et le tableau §6 passe d'une ligne à deux ; aucune caractérisation n'est autorisée pour autant, ni « franc-tireur » ni « loyal », l'interprétation restant au lecteur ; le corps de #324 posait déjà cette frontière le 29/08 sans que §2 en tienne compte.
- `2026-09-01` <a id="position-politique-groupes-686"></a>[La position politique d'un groupe est celle que l'Assemblée déclare, lue dans une table committée (#686)](decisions/position-politique-groupes-686.md) — l'AN qualifie **elle-même** ses groupes (`organe.positionPolitique` sur les organes `GP` d'AMO30 : **40 des 63** organes, `Opposition` 24, `Minoritaire` 11, `Majoritaire` 5), le pipeline le lisait déjà pour les profils individuels (`mandats[].position_dans_hemicycle`, #354) et **nulle part pour les 7 fiches de groupe**, alors que la posture commande la lecture de tous leurs compteurs ; les fiches gagnent `position_politique` — `position`, `source_url` **obligatoire même sur `non_declaree`** (miroir de §2 règle 6), `verifie_le`, et `organes[]` qui publie **chaque organe** avec la chaîne source verbatim —, branché par la **table committée** `correspondance_sigles_an` et jamais par ressemblance de sigle : nos fiches disent `REN` et `LFI`, le référentiel dit `RE` et `LFI-NUPES`, et l'appariement direct rendait `None` sur **deux fiches sur cinq, dont la seule majoritaire du corpus** ; `non_declaree` est une **valeur publiée** distincte d'un champ absent — les **14 groupes de la XVIIe** sont dans ce cas, l'AN ne qualifiant qu'une législature achevée, et rien n'est jamais déduit d'un comportement de vote (§2 règle 1) ; `Minoritaire` reste une **catégorie vide**, `DEM` et `HOR` étant déclarés tels en XVIe sans avoir de fiche, et les replier serait un acte éditorial ; **deux organes successifs qui divergent se publient `divergente`**, jamais repliés sur le dernier (qui déciderait laquelle des deux moitiés de la législature définit le groupe, en silence) ni sur `null` (déjà pris par `non_declaree`) — mesuré nul aujourd'hui, les deux organes `SOC` de la XVIe s'accordant, et publié quand même ; le schéma exige que `position` soit **exactement** le résumé de `organes[]`, ce qui fait d'une posture inventée une erreur de schéma ; garde-fou **§4b du portail de qualité, seuil 0** sur le patron du §5b (#525) — fiche AN publiée sans entrée, table invalide, ou position publiée contredisant la table ; le champ reste **optionnel** (#653/#558) et les 5 fiches AN ne le porteront qu'après un run réel, ce que le §4b imprime comme compteur de migration ; `VERSION_INDEX_GP` passe en `v2` parce qu'un index `v1` restauré du cache CI ferait mesurer « aucun groupe qualifié » sur une archive qui en qualifie 40 (#510) ; `POSITION_POLITIQUE_AN_VERS_PIVOT` devient canonique dans `schema_pivot` et la table des sigles passe d'`an_roster` à `groupes_config` (précédent #558) ; **aucun taux, classement ni moyenne par posture** — la posture explique des chiffres, elle ne les corrige pas.
- `2026-09-01` <a id="qualification-textes-portes-689"></a>[Un projet de loi porté au nom du Gouvernement n'est pas une production personnelle (#689)](decisions/qualification-textes-portes-689.md) — `textes_portes[]` publiait sous un unique `role: "auteur"` la **proposition de loi** qu'un·e parlementaire dépose ET le **projet de loi** qu'un membre du Gouvernement porte au nom de l'exécutif : mesuré à `e40d0d32`, **316 des 472 entrées publiées** sont des projets de loi, dont **282 des 283 d'`edouard-philippe`** (toutes déposées pendant qu'il était Premier ministre) et **30 des 34 de `gabriel-attal`**, si bien que 283 et 5 se lisaient sous le même nom sans mesurer la même chose ; le croisement date de dépôt × mandats électifs, qui a servi à découvrir le défaut (**304 / 423** chez les 13 candidats déclarés), est un **contrôle et jamais une source** — il manque les 8 textes qu'Attal a portés comme ministre en étant encore député et le seul projet de loi de `bruno-retailleau`, ministre tout en restant sénateur, et il dirait « hors mandat » là où il faut dire « porté au nom du Gouvernement » ; un discriminant tiré du **libellé** est écarté pour la même raison qu'en #672 — les dossiers de la XVe s'intitulent « Bioéthique », « CETA », « Coopération avec le Luxembourg » et un filtre « commence par *Projet de loi* » en manque **283 sur 304** ; la nature se lit donc dans le champ que le dépôt lisait **déjà** pour les fiches de gouvernement (#435/#400), le préfixe de l'uid du document du premier acte de dépôt, promu en `gouvernement_textes.nature_texte_depose` (`PRJL`/`PION`/`PNRE`) dont `_origine` **dérive** désormais au lieu de relire l'archive (verdict identique sur les 10 674 dossiers des trois archives, vérifié dossier à dossier) ; le pivot gagne **`nature_texte`**, le fait sourcé, et **`role` est scindé** en `initiateur_projet_de_loi` / `auteur_proposition_de_loi` / `auteur_proposition_de_resolution`, parce qu'un champ voisin qu'il faut penser à lire n'empêche personne de confondre les deux natures *par défaut* — `auteur` survit et sa définition **rétrécit** aux 5 entrées d'initiateur dont la source n'établit pas la nature (missions d'information, commissions d'enquête, déclaration du Gouvernement), et `validate_profil` refuse toute **contradiction** entre les deux champs, ce qui rend la redondance sûre au lieu d'en faire une seconde vérité (invariant de `chambre`/`chambres[0]`, #493) ; le rôle est **dérivé, jamais fusionné**, et la nature traverse la fusion additive par `backfill_dossier_nature` — sans lui l'entrée ancienne gagne et le brut n'acquiert **jamais** le champ, le trou de #639 et de #492 au même endroit : un filtre posé avant la fusion additive ne filtre rien ; le cache disque passe en `index_acteur_textes_v3.json`, l'existence d'un index n'étant pas la preuve de sa conformité (#440, #639) ; garde-fou **§5c du portail de qualité, soft** — effectifs par rôle, projets de loi portés au nom du Gouvernement, et compteur d'attente qui vaut **451 / 472 au 01/09/2026** parce que la qualification n'atteint une fiche qu'au run réel qui la recollecte (bloquer interdirait les runs censés la propager, #447/#450), avec sa condition de retrait écrite ; **aucune perte à déclarer** — `nature_texte` est un ajout et un changement de valeur de scalaire n'est pas bloquant pour `audit_diff_profils` ; le repli par intitulé de `web/UI_finale` (`estProjetDeLoi`) survit **déclaré**, derrière `nature_texte`, faute de quoi la page afficherait 0 projet de loi là où elle en signale 13.
- `2026-09-01` <a id="rattachement-au-dossier-interventions-et-scrutins-639"></a>[Rattacher une intervention ou un scrutin à son dossier : les deux volets restants sont écartés, mesure à l'appui (#639)](decisions/rattachement-au-dossier-interventions-et-scrutins-639.md) — instruction, pas implémentation : #639 portait quatre matières, deux sont livrées et revérifiées ici (`textes_portes[]` **472 / 472**, table `textes` des index d'amendements à 6 · 777 · 444 · 565 entrées), les deux autres sont **écartées avec leur condition de réouverture** ; **interventions** — 0 des **652 703** entrées publiées des 481 profils porte un `dossier_id`, et le balayage **exhaustif** des 1 206 comptes rendus Syceron en cache (605 XVIe + 601 XVIIe, jamais un échantillon de 40) rend **0** occurrence de `DLR5L`, `PRJLANR5`, `PIONANR5`, `PNREANR5` ou `ODJ`, la seule référence structurée étant l'uid de la réunion ; la route `seance_ref` **existe** (12 629 `reunionRef` + 10 328 `odjRef` sur les 10 967 dossiers des trois archives, 87,2 % rattachés à un seul dossier) mais son plafond honnête est **631 / 652 703 = 0,097 %** — 8 283 entrées portent une référence de séance (les 636 461 entrées `roster_groupe` n'en portent aucune, `_reduire_au_theme` la retire avec les champs lourds, #657), 7 484 tombent sur une séance qu'un dossier référence, et **une séance ne traite pas un dossier** : 345 des 568 séances citées (60,7 %) portent plusieurs points d'ordre du jour, où siègent **6 742 des 8 283** entrées (81,4 %) ; `bibard` est écarté deux fois — c'est un fragment d'affichage (`' (n[[o]]\xa02915 rectifié)'`, vide ou blanc sur 4 786 / 23 145 points de la XVIe) et le numéro seul n'identifie pas un document (1,9 à 3,1 % des couples *(législature, numéro)* en désignent plusieurs) ; **scrutins** — 0 des 17 748 publiés porte un `texte_lie_id` et les 66 motions de censure ont toutes **le motif déclaré, aucune le lien** ; le lien inverse remesuré rend **706 / 17 748 (3,98 %)** et non 715 ni 760 (710 `voteRef` distincts, 558 dossiers, **0 / 792** en XIVe ; le « 34 676 dossiers » cité était le nombre d'**entrées** des archives, la population étant 10 967), `dossierRef` n'est **pas remesurable hors ligne** (les archives brutes XV-XVII ne sont pas sur disque, `_parse_scrutins_zip` n'en garde qu'une projection — trou déclaré, aucun appel réseau) mais la borne de mars 2026 est **corroborée indirectement** : 2 606 des 17 748 scrutins publiés sont datés du 01/03/2026 ou après, à deux unités des 2 608 mesurés porteurs du champ ; l'union des deux routes sourcées plafonne à **3 234 / 17 748 (18,2 %)** et laisse **14 514 (81,8 %)** sans rattachement, dont **792 / 792 (100 %) de la XIVe**, sur un corpus qui court du 03/07/2012 au 21/07/2026 ; **le coût n'est pas l'obstacle** — +2,42 Mio sur `scrutins.json` (9,75 → 12,17) et +36,3 Mio sur les 481 socles pour 1 312 951 votes, contre un plus gros fichier versionné à 48,1 Mio et des seuils de `garde_fou_blobs` à 50 / 80 —, l'obstacle est éditorial ; **une borne déclarée ne suffit pas** parce que le vide est la règle et non une frange, parce que `LISTES_COUVERTES` indexe cinq **listes** et pas des champs (la borne `votes` « couvert depuis 2012-06-20 » est vraie : ce qui manque est un champ du vote), et parce que le lecteur lit la page d'une loi, pas l'en-tête d'un fichier — la vue « votes groupés par loi » est donc **impraticable par clé sourcée** et n'en a pas besoin, `regrouper-nest-pas-joindre-639` l'ayant déjà servie à 99,4 % par regroupement d'intitulés ; le remède de #639 « un cache sans le champ est refusé » **ne se transpose pas** à `dossier_id`, absent par nature des archives XIV-XVI, où il rejetterait les trois index figés à perpétuité ; conditions de réouverture écrites et conjointes des deux côtés, et trois trous nommés (le champ sur les archives XV-XVII, `vp/reunions/Agenda.json.zip` toujours non vérifié, le rendement d'une recollecte).
- `2026-09-01` <a id="report-texte-vise-source-696"></a>[Le `texte_vise` fautif se reprend depuis l'archive figée, pas par une fusion plus permissive (#696, 01/09/2026)](decisions/report-texte-vise-source-696.md) — **2 500 des 484 132 amendements publiés** de `pivot_data/amendements/` portent dans `texte_vise` un **intitulé** au lieu de l'uid du document AN, tous en XVe législature et pour 5 intitulés distincts (2 458 « Système universel de retraite ») : #639 avait corrigé la collecte, mais l'index est fusionné additivement et `merge_amendements_index` laisse gagner « la nouvelle valeur si elle est renseignée » — un intitulé **est** renseigné, donc l'entrée gardait le sien à chaque reconstruction ; **quatrième occurrence** de la famille nommée par `AGENTS.md` §3a (#492, #639, #641) et **même remède** : un report nommé, jamais une fusion plus permissive ; le cadrage était même optimiste — la fusion ne conserve pas seulement le défaut, elle le **réintroduit** (`an:AMANR5L15PO59051B4857P0D1N000045` est porté avec son uid par **trois** profils bruts et publié avec l'intitulé du quatrième, parce que `jean-luc-melenchon.json` passe après eux dans l'ordre des fichiers), et les 13 399 paires fautives du corpus brut vivent **dans un seul profil**, le sien ; `amendements_index.backfill_texte_vise` relit donc la valeur dans `raw_data/amendements_an_figes/<legislature>/`, **jamais** reconstruite depuis le titre ni appariée par libellé (le préfixe du document — `PRJL`/`PION`/`PNRE`/`RAPP` — n'est pas dans l'uid de l'amendement, le déduire serait l'inventer), strictement monotone, **sans toucher la clé de fusion** (l'élargir est le défaut de #668) et **avant** `resoudre_textes` pour que la réparation gagne son dossier dans le même run ; le critère est la **grammaire** de l'uid AN (`est_uid_texte`), mesurée sur 2 387 valeurs distinctes publiées (2 382 acceptées, 5 refusées = exactement les 5 intitulés) et sur 2 086 valeurs distinctes des trois archives figées (2 086 acceptées, 0 refusée) — le critère « ne contient pas d'espace » de l'issue rend le **même verdict** sur les deux populations, **aucun contre-exemple**, mais la grammaire est plus stricte et « Bioéthique » est un titre de dossier réel ; câblé sur les **deux** chemins d'appel parce que la CI ne passe jamais par `build_amendements_index_pivot.py` (piège de #657) ; ce qu'il ne répare pas est compté et nommé — une législature sans archive figée (la XVIIe, cas **vide** aujourd'hui) et un amendement absent de l'archive (**zéro** : les 2 500 sont toutes réparables) —, la couche brute gardant son intitulé puisque le report se rejoue à chaque construction ; l'archive est lue **par projection** (280 Mio de RSS contre 610 en chargement direct, mesuré) et jamais ouverte pour une législature sans entrée fautive ; le run à venir doit faire passer `jean-luc-melenchon` de **23 dossiers / 2 499 dépôts sans dossier** à **25 / 0**.
- `2026-09-01` <a id="trame-profil-candidat-328"></a>[La trame du profil candidat : l'institution est une colonne, jamais un chapitre (#328)](decisions/trame-profil-candidat-328.md) — quatrième lot de #324 : la fiche des **13 candidats déclarés** (les 468 `roster_groupe` n'en ont pas) passe de quatre KPI et trois onglets à **sept emplacements identiques pour les treize**, dont l'ordre est chronologique et jamais hiérarchique, et où **aucun total n'additionne deux natures d'acte** ; les règles vivent dans `web/UI_finale/src/utils/profilCandidat.js`, qui **consomme** `styleForPosition`/`emptyListMessage`/`sourceBadge`/`isWholeTextVote`/`WHOLE_TEXT_VOTE_BOUND` du lot 1 et de #672 sans en réécrire une ; **l'issue se trompait sur son propre obstacle** — `positionPolitique` EST collectée, sous le nom `position_dans_hemicycle` sur les mandats `groupe_politique`, avec le `source_url` qu'exige §2 règle 6 (mesuré sur les 541 mandats des 13 : 14 `opposition`, 7 `majorite`, 17 `gouvernement`, 503 nuls, `minoritaire` sur aucun), donc la frise porte vraiment la position déclarée, collée au chiffre qu'elle explique (Jérôme Guedj : **24 déposés / 6 adoptés** en majorité XIVe, **1 968 / 67** en opposition XVIe, **437 / 87** en XVIIe sans position publiée — l'Assemblée n'en déclare aucune pour la législature en cours) ; une **seule** bande, une ligne par rôle rangée par date, teinte = institution et motif = position, aucune étiquette dans la bande (des repères numérotés, repliés sur deux niveaux sous 3,4 % d'écart) ; `siegesElectifs` replie les enregistrements en sièges sur leur date de FIN (**5 → 4** chez Guedj, **5 → 3** chez Attal) sans rien supprimer et le déclare ; le bilan de gouvernement sort de « ce qu'il a proposé » — la place contredisait sa propre phrase — et devient une section d'**ensembles** (Attal : Philippe II 282 textes dont 1 par 49.3, Castex 195/0, Borne 111/**6**, Attal 25/0), les 49.3 **hors** de la répartition colorée (§2 règle 4) ; trois régimes de qualité d'orateur au lieu de deux, parce que 35 / 3 933 chez Jean-Luc Mélenchon n'est ni 3 555 / 3 963 ni 0 / 2 702 ; la direction des questions au gouvernement se déclare par ligne (Guedj **0 / 215** ministérielles — il les a posées ; Attal **723 / 743** — il y a répondu, deux conditions conjointes donnant le même 723) ; l'axe des votes devient **continu** avec trois situations (`gouvernement`, `hors_mandat`, `en_mandat`), parce qu'un axe troué efface les années de gouvernement et qu'un `0` nu publierait le taux de présence qu'interdit §2 règle 3 — et la barre Pour/Contre/Abstention d'Attal, que la maquette suspendait, publie ses **150 votes réels sur l'ensemble d'un texte** (140/7/3) À CÔTÉ du fait établi ; les écarts avec le groupe restent **scrutin par scrutin, jamais totalisés**, avec le nombre de scrutins communs publié (Guedj **814 communs, 2 écarts** ; Attal **1** commun — la maquette annonçait 0 — et 0 écart) ; la voix du texte suit `identite.civilite` (9 / 13 renseignées) et n'invente aucun genre quand elle manque ; effet de bord : `chambreLabel(pivot.chambre)` disparaît, **la condition 1 de retrait du scalaire `chambre` est remplie** et `test_condition_1_de_retrait_etat_global` passe en assertion positive comme sa propre docstring l'annonçait ; le faisceau de la maquette est refait par **comptage d'un champ de la source** au lieu d'un thème écrit à la main (§2 règle 8), avec son seuil de concentration publié (90 %, soit 6 / 34 dossiers pour 2 206 / 2 429 amendements chez Guedj) ; **§2 règle 7 et §6 rangent l'écart individu/groupe en interne seulement, la trame le publie** : la contradiction est nommée et laissée à arbitrage, la section étant retirable d'un bloc.
- `2026-09-01` <a id="vivier-de-points-et-empreinte-de-commission-328"></a>[« L'essentiel » : un vivier de points garanti par rôle, et une empreinte de commission sourcée (#328, 01/09/2026)](decisions/vivier-de-points-et-empreinte-de-commission-328.md) — cinq décisions de la propriétaire sur le bloc d'ouverture livré par #694, dont trois changent ce que la page mesure ; le bloc s'appelle **« L'essentiel »** (« Coup d'œil » promettait de la rapidité, pas du contenu) partout sauf dans `READING_LEVELS`, où « coup d'œil » nomme un des **trois niveaux de lecture** de #326 et non cette section ; les deux moitiés de la phrase d'introduction partagent désormais **une seule classe** (19 px / 500) parce que ce sont les deux termes d'une **opposition** — 17 px gris contre 22 px blanc gras faisait lire la première comme la légende de la seconde —, l'accent portant sur les **deux mots opposés** (*réaction* / *initiative*) et jamais sur une ligne, jamais en jaune signal ; les cinq points sont tirés d'un **vivier de sept** avec **au moins un point par rôle réellement tenu** (option C ; A n'empêche pas le volume d'un rôle d'écraser l'autre, B double la longueur de la section — 283 textes portés chez `edouard-philippe`), le rôle se lisant sur un fait collecté — être **membre** d'un gouvernement, **6 des 13 candidats déclarés**, un parlementaire *en mission* n'en étant pas un — et le rôle de chaque point aussi : les **6 651** dépôts comme auteur principal des 13 portent `type_deposant` `depute` ou `commission_rapporteur` et **jamais** `gouvernement`, aucun ne tombant dans une période d'appartenance gouvernementale ; la **réparation principale** est la scission de « textes portés » par `nature_texte` (#689, renseignée sur **418 des 423**), qui faisait lire les 34 textes de `gabriel-attal` comme 34 actes personnels quand 30 sont des projets de loi du gouvernement — et `institutionDuTexte` lit **`role` avant la nature**, parce qu'il est *rapporteur* d'un projet de loi, un acte parlementaire sur un texte du gouvernement ; **trois états, pas deux** : 4 des 423 ne sont attribués à aucune institution ; la garantie **ne fabrique rien** — un rôle tenu que le vivier ne documente pas se DIT (`laurent-wauquiez`, `segolene-royal`, `xavier-bertrand`) — et **ne déplace aucun point sur les 13 profils publiés aujourd'hui**, ce qui est mesuré et écrit plutôt que tu ; le point « amendements » publie un **couple** regroupé par `dossier_id` — « 25 dossiers législatifs amendés · 2 405 amendements déposés sur eux » — parce que le compte brut mesure la participation à un épisode de dépôt en masse (574 des 2 429 dépôts de `jerome-guedj` sur le seul PLFRSS 2023, médiane de 2,5 à 8 dépôts par dossier sur les quatre profils qui en portent plus de 50), que filtrer sur les adoptés mesure le terrain et repose sur un `sort` **inconnu sur 1 822 de ces 2 831**, et que les `texte_vise` sont des **lectures** (47 chez `jerome-guedj` pour **25** dossiers) ; la clé `dossier_id || texte_vise` publiait « 34 dossiers » là où il y en a 25, le défaut `a or b` de #668 ; l'empreinte thématique est la **commission saisie au fond**, lue sur l'acte `AN1-COM-FOND-SAISIE` des archives AN et résolue par le référentiel des organes — **6 024** dossiers, **0** saisie sans `organeRef`, **0** organe non résolu, **0** divergence entre saisies d'un même dossier —, publiée dans le nouveau `pivot_data/commissions_dossiers.json` et **jamais déduite d'un intitulé**, ce qui serait une classification construite ici (§2 règle 1) ; **aucun seuil** ne décide qu'il y a une tendance : `laurent-wauquiez` a 4 et 4 en tête, c'est la donnée qui n'en produit pas, et la barre le montre en ne montrant rien ; **« Dossiers examinés par », jamais « travaille sur »** (« Lois » couvre l'immigration, la justice et les institutions) ; la **borne se déclare** — chez `jean-luc-melenchon` la répartition repose sur 332 de ses 2 831 dépôts, cause #696, déclarée et non corrigée ici ; le rendu **s'adapte au cas** (barre pour les commissions, podium pour les mandats en commission, liste pour les textes, texte pour un vocabulaire ouvert de 417 points d'ordre du jour), **un seul ton** pour les segments à égalité et **aucun jaune signal** dans la section ; non vérifié et nommé : aucun test n'exécute de JavaScript, le rendu SSR réel des 13 profils ne couvre ni mise en page, ni responsive, ni contraste, ni clavier, et la table des commissions n'apparaîtra qu'au premier run de `merge-and-pivot`.
- `2026-08-31` <a id="agregats-publies-controle-perte-649"></a>[Les agrégats publiés entrent dans le contrôle de perte, et l'ordre de grandeur reste hors contrat (#649)](decisions/agregats-publies-controle-perte-649.md) — `amendements_agreges` et `comptages.par_statut` n'étaient regardés par aucune collection d'`audit_diff_profils` : le run `33351244845` a divisé par 5 à 32 le compteur principal de cinq fiches publiées et le commit `3c8e1f0c` est passé sans une ligne ; ils deviennent des **scalaires surveillés** — disparition et passage à `null` bloquent, la baisse de valeur est relevée — et il n'y a **pas** de quatrième catégorie bloquante sur l'ordre de grandeur, parce que la chute juste de `3c8e1f0c` (× 0,03 à × 0,21) est plus forte que la chute défectueuse d'`a125e9e` (× 0,00 à × 0,64) sur chacune des fiches et qu'aucun seuil de ratio ne les sépare ; rejeu sur les 65 transitions committées : 4 constats nouveaux, tous des pertes réelles, zéro faux positif.
- `2026-08-31` <a id="amendements-distincts-et-signatures-643"></a>[Un amendement cosigné n'est pas N amendements : deux grandeurs, deux noms (#643)](decisions/amendements-distincts-et-signatures-643.md) — `_aggregate_amendements` comptait une entrée de profil par signataire et la publiait sous le mot « amendements » (92,2 % des entrées du corpus sont des cosignatures, d'où « 2 600 765 amendements déposés » pour les 76 députés d'`AN:LFI`) ; `amendements_agreges` compte désormais les amendements **distincts** et les signatures vivent sous leur nom, le facteur allant de × 5,0 à × 31,7 selon la fiche et le taux d'adoption bougeant dans les deux sens (`AN:SOC` 7,24 % → 14,54 %, `AN:LFI` 5,01 % → 2,99 % sur le seau `depute`) ; la déduplication passe par un cumul partagé par fiche — 132 960 amendements retenus au lieu de 2 647 601 signatures — et deux bandes de sort de plus rendent les six bandes sommantes, dont les 33,3 % de sorts absents qu'un décompte muet publierait comme des zéros.
- `2026-08-31` <a id="civilite-et-pcs-insee-659"></a>[La civilité et la nomenclature PCS de l'INSEE traversaient le pipeline sans y laisser de trace (#659)](decisions/civilite-et-pcs-insee-659.md) — `etatCivil.ident.civ` était rangée dans l'index d'identité depuis #556 et s'arrêtait là, `profession.socProcINSEE` n'était pas lue du tout : re-mesuré sur les 3 117 fiches AMO30, la civilité est renseignée **3 117/3 117** (M. 2 106, Mme 1 011) et les deux niveaux de la nomenclature PCS de l'INSEE **2 177/3 117**, toujours renseignés ou absents **ensemble** ; les trois champs atteignent `identite` (clés facultatives, comme `identifiants` et `provenance_champs`), publiés **verbatim** parce que c'est la source qui classe — une catégorisation construite ici serait un acte éditorial (§2 règle 1) —, avec « non classé » (940 fiches au marqueur `xsi:nil`) tenu distinct de la famille « Sans profession déclarée » (85 fiches), le contresens exact de #556 ; le regroupement des variantes typographiques est laissé à qui agrège, purement typographique et jamais sémantique ; `NOM_INDEX_IDENTITE` passe en `v4` sans quoi le cache restauré rendrait l'index d'avant, et `validate_profil()` refuse désormais un non-`str` sur `CHAMPS_IDENTITE_TEXTE_LIBRE`, seul contrôle capable de voir le marqueur là où `audit_diff_profils` ne compare que la présence du bloc (#649).
- `2026-08-31` <a id="cle-fusion-textes-portes-668"></a>[Une clé de fusion en `a or b` change d'identité quand `a` se remplit (#668)](decisions/cle-fusion-textes-portes-668.md) — le rang 2 de #639 a réparé `source_url`, `null` sur 472 / 472 textes portés, et la clé `source_url or (titre, date_min, legislature)` a basculé de branche sous les entrées déjà publiées : 940 entrées pour 472 dossiers réellement collectés, **468 doublons** en ligne sur 22 profils (l'issue en comptait 471, écart re-mesuré — 3 sont des dossiers distincts partageant un titre, `ludovic-mendes` portant la proposition de loi `DLR5L17N51626` **et** son rapport `DLR5L17N51485`) ; la clé passe sur `dossier_id`, l'identifiant AN publié depuis #639 et déjà porté au brut par `dossiers_legislatifs[].id`, `source_url` en sort (0 repli portant deux `dossier_id` distincts, et 4 entrées identifiées n'ont pas d'URL) ; le `or` subsiste pour les entrées sans identifiant — le supprimer les effondrerait sur une clé `None` (#432) — et ce qui neutralise la bascule est la **reprise**, `clean_stale_textes_portes`, qui existait sous ce nom pour ce motif mais départageait sur un critère de schéma que les deux versions satisfont **et n'était appelée nulle part** : 940 → 472, exactement les 472 dossiers collectés, 468 retirées à déclarer par `allow_declared_losses` ; motif cherché ailleurs et **mesuré** — `_pivot_amendement_key` (6 091 732), `_pivot_vote_key` (1 312 951) et `_pivot_intervention_key` (16 242) sont à 100 % sur leur branche principale, donc aucune ne peut basculer, ce que la mesure constate sans le garantir ; l'extension d'`audit_diff_profils` aux **apparitions** est nommée et laissée hors lot (périmètre #649).
- `2026-08-31` <a id="collecte-interventions-reduite-au-theme-657"></a>[La collecte d'interventions des membres de roster est réduite au thème (#657)](decisions/collecte-interventions-reduite-au-theme-657.md) — l'« empreinte thématique » d'une fiche de groupe était celle d'UNE personne (470 étiquettes sur `AN:RN` portées par Marine Le Pen, 382 sur `AN:SOC` par Jérôme Guedj, 0 sur `AN:LFI` et `AN:LR`), parce que `tags_thematiques` dérive intégralement d'`interventions[]` et que les 468 membres de roster publiaient `interventions: []` — le motif écrit de #357, « aucun agrégat ne les consomme », était faux ; `--interventions-theme-seul` collecte les débats **sans leur verbatim** et laisse les questions officielles (qui ne portent aucun thème), chaque entrée publiée déclarant sa forme par `collecte: "theme_seul"` — les champs absents le sont, jamais publiés à `null`, un `"texte": null` étant un fait sur la personne là où le fait porte sur le run ; mesuré sur les 468 membres, législatures 16+17 : **139,0 Mio publiés au lieu de 413** (383 o/entrée contre 1 138), index de cache **194,2 Mio au lieu de 798**, parcours **38,9 s au lieu de ~93**, et **aucune étiquette perdue** — `sujet` vient du titre de point, les deux modes indexant 287 789 et 305 862 entrées au nombre près ; l'empreinte de `AN:LFI` passe de 0 à 1 981 étiquettes portées par 76 membres sur 76, celle d'`AN:REN` de 197 (1 porteur) à 3 943 (192 sur 193) ; six garde-fous, dont l'index réduit dans un répertoire DISTINCT (#447 transposé au contenu d'une entrée), l'exemption des candidats déclarés (la fusion additive gèlerait leur forme complète) et le repli par provenance de `couverture_profil` qui ne s'applique plus qu'aux 19 profils sans `meta.collecte_ecartee` ; la réduction des 16 242 entrées déjà publiées est **mesurée et non faite** (21,3 → 5,5 Mio) parce que 665 de leurs 1 331 étiquettes viennent exclusivement du repli `mots_cles` ; la 15e législature n'étant pas mesurable, tous ces volumes sont un plancher (204 des 468 membres y ont voté).
- `2026-08-31` <a id="couleurs-de-vote-sans-echelle-326"></a>[Les couleurs de vote ne forment pas une échelle, et « Absent » n'est pas une catégorie (#326)](decisions/couleurs-de-vote-sans-echelle-326.md) — premier lot d'implémentation de #324, trois arbitrages rendus sur le **rendu d'écran soumis avant toute ligne de code** (§11) ; mesuré au commit de données `245511b4`, les **1 312 951** positions publiées des 481 profils ne prennent que **quatre** valeurs — `contre` 633 551, `pour` 584 692, `abstention` 73 479, `non_votant` 21 229 — et **`absent` n'apparaît jamais**, si bien que les cinq catégories annoncées par l'issue en auraient inventé une : la publier reviendrait à publier une absence comme un fait de vote, c'est-à-dire le taux de présence individuel qu'interdit §2 règle 3 ; `non_votant`, qui n'était PAS dans `VOTE_STYLE`, résolvait sur `undefined` et affichait une pastille sans fond et un libellé vide — une fois sur 8 860 chez les 13 candidats déclarés, donc invisible là, réel sur les fiches de groupe — et se distingue désormais par la **forme** (contour tireté, `color: null`) et jamais par une teinte, le gris neutre étant écarté parce qu'il **est** celui de l'abstention et toute autre teinte reconstruisant l'échelle chaud-froid qu'interdit §2 règle 1 ; le badge dit « Lien de source non publié » et jamais « non vérifié », parce que le premier parle de nous quand le second ferait porter le doute sur les 484 132 amendements, tous issus de l'open data (couverture des liens : scrutins 17 748/17 748, interventions des 13 candidats 16 242/16 242, textes de gouvernement 725/725, textes portés 472/472, mandats 1 915/41 723, amendements 0/484 132) ; `VOTE_STYLE` et `OUTCOME_COLOR` cessent d'être dupliqués dans `CandidateProfile.jsx` et `GroupProfile.jsx` — la « divergence silencieuse » que le `DESIGN_SYSTEM` §2 signalait lui-même — au profit de `src/utils/lecture.js`, qui porte aussi les cinq autres primitives du lot ; faute de runner JS dans le dépôt (`oxlint` seul), les trois arbitrages sont verrouillés par un test Python lisant le **code exécuté**, commentaires retirés, patron de #529 et #530 — ajouter un runner JS est nommé et laissé hors lot.
- `2026-08-31` <a id="date-de-reference-des-comptes-de-groupe-653"></a>[Tous les comptes d'une fiche de groupe se rapportent à une date, et elle est publiée (#653)](decisions/date-de-reference-des-comptes-de-groupe-653.md) — aucune des 7 fiches ne décrit la législature en cours, or trois compteurs disaient « aujourd'hui » et mesuraient en fait la **carrière ultérieure** des membres : `effectif.actuel` égalait exactement le nombre de membres portant un mandat électif ouvert (38/38, 85/85, 60/60 sur `LR`, `REN`, `LFI`) et `nb_membres_actifs` comptait leur commission **d'aujourd'hui** ; une `date_reference` dérivée — clôture de la période si toutes les appartenances sont refermées, génération sinon — est **publiée** dans la fiche et les trois compteurs s'y rapportent, renommés pour le dire (`a_la_date_de_reference`, `present_a_la_date_de_reference`) ; les valeurs annoncées à l'arbitrage **ne se reproduisent pas** (REN 85 → 169), et deux contrôles indépendants établissent les nouvelles — égalité avec les mandats de groupe clos le jour de la dissolution, et la contrainte « une seule commission permanente » (#656) retrouvée à l'unité près sur 4 fiches sur 5 ; sélectionner le mandat **ouvert à cette date** est ce qui rend le compteur juste (sans quoi les affaires sociales LFI tombent de 9 à 3), le cumul et le nombre d'entrées restant identiques au bit près.
- `2026-08-31` <a id="dates-appartenance-groupe-653"></a>[`debut_dans_groupe` se lit sur le mandat de groupe, plus sur le premier mandat électif (#653)](decisions/dates-appartenance-groupe-653.md) — le champ mesurait le début du **premier mandat électif**, approximation devenue fausse dans l'autre sens depuis #647 (Vincent Rolland publié « LR depuis 2002-06-19 » alors qu'il entre dans le groupe LR de la XVIe le 2022-06-29) ; les dates viennent désormais du **mandat de groupe politique de la législature de la fiche** que le roster AMO30 rend déjà — transit écarté, organes successifs recollés (#526) —, ce qui corrige **451 des 452 dates d'entrée** des 5 fiches AN de la XVIe, dont 8 de 20 ans, sans retirer une seule entrée `membres[]` ni laisser un seul membre sans mandat de groupe identifiable ; sans roster les dates sortent `null` et comptées, **jamais repliées sur le mandat électif** (§2 règle 5) ; `effectif.actuel` est nommé pour ce qu'il compte — les membres sans fin d'appartenance **dans cette législature**, donc 0 sur une législature close — et le roster mono-législature est instruit sans être élargi, faute d'un arbitrage sur la continuité `REN`/`EPR` et sur des dénominateurs de cohésion composites.
- `2026-08-31` <a id="destinataire-avertissements-642"></a>[`meta.warnings[]` déclare son destinataire, dans un jumeau typé et aligné (#642)](decisions/destinataire-avertissements-642.md) — 52 des 481 profils publiés portent 115 avertissements dont **80 internes et 35 lisibles** (re-mesuré le 31/08/2026), sans rien qui les partitionne : l'interface ne publiait donc aucun des deux registres, et la page de `bruno-retailleau` restait sans explication alors qu'elle est écrite dans le fichier ; le destinataire est désormais déclaré **par avertissement, à l'endroit qui l'écrit** — vocabulaire fermé à deux valeurs, obligatoire, validé — dans `meta.avertissements[]`, jumeau typé et **aligné entrée par entrée** sur `meta.warnings[]`, ce qui rend une disparition impossible là où `audit_diff_profils` ne surveille rien ; une table indexée sur le préfixe est écartée parce que « votes introuvables » couvre à la fois un constat et une panne (#484 à l'identique), le constat ParlTrack **s'écrit deux fois** faute de destinataire « mixte », et les 19 avertissements `synchro_sources.nosdeputes` qu'aucun code n'écrit plus sont typés par une table fermée indexée sur le message entier.
- `2026-08-31` <a id="dossier-des-amendements-639"></a>[Un amendement retrouve son dossier, et la clé qu'on lui avait retirée (#639, rang 3)](decisions/dossier-des-amendements-639.md) — `fetch_amendements_officiels` écrivait `record["texte_vise"] = titre` **avant** l'écriture du profil brut : 293 582 des 484 132 amendements publiés (60,6 %) n'ont plus qu'un libellé, et les 190 550 qui ont gardé leur uid de document AN ne le doivent qu'aux trous de l'index de titres (59 263 pour la seule XIVe) ; la brique qui joint le texte au dossier — `json/document/*.json`, décrit « sans rapport, à filtrer » depuis le spike #207 — porte `dossierRef` sur **21 936 de ses 21 937 uid**, avec **0 divergence** entre les trois archives, et le rattachement se fait donc d'uid à uid, jamais par libellé même à l'égalité stricte (§2 règle 2) ; le `dossier_id` vit **une fois par texte** dans une table de fichier — 484 132 amendements ne visent que 2 248 textes distincts — ce qui coûte **+0,10 Mio mesuré après écriture** sur les quatre index (+0,04 sur `15.json`) contre +5,7 Mio pour un champ par amendement ; couverture immédiate **130 244 / 484 132 (26,9 %)** sans aucune recollecte et 99,4 % des codes présents sur XV-XVII, les 353 888 restants comptés et déclarés (291 177 en attente de recollecte, 59 236 de la XIVe dont aucune archive de dossiers n'est ingérée) ; aucun index figé n'est refusé, contrairement au rang 1 — les trois portent déjà le code.
- `2026-08-31` <a id="filtre-publication-apres-fusion-641"></a>[Un filtre de publication posé avant la fusion ne filtre rien (#641, réouverture)](decisions/filtre-publication-apres-fusion-641.md) — le lot #641 avait bien vu que « la fusion ne fait jamais régresser un scalaire vers `null` » et en avait tiré un second filtre, mais posé sur le bloc que la **normalisation produit** et non sur celui qui part au fichier : le run `33395056902` a donc nettoyé les 3 professions à préfixe parasite — valeur neuve renseignée, elle gagne — et republié les **5** libellés du code 85 — valeur neuve `None`, `_composer_identite` rend la main à l'ancienne (#601) ; `filtrer_identite_publiee` s'applique désormais **après** la composition, sur les deux chemins de `merge_pivot_profile`, via une table nommée champ par champ (`FILTRES_PUBLICATION_IDENTITE`) parce qu'`identite` porte cinq libellés recopiés d'AMO30 et qu'un filtre général inventerait la sémantique des quatre autres ; effet re-mesuré sur les 481 profils publiés — **5** passent à `null`, 472 inchangés, 4 sans le champ —, la provenance du champ disparaît avec la valeur (#603), et **aucun `allow_declared_losses` n'est requis** : `audit_diff_profils` réduit `identite` à `"<renseigné>"` et ne compare que la présence du bloc, qui reste présent ; filtrer le bloc ancien **avant** la composition est écarté — cela peut faire basculer `bloc_sans_fond` et désarmer la réserve de #597 sur `nom_complet`/`groupe_nom` ; les 3 réussites et les 5 échecs passaient tous la suite, le test manquant portait sur la transition, pas sur les étapes.
- `2026-08-31` <a id="libelle-chef-du-gouvernement-au-feminin-658"></a>[Le libellé d'organe du chef du gouvernement s'accorde en genre, la qualité jamais (#658)](decisions/libelle-chef-du-gouvernement-au-feminin-658.md) — `/gouvernements/BORNE` affichait « Non renseigné » alors qu'Élisabeth Borne figure dans ses propres `membres[]` : son mandat porte `label = "Première ministre"` (libellé de l'organe `PO791580`, accordé) et `fonction = "Premier ministre"` (qualité, jamais accordée), et le libellé était comparé par égalité stricte, sans même la normalisation typographique appliquée à la qualité trois lignes plus bas ; le balayage des **1 162 mandats `MINISTERE` de l'archive AMO30** (3 117 fiches acteur) tranche la question ouverte — `libQualite` n'a **que 9 valeurs, toutes au masculin**, donc une ministre déléguée est bien reconnue et `_normalise_fonction` n'a aucune raison d'être relâchée — tandis que le genre se joue sur le libellé d'organe, d'où une liste fermée relue et datée (`LABELS_PORTEFEUILLE_PREMIER_MINISTRE_OBSERVES`) plutôt qu'une règle de genre ; le double verrou de #474 reste entier et n'est pas théorique au féminin (`yannick-chenevard`, publié, porte « Première ministre » / `en mission`), 0 membre publié manqué côté qualité (260 mandats, toutes classées) ; la seule qualité restée non classée, « Haut-commissaire », est ajoutée à la liste blanche sur un motif qui est une **lecture du référentiel et non une appréciation institutionnelle** — ses 4 mandats (2 personnes) sont rangés `typeOrgane == "MINISTERE"` sous des organes de `codeType == "MINISTERE"`, celui de Jean-Paul Delevoye étant intitulé « Ministère des solidarités et de la santé – Retraites » — pour un delta mesuré de **0 mandat, 0 personne et 0 fiche** (ni Hirsch ni Delevoye n'a de profil publié : c'est un pré-positionnement verrouillé par un test) ; `premier_ministre` passe de 3/10 à **4/10** fiches, `BORNE` seule bougeant.
- `2026-08-31` <a id="mandats-agreges-siege-vs-passe-656"></a>[`mandats_agreges` : « qui y siège » et « qui y est passé » sont deux nombres, pas un (#656)](decisions/mandats-agreges-siege-vs-passe-656.md) — `nb_membres` additionnait les passages d'un jour (43 % des 2 708 adhésions de commission publiées, un⋅e député⋅e ne siégeant qu'en une commission permanente à la fois : AMO30 écrit toute bascule temporaire comme un mandat à part entière) : deux compteurs nommés, un dénominateur publié, un tri sur « qui y siège ».
- `2026-08-31` <a id="mandats-electifs-liste-complete-640"></a>[Un profil publie tous ses mandats de député, et le compteur devient un témoin de couverture (#640)](decisions/mandats-electifs-liste-complete-640.md) — le mandat électif était reconstruit depuis le couple unique `mandat_debut`/`mandat_fin` d'`identite_an` alors que le même objet portait `nb_mandats` (379 des 457 profils concernés publiaient moins que leur propre compteur, 612 mandats manquants) ; AMO30 porte bien l'historique (3 954 mandats ASSEMBLEE pour 3 117 acteurs, plus ancien 2002-06-19), regroupé sur `(legislature, dateDebut)` — jamais sur la seule législature, qui unirait les deux mandats de Bertrand Petit séparés par une élection annulée — pour 613 périodes nouvelles sur 393 profils, 26 écarts résiduels nommés et 18 doublons hérités que la fusion additive ne peut pas retirer.
- `2026-08-31` <a id="profession-code-nomenclature-641"></a>[Un code de nomenclature n'est pas une profession, et « sans activité professionnelle » n'en est pas une (#641)](decisions/profession-code-nomenclature-641.md) — 8 des 457 profils qui renseignent `identite.profession` publiaient un code brut `(nn) - ` ; le préfixe est retiré (3 profils) et l'énoncé d'une absence passe à `null` (5 profils), sur un critère en deux parties — la famille du code 8x **et** le libellé de la source — parce que la famille seule nullerait « Elève, étudiant » ; le filtre existe aux deux étages parce que la fusion ne fait jamais régresser un scalaire vers `null`, donc la collecte corrigée seule laisserait les cinq libellés en place indéfiniment.
- `2026-08-31` <a id="profil-de-groupe-lecture-329"></a>[La fiche de groupe se lit en nombres datés, jamais en barres ni en compteurs nus (#329)](decisions/profil-de-groupe-lecture-329.md) — troisième lot d'implémentation de #324, posé sur les six fondations du lot 1 (#326) : les règles propres au groupe vivent dans `web/UI_finale/src/utils/groupe.js`, qui **consomme** `ratio`/`truncation`/`emptyListMessage`/`sourceBadge`/`styleForPosition` et n'en réécrit aucune ; la **barre de cohérence** (`.gp-coherence-track`/`.gp-coherence-fill`) est **retirée** au profit des six décomptes, parce qu'une barre place des catégories sur une échelle du pire au meilleur (§2 règle 1) et que la partition est exacte — `pour + contre + abstention + non_votant + absents + excuses == membres_eligibles` sur **19 832 / 19 832** entrées des 5 fiches AN, `taux_participation` retrouvé 19 832 / 19 832 et `taux_coherence` 19 687 / 19 832, les 145 restantes étant celles à `position_majoritaire: null` et 0 vote exprimé ; `absents` s'affiche **« Sans trace de vote »** parce que le pipeline y compte les éligibles pour qui AUCUN vote n'a été trouvé — une absence de donnée, pas une absence constatée, et « absents » publierait le taux de présence qu'interdit §2 règle 3 ; `excuses` n'est **pas publié**, valant 0 sur les 19 832 entrées faute de position `excuse` dans le corpus (le cas d'`absent` dans les 1 312 951 positions de #326), et la décision est prise à l'échelle de la **fiche**, jamais de l'entrée ; le KPI « **Effectif actuel** » disait faux deux fois — il affichait `roster_total` (76 sur `AN:LFI`) et se disait « actuel » sur une législature close — et devient « Membres du groupe au 09/06/2024 », 75 sur 76, avec l'origine de `date_reference` publiée ; **les deux jeux de noms sont lus** (#653/#656 et leurs anciens noms), sans quoi les 17 cartes de mandats des 2 fiches Sénat gelées rendaient `undefined membre y a siégé au moins une fois` et « Aucun membre n'y siégeait » sur une donnée absente (§2 règle 5) — « Sénat · Législature null » corrigé au passage ; les `slice(0, 12)` et `slice(0, 20)` déclarent enfin leur dénominateur (3 832 à 4 099 scrutins, 1 554 à 4 303 étiquettes) et leur règle, **vérifiée** : `cohesion_votes` est trié par date décroissante sur les 5 fiches, contrôlé après jointure sur `pivot_data/scrutins.json` (19 832 / 19 832 résolues, 19 832 / 19 832 avec `source_url`) ; `meta.couverture_roster.etat`/`preuve`, publiés sur 7/7 fiches et lus par personne, ouvrent une section « Vérification » — 15 profils sur 235 sur `groupe-Senat-LR` est un **périmètre**, pas une perte, et `hors_perimetre` est traduit en `non_collecte` et non `hors_couverture`, parce que ce vide vient de NOTRE décision et pas d'une période que la source ne publierait pas ; l'**empreinte thématique** passe de la case D à la case A sur décision de la propriétaire, le motif de son report (« 0 / 468 membres portent une intervention ou un tag ») étant périmé par #657 — re-mesuré à `c6edee05` : **448 / 468** profils `roster_groupe` portent une étiquette, les 5 fiches AN en publient 1 554 à 4 303 avec `nb_membres_porteurs` jusqu'à **99**, et chaque étiquette part avec son porteur sur `len(membres)` (jamais `roster_total`), sous une phrase qui rappelle que ce sont des sujets d'intervention et non des positions (§2 règle 8) ; la fiche déclare enfin ce qu'elle ne publie pas — elle ne nomme jamais qui s'est écarté de la ligne, l'écart individu/groupe restant un contrôle interne ; 22 cas Python lisant le **code exécuté** (commentaires retirés) verrouillent le tout, faute de runner JS ; `meta.avertissements[]` absent des fiches de groupe, `mandats_agreges` non tronqué (535 à 1 153 cartes) et la 6ᵉ vue (#672) sont nommés et laissés hors lot.
- `2026-08-31` <a id="qualification-perdue-a-la-fusion-639"></a>[La qualification d'un scrutin se perdait entre la collecte et le profil brut (#639, rang 1)](decisions/qualification-perdue-a-la-fusion-639.md) — le rang 1 avait traité les deux bouts qu'il possédait, parseur d'archives et index figés (qui portent bien la qualification : 1 354, 4 417 et 4 105 scrutins pour les XIVe, XVe et XVIe, dont 4, 5 et **34** motions de censure), et le run `33395056902` a pourtant publié `type_scrutin: null` et `demandeur: null` sur les **17 748** scrutins et `type_vote: "vote_texte"` sur les 17 748 — les 66 motions de censure invisibles dans la seule couche que `web/` lit ; deux maillons, tous deux dans une transition — `merge_raw_profile` fusionne `votes[]` en additif pur, l'entrée ancienne gagne et sa clé `(numero_scrutin, date)` ne contient aucun champ neuf, donc les 1 016 / 1 016 votes bruts de `jean-luc-melenchon` restaient à sept clés run après run (le trou exact de #492 sur `mandats[].chambre`, documenté à l'époque comme une particularité de la chambre) ; et `_valeur_scrutin` repliait `type_vote` sur « vote_texte », ce que `construire_index` ne complète jamais ensuite, donc la première occurrence non qualifiée d'un scrutin verrouillait un fait faux (§2 règle 4) ; `backfill_vote_qualification` reporte les trois champs nommés **sans toucher la clé** — vérifié, 1 016 clés identiques avant/après, 1 016 entrées après fusion, 1 016 mappings sur la branche principale de `_pivot_vote_key` —, le défaut `vote_texte` disparaît de l'index et de `scrutin_non_resolu`, et `merge_scrutins_index` conserve la valeur publiée plutôt que de régresser ; rejeu sur données réelles : 0 → 1 016 `type_scrutin`, 0 → 1 010 `demandeur`, 0 → **5** motions pour la XVe, 17 748 scrutins inchangés, 0 erreur de validation ; la couverture se remplit profil par profil et non d'un coup — lire les index figés directement est écarté, l'index pivot ne doit porter que des scrutins qu'un profil référence et la XVIIe n'a pas d'index figé ; la suite était verte des deux côtés, le bout en bout s'arrêtant au profil brut écrit pour la PREMIÈRE fois.
- `2026-08-31` <a id="qualification-scrutins-et-cle-dossier-639"></a>[La qualification d'un scrutin et la clé de son dossier étaient lues puis jetées (#639, rangs 1 et 2)](decisions/qualification-scrutins-et-cle-dossier-639.md) — `typeVote` est renseigné sur 18 311 / 18 311 scrutins bruts et `dossiers_legislatifs[].id` sur 472 / 472 entrées, tous deux jetés à la projection : les 66 motions de censure étaient publiées `vote_texte` comme les 17 682 autres, et l'invariant §5 qui exige leur `texte_lie_id` était vacuement satisfait ; il devient exigible et n'est pas satisfiable (0 / 18 311 référence législative sur un scrutin AN), donc la clé absente est **déclarée** (`texte_lie_non_resolu.motif`) plutôt qu'inventée ou tue, et un cache de scrutins non qualifié — index figé committé compris — est refusé au lieu d'être relu.
- `2026-08-31` <a id="regrouper-nest-pas-joindre-639"></a>[Regrouper des faits d'une même source n'est pas les joindre entre sources (#639, #594)](decisions/regrouper-nest-pas-joindre-639.md) — le lien vote → loi existe et se lit (99,4 % des 17 748 scrutins portent un intitulé de texte, pour 1 229 textes distincts) ; ce qui manque est un identifiant, et la distinction sépare deux gestes — regrouper des chaînes d'une même source est permis, décréter qu'un scrutin et un amendement visent le même dossier est une affirmation qu'aucune source ne fait ; corollaire, le rattachement des scrutins attend que `scrutins.json` sache porter une borne datée, l'AN ne renseignant son `dossierRef` que depuis mars 2026 (0 avant, 100 % après, sur les 8 434 scrutins de la XVIIe).
- `2026-08-31` <a id="selection-vote-sur-ensemble-672"></a>[Un vote « sur l'ensemble d'un texte » se reconnaît à un motif ancré, jamais à une sous-chaîne (#672)](decisions/selection-vote-sur-ensemble-672.md) — deuxième lot de #324, et l'étape 1 de l'issue est levée : `type_scrutin` est publié sur **17 748 / 17 748** scrutins sans un seul `null` (`public_ordinaire` 17 312, `solennel` 361, `motion_censure` 66, `tribune` 9, commit de données `c6edee05`) ; la sélection existante cherchait `l'ensemble` **en sous-chaîne** et était fausse dans les deux sens — 938 retenus, dont 5 vrais faux positifs (1 motion de rejet préalable, 1 article unique, 2 amendements, 1 article premier), et 22 scrutins manqués parce que l'apostrophe **typographique** n'apparaît que dans les législatures 16 (343) et 17 (541), les deux plus **récentes** ; deux corrections à l'énoncé : l'ancrage strict qu'il prescrit écarte `an:14:32`, seul scrutin des 17 748 dont l'intitulé commence par « **sur** l'ensemble » et vrai vote **solennel** sur l'ensemble — d'où `^(sur )?l'ensemble\b`, 933 —, et l'ancrage ne suffit pas puisque **8** des 933 portent sur une **sous-partie** (5 articles, 3 parties de budget dont un solennel), que l'issue n'avait pas relevés et qui reproduiraient exactement le contresens qu'elle ferme (§2 règle 2) : **925 retenus** ; la règle est écrite **une seule fois**, dans `web/UI_finale/src/utils/lecture.js`, en deux moitiés de nature différente — `type_vote` **sourcé** (#639), qui écarte les 66 motions de censure et ne rouille pas (§2 règle 4), et le motif sur le libellé, **approché**, qui rouille et se déclare ; 925 est donc un **plancher** que `MethodologyPage.jsx` publie à la place de l'affirmation d'exhaustivité que `AGENTS.md` §5 signalait lui-même comme non soutenue (17 312 scrutins portent `SPO`, qui ne sépare pas l'ensemble de l'article, et **70 des 361 solennels** ne sont pas retenus faute de la locution) ; faute de runner JS, le verrou est un test Python qui lit le code exécuté **et** exécute les littéraux d'expression régulière extraits du JS sur des libellés recopiés verbatim — le rejeu est **dérivé** de la source et non réécrit à côté d'elle, deux pièges ayant été trouvés par mutation (`re.match` ancre implicitement là où `RegExp.test` non, ce qui laissait passer un motif redevenu sous-chaîne ; une normalisation recopiée survivait à sa suppression dans le module) ; cinq mutations sont vérifiées échouantes ; `normalizeLabel` n'est appliquée qu'à cette sélection, l'étape 3 de l'issue — « partout où un libellé est comparé » — restant à moitié honorée, et les cinq vues consommatrices restent à écrire.
- `2026-08-31` <a id="sparse-checkout-extract-an-674"></a>[Un shard d'extraction ne matérialise que son propre profil (#674)](decisions/sparse-checkout-extract-an-674.md) — le run `33404236969` a tué ses **13 shards `extract-an`** l'un après l'autre à 5 min 00 **dans `actions/checkout`**, l'étape d'extraction restant `skipped` : aucun profil écrit, 40 minutes pour rien, le défaut de #498 ; ce n'était pas le réseau — le checkout valait **4 min 52 à 6 min 03 dans tous les jobs du run**, `extract-an` étant seulement le seul à `timeout-minutes: 5` — mais le poids de l'arbre, **8 483 Mio dont 7 525 pour le seul `raw_data/profiles/`** (1 503 fichiers), là où le garde-fou de #580 surveille le plus gros FICHIER et rien du total ; le checkout passe donc en liste blanche avec `filter: blob:none` (patron de `tests.yml`) et ne matérialise que le code, les référentiels, les index figés et **le seul profil du shard** — socle et tranches (#580), 16,4 Mio au pire contre 7 525 —, ce que légitime le fait que `--only` filtre sur `raw_data/candidats.json` et que `slugs_du_repertoire` ne soit appelée que par des scripts de migration et d'audit ; `timeout-minutes` reste à 5, un cas de test échouant si on le relève, parce que relever le plafond ne supprime pas la cause, coûte les ~65 min de checkout des 13 shards en série et tombe sous l'interdit de §3b ; le garde-fou échoue **localement** sur la fermeture des imports du point d'entrée — la prose et les docstrings écartées (le motif attrapait des phrases), le chemin borné au premier caractère non-chemin (des messages nomment un fichier puis continuent la phrase), et un seul bloc `sparse-checkout: |` toléré puisque le parseur rend le premier — parce qu'ici un référentiel absent ne lève pas toujours mais **se replie** (#525) et publie moins sans rien casser ; `extract-roster-groupes` et `merge-and-pivot` paient le même checkout sans en mourir, et le total du dépôt reste sans surveillance : nommés, hors lot.
- `2026-08-30` <a id="audit-599-projection-blocs-lus-628"></a>[Un audit lit le corpus par projection, et son plafond de mémoire est dans un test (#628, 2026-08-30)](decisions/audit-599-projection-blocs-lus-628.md) — l'audit de #599, livré « rejouable », rangeait chaque profil entier dans un dict par slug (~3,9 Gio de pic, `exit 137` sur une machine à 4 Gio et swap saturé) alors que ses mesures ne lisent que `identite`, `identifiants`, `meta` et le cardinal des listes — 0,39 Mo sur 681,6 ; pic ramené à 113 Mio, rapport prouvé identique sur quatre quarts disjoints du corpus, et le plafond est désormais déduit dans le test du poids sur disque des blocs à relâcher.
- `2026-08-30` <a id="bloc-sans-fond-484"></a>[Un bloc structuré sans fond n'écrase plus un bloc collecté (#484)](decisions/bloc-sans-fond-484.md) — un squelette d'identité à 8 clés dont 7 nulles n'est pas vide, donc il gagnait ; la fusion demande désormais si un bloc a du fond.
- `2026-08-30` <a id="corroboration-chambres-publiees-486"></a>[La corroboration porte sur les chambres publiées, pas sur la complétude des mandats — et la condition de retrait de `chambre` devient atteignable (#486)](decisions/corroboration-chambres-publiees-486.md) — 30 des 31 occurrences publiées du warning énonçaient un problème que leur propre phrase déniait ; le prédicat cesse de gager le retrait d'un champ de niveau profil sur une complétude de niveau mandat que la fusion additive ne peut pas atteindre (29 des 511 mandats électifs publiés ne sont plus rendus par la source), et la condition de retrait de `chambre` se réduit aux 20 profils que seule une collecte sénatoriale débloque.
- `2026-08-30` <a id="couverture-remplacee-par-liste-602"></a>[La couverture se remplace à la maille où #539 la publie, et un cas non tranchable se déclare (#602)](decisions/couverture-remplacee-par-liste-602.md) — le remplacement descend du bloc à la liste métier (2 858 entrées sur 3 800 étaient perdues face à un écrivain partiel, sur les 481 profils publiés) ; à date et rang d'interrogation égaux, la divergence se déclare au lieu de se choisir sur l'ordre des jobs.
- `2026-08-30` <a id="donnees-versionnees-integrite"></a>[Pourquoi les données vivent dans git : l'intégrité de ce que le site montre](decisions/donnees-versionnees-integrite.md) — la propriété protégée est que toute modification de ce que le site montre soit un événement public, attribué et daté ; `raw_data/` est une archive, pas un cache (38 mandats irrécupérables mesurés sur #484).
- `2026-08-30` <a id="hook-diagnostic-sparse-checkout"></a>[Diagnostiquer le piège du sparse-checkout plutôt que le prévenir (#619, 30/08/2026)](decisions/hook-diagnostic-sparse-checkout.md) — la liste blanche vaut 8,4 Go non téléchargés et 41 s au lieu de 4 min 30 ; le piège qu'elle crée est diagnostiqué à l'échec, pas prévenu par analyse statique, et un seul analyseur du bloc YAML sert désormais les trois lecteurs.
- `2026-08-30` <a id="instructions-une-source-plusieurs-noms"></a>[Une seule source d'instructions, plusieurs noms de fichiers (#612)](decisions/instructions-une-source-plusieurs-noms.md) — `AGENTS.md` reste le fichier ; `CLAUDE.md` et `.github/copilot-instructions.md` sont des liens symboliques vers lui. Claude Code suit le lien, vérifié ; Copilot non vérifié.
- `2026-08-30` <a id="lectures-pipeline-par-projection-635"></a>[Trois lectures du corpus passent à la projection, et chacune a son plafond dans un test (#635, 2026-08-30)](decisions/lectures-pipeline-par-projection-635.md) — les trois accumulations que #628 avait nommées sans les traiter (`gouvernement_roster.load_profils_from_dir`, `audit_pivot_dataset.load_pivot_directory`, la fiche de groupe) rangeaient chaque profil entier : `MemoryError` autour du 370e puis du 300e des 481 profils committés sous un plafond de 2,0 Gio, et près d'un Gio pour la seule fiche LFI ; ramenées à ~140, 22 et 82 Mio par une liste blanche de blocs lus et — pour les listes réellement parcourues — la rétention du seul résultat qu'elles en tirent, sorties prouvées identiques (quatre quarts disjoints du corpus, cinq fiches AN) et trois plafonds déduits du poids sur disque des blocs relâchés.
- `2026-08-30` <a id="permanence-champs-identite-601"></a>[Un champ d'identité publié ne meurt plus sans un run à perte déclarée (#601)](decisions/permanence-champs-identite-601.md) — composer champ par champ fait qu'une valeur survit au silence de sa source ; assumé et écrit, la péremption déclarée étant écartée faute de pouvoir distinguer « la source ne dit plus » de « la source dit non ».
- `2026-08-30` <a id="populations-profils-portees-par-les-outils-630"></a>[Les deux populations de `pivot_data/profiles/` sont portées par les outils, pas par une consigne (#630, 2026-08-30)](decisions/populations-profils-portees-par-les-outils-630.md) — `check_quality_gate.py` ne contenait pas une occurrence de `provenance` et intitulait « Candidats » des sections portant sur les 481 profils (13 candidats déclarés, 468 membres de roster) ; tout compte affiché passe désormais par `Ventilation`, et la §2 cesse de nommer les 468 membres de roster « Inattendus » un par un (rapport de 1 054 à 592 lignes).
- `2026-08-30` <a id="provenance-par-champ-603"></a>[Quelle source a rempli quel champ, et quand — un bloc à côté d'`identite` (#603)](decisions/provenance-par-champ-603.md) — `meta.provenance_champs` décrit `identite` seule, champ par champ ; dérivé après la fusion et jamais fusionné, l'inconnu se publie `source: null`, et `sources[0]` aurait attribué 2 597 des 2 612 champs à une source retirée depuis #529.
- `2026-08-30` <a id="synchro-sources-derniere-recuperation-600"></a>[`synchro_sources` publie la dernière récupération réussie, et pas son origine (#600)](decisions/synchro-sources-derniere-recuperation-600.md) — une date en retard sur le profil est un fait (la source n'a pas été re-collectée), pas une anomalie ; l'origine cache/réseau n'existe pas et ne sera pas publiée sans lecteur.
- `2026-08-30` <a id="table-inversee-decisions-par-module"></a>[Le lien code → décisions se dérive, il ne s'écrit pas à la main](decisions/table-inversee-decisions-par-module.md) — `docs/decisions-par-module.md` est généré : une décision **gouverne** un module quand elle nomme un de ses symboles de tête, elle le **mentionne** quand elle n'écrit que le fichier ; `merge_profile.py` citait 0 des 39 décisions qui le gouvernent, et le seuil de 5 du test ferme la régression.
- `2026-08-30` <a id="union-warnings-extinction-600"></a>[L'union des avertissements peut ressusciter un démenti, et deux familles Syceron s'éteignent (#600)](decisions/union-warnings-extinction-600.md) — unir les warnings des deux écrivains fait ressusciter les démentis ; une intervention Syceron publiée éteint les deux familles de #560, les questions ne comptant pas.
- `2026-08-29` <a id="absences-publiees-comme-faits-556-558-560"></a><a id="marqueur-nil-identite-556"></a><a id="groupe-gele-couverture-558"></a><a id="frontiere-vs-panne-560"></a>[Trois absences publiées comme des faits (#556, #558, #560)](decisions/absences-publiees-comme-faits-556-558-560.md) — une absence décidée, bornée par la source ou marquée en XML n'est pas un fait ; filtrage à la lecture, pas champ par champ.
- `2026-08-29` <a id="compteur-fenetre-579"></a><a id="etape-ci-runner-576"></a>[Trois contrôles qui écrivaient leur conclusion sans la mesurer (#576, #579)](decisions/compteur-fenetre-579.md) — trois compteurs recâblés sur la mesure réelle.
- `2026-08-29` <a id="deux-axes-formulaire-578"></a>[Un paramètre commandait ce qu'il ne nommait pas (#578)](decisions/deux-axes-formulaire-578.md) — le formulaire de lancement porte deux axes disjoints — ce qu'on fait des profils écrits, qui on collecte — plus le cache ; `roster_limit` ne commande plus de politique.
- `2026-08-29` <a id="partition-profils-legislature-580"></a><a id="garde-fou-blob-580"></a>[Le seuil de blob sort du critère de sortie, et les profils bruts se partitionnent par législature (#580)](decisions/partition-profils-legislature-580.md) — le profil brut se partitionne par législature (56,0 → 23,4 Mo) et le plafond de blob devient un garde-fou du quality gate, pas un critère de sortie.
- `2026-08-29` <a id="perimetre-coupure-575"></a><a id="runner-bornage-576"></a>[Un garde-fou qui bloquait sur ce que la coupure garde, et une procédure qui se saute (#575, #576)](decisions/perimetre-coupure-575.md) — et `--preparer` imprimait une procédure que rien n'exécutait.
- `2026-08-28` <a id="bornage-execute-567"></a>[La coupure d'historique a tourné pour la première fois — et `--preparer` n'avait jamais imprimé sa procédure (#567)](decisions/bornage-execute-567.md) — douze tests de motifs textuels ne valaient pas une exécution.
- `2026-08-28` <a id="cache-completude-interventions-550"></a>[La clé de cache AN porte la COMPLÉTUDE, et la sauvegarde devient explicite (#550)](decisions/cache-completude-interventions-550.md) — une clé doit décrire la complétude de ce qu'elle protège.
- `2026-08-28` <a id="cache-fraicheur-interventions-555"></a>[Les `restore-keys` du cache AN traversaient les semaines : la fraîcheur ne se met pas dans la clé, elle se lit dans celle qu'on a restaurée (#555)](decisions/cache-fraicheur-interventions-555.md) — la semaine était dans la clé depuis toujours ; c'est la restauration qui la contournait.
- `2026-08-28` <a id="collecte-vs-publie-545"></a>[Ce que la normalisation a le droit de faire : la table de relations collecté → publié (#545)](decisions/collecte-vs-publie-545.md) — chaque liste déclare les chemins bruts dont elle somme les longueurs ; seuil 0.
- `2026-08-28` <a id="couverture-listes-539"></a>[Ce qu'une liste vide veut dire : les quatre états de couverture (#539)](decisions/couverture-listes-539.md) — une liste vide recouvre quatre états sans rapport (couvert, non collecté, hors couverture, panne) : ils sont désormais nommés.
- `2026-08-28` <a id="critere-sortie-volumetrie-429"></a>[Le critère de sortie de l'épic volumétrie était écrit sur la mauvaise grandeur (#429)](decisions/critere-sortie-volumetrie-429.md) — aucune limite GitHub ne porte sur l'arbre de travail ; le critère est récrit sur le dépôt et le blob.
- `2026-08-28` <a id="defaut-collecte-vs-panne-562"></a>[Une exception n'est pas une preuve, et un défaut de notre code n'est pas une panne de l'Assemblée nationale (#562)](decisions/defaut-collecte-vs-panne-562.md) — un `TypeError` du dépôt, attrapé, était publié comme un état de la source.
- `2026-08-28` <a id="fenetre-recalibrage-551"></a>[La fenêtre de 30 ne pose pas le plateau qu'on croit, et la table mesurée ne le dit pas (#551)](decisions/fenetre-recalibrage-551.md) — mesure, projection et quatre arbitrages rendus sur la fenêtre de squash.
- `2026-08-28` <a id="identite-profils-539"></a>[Comment naît l'identité d'un profil, et où vont les identifiants de source (#539)](decisions/identite-profils-539.md) — comment naît l'identité d'un profil, et pourquoi les identifiants de source ne vivent plus dans l'`id`.
- `2026-08-27` <a id="budgets-extract-an-perimes-546"></a>[Le correctif de #540 validé en conditions réelles, et les deux budgets qu'il a périmés (#546)](decisions/budgets-extract-an-perimes-546.md) — le correctif de #540 validé en conditions réelles a périmé les deux budgets qui le bornaient.
- `2026-08-27` <a id="budgets-extract-an-remesures-546"></a>[La décomposition d'un shard `extract-an`, et les deux budgets recalés dessus (#546)](decisions/budgets-extract-an-remesures-546.md) — décomposition mesurée d'un shard `extract-an`, et les deux budgets recalés dessus.
- `2026-08-27` <a id="cle-fusion-interventions-540"></a>[Une URL de source n'est pas un identifiant : la clé de fusion des interventions (#540)](decisions/cle-fusion-interventions-540.md) — la clé devient l'identifiant de la prise de parole, pas son URL.
- `2026-08-27` <a id="licence-lot-6-530"></a>[Le versant AN passe en Licence Ouverte, et `meta.licence_donnees` devient un champ dérivé (#530, lot 6 de l'épic « une seule source AN »)](decisions/licence-lot-6-530.md) — « plus rien ne vient de NosDéputés » ne vaut pas « Licence Ouverte » : `meta.licence_donnees` devient un champ dérivé.
- `2026-08-27` <a id="push-donnees-cle-de-deploiement-508"></a>[Rétablir le check requis sans bloquer le bot : le push de données passe par une clé de déploiement (#508)](decisions/push-donnees-cle-de-deploiement-508.md) — le push de données passe par une clé de déploiement, seul moyen de rétablir un check requis sans bloquer le bot.
- `2026-08-27` <a id="retrait-nosdeputes-529"></a>[NosDéputés sort du pipeline (#529, lot 5 de l'épic « une seule source AN »)](decisions/retrait-nosdeputes-529.md) — le profil brut vient entièrement de l'open data AN.
- `2026-08-27` <a id="syceron-actif-510"></a>[Syceron activé, repli NosDéputés retiré, index tranché par acteur (#510)](decisions/syceron-actif-510.md) — un repli qui remplace la source primaire masque sa panne.
- `2026-08-26` <a id="bascule-roster-an-amo30-527"></a>[La bascule : le roster des groupes AN vient d'AMO30 (#527, lot 1b de l'épic « une seule source AN »)](decisions/bascule-roster-an-amo30-527.md) — une ligne bascule, trois conséquences qu'elle ne porte pas seule.
- `2026-08-26` <a id="cloisonnement-branche-roster-524"></a><a id="roster-suspension-totale-code-2"></a>[Cloisonnement de la branche roster, et le code 2 « suspension totale » (#524)](decisions/cloisonnement-branche-roster-524.md) — une panne de source coûte la branche roster, jamais le commit.
- `2026-08-26` <a id="correspondance-acteurs-an-525"></a>[La correspondance slug ↔ acteur AN devient un artefact committé (#525, lot 2 de l'épic « une seule source AN »)](decisions/correspondance-acteurs-an-525.md) — un artefact committé et vérifié, jamais une heuristique de noms.
- `2026-08-26` <a id="retrait-senat-528"></a>[Le Sénat sort du périmètre, et le job qui concluait vert sans rien produire est retiré (#528, lot 3 de l'épic « une seule source AN »)](decisions/retrait-senat-528.md) — décision éditoriale, pas technique ; coût mesuré quasi nul sur le corpus publié.
- `2026-08-26` <a id="roster-an-derive-amo30-526"></a><a id="perimetre-17e-legislature-526"></a>[Le roster des groupes AN est dérivé d'AMO30, derrière un drapeau baissé (#526, lot 1 de l'épic « une seule source AN »)](decisions/roster-an-derive-amo30-526.md) — le roster AN dérivé d'AMO30 est livré derrière un drapeau baissé, avec l'écart publié entrée par entrée comme compteur de migration.
- `2026-08-26` <a id="syceron-archives-verifiees-parseur-510"></a>[Suite du 26/08/2026 : les trois archives vérifiées, les deux défauts de parseur corrigés](decisions/syceron-archives-verifiees-parseur-510.md) — les trois archives Syceron vérifiées au `content-length`, et les deux défauts de parseur (traversée, titre de point) corrigés.
- `2026-08-24` <a id="extraction-groupe-suspendue-516"></a>[Suspendre l'extraction des deux groupes Sénat, sans les retirer de la config (#516)](decisions/extraction-groupe-suspendue-516.md) — le bloc exige `depuis`, `motif`, `references`, `condition_reprise`.
- `2026-08-24` <a id="plafond-roster-et-commit-518"></a>[Le plafond de lecture du roster, et le commit qui ne paie plus pour une source lente (#518, second incident)](decisions/plafond-roster-et-commit-518.md) — le plafond était placé dans la distribution de réponse de la source.
- `2026-08-24` <a id="point-de-sauvegarde-dans-les-profils-518"></a>[Un fichier de progression dans un répertoire de données (#518, troisième incident)](decisions/point-de-sauvegarde-dans-les-profils-518.md) — `Path.glob` rend les dotfiles : tout inventaire de `raw_data/profiles/` saute les noms commençant par un point.
- `2026-08-24` <a id="roster-unique-par-run-518"></a>[Un seul roster par run, une reprise sur ce qui est retentable, et des échecs qu'on peut lire (#518)](decisions/roster-unique-par-run-518.md) — le roster transite par artifact ; reprise seulement sur ce qui est retentable.
- `2026-08-21` <a id="budget-collecte-source-injoignable-514"></a>[Une source injoignable ne consomme plus le timeout d'un job, et son silence cesse de se lire comme un constat (#514)](decisions/budget-collecte-source-injoignable-514.md) — une source injoignable ne consomme plus le timeout du job, et son silence cesse de se lire comme un constat.
- `2026-08-20` <a id="budget-collecte-interventions"></a>[Borner la collecte d'interventions, pas le job qui la contient (#498)](decisions/budget-collecte-interventions.md) — un shard tué par `timeout-minutes` n'écrit aucun profil ; un budget épuisé écrit et le déclare.
- `2026-08-20` <a id="budget-execution-pleine-echelle-467"></a>[Budget d'exécution à pleine échelle : 630 min annoncées, 55 mesurées (#467)](decisions/budget-execution-pleine-echelle-467.md) — et un cache disque évite un re-téléchargement, jamais un re-parse.
- `2026-08-20` <a id="cache-mode-interventions-505"></a>[La clé de cache AN porte le MODE, et le job roster ne l'écrit plus (#505)](decisions/cache-mode-interventions-505.md) — un job n'écrit jamais la clé d'un contenu qu'il ne produit pas.
- `2026-08-20` <a id="chambre-par-mandat-electif"></a>[La chambre est un fait du mandat, pas du profil : `mandats[].chambre` estampillée à la collecte (#492)](decisions/chambre-par-mandat-electif.md) — la chambre est un fait du mandat, estampillé à la collecte, pas une propriété du profil.
- `2026-08-20` <a id="chambres-profil-derivees"></a>[`chambres` au niveau profil : une liste dérivée, et `chambre` qui n'en est plus que le premier élément (#493)](decisions/chambres-profil-derivees.md) — un champ dérivé se recalcule, il ne se fusionne pas ; le repli est déclaré par profil.
- `2026-08-20` <a id="ci-tests-pytest"></a>[Un job CI exécute la suite de tests : audit préalable et arbitrages (#473)](decisions/ci-tests-pytest.md) — aucun test ne lit le corpus vivant ni le réseau ; le sparse-checkout le rend structurel.
- `2026-08-20` <a id="collecte-non-publiee"></a><a id="roster-jamais-ecrit-vide"></a>[Un timeout ne peut plus écraser le roster, et rien de collecté ne reste non publié (#511)](decisions/collecte-non-publiee.md) — rien de collecté ne reste non publié (seuil 0), et un timeout ne peut plus écrire un roster vide.
- `2026-08-20` <a id="collecte-vide-necrase-jamais"></a>[Une collecte vide n'écrase jamais une collecte non vide (#465)](decisions/collecte-vide-necrase-jamais.md) — sous `--no-merge`, un champ revenu vide ne remplace jamais un champ non vide — par champ, jamais par profil.
- `2026-08-20` <a id="consommateurs-chambres-migres"></a>[Les consommateurs de `chambre` migrés vers `chambres`, et le garde-fou qui datera son retrait (#494)](decisions/consommateurs-chambres-migres.md) — les consommateurs de `chambre` migrés vers `chambres`, avec le garde-fou qui datera le retrait du scalaire.
- `2026-08-20` <a id="deduplication-entrees-membres"></a>[`membres[]` publiait deux fois le même fait : dédupliquer sans effacer les changements de portefeuille (#480)](decisions/deduplication-entrees-membres.md) — dédupliquer `membres[]` sans effacer un changement de portefeuille.
- `2026-08-20` <a id="deux-chambres-interrogees"></a>[Le passé sénatorial est un fait de carrière, pas une donnée d'activité : bicaméral pour les candidats seulement (#488)](decisions/deux-chambres-interrogees.md) — les deux chambres ne sont interrogées que pour les candidats déclarés.
- `2026-08-20` <a id="fenetre-historique-donnees"></a>[Borner l'historique de données : ce que ça rend vraiment, et quand (#434)](decisions/fenetre-historique-donnees.md) — squash déclenché par la mesure, jamais de réécriture automatique.
- `2026-08-20` <a id="id-pivot-sans-prefixe"></a>[L'`id` d'un profil pivot est le slug : le préfixe de provenance était instable (#487)](decisions/id-pivot-sans-prefixe.md) — le préfixe changeait de valeur sur une carrière inchangée.
- `2026-08-20` <a id="integrite-referentielle-pivot"></a>[Rien ne vérifiait que les clés publiées résolvent : le contrôle d'invariance (#485)](decisions/integrite-referentielle-pivot.md) — une clé publiée qui ne résout pas est un vote sans objet ; seuil 0 avant commit.
- `2026-08-20` <a id="interventions-senat-501"></a>[`extract-senat` ne collecte plus d'interventions : la collecte n'en retenait aucune, par construction (#501)](decisions/interventions-senat-501.md) — tout chemin de collecte déclare ce qu'il fait des interventions.
- `2026-08-20` <a id="libelles-formulaire"></a>[Les descriptions d'input sont des LIBELLES, pas des descriptions](decisions/libelles-formulaire.md) — GitHub affiche la description d'un input comme libellé du champ : ce sont des libellés, pas des descriptions.
- `2026-08-20` <a id="mandat-electif-perdu-fausse-le-denominateur"></a>[Un mandat électif perdu ne manque pas seulement sur la fiche : il sort le membre du dénominateur de son groupe (#465)](decisions/mandat-electif-perdu-fausse-le-denominateur.md) — un mandat électif perdu sort le membre du dénominateur de son groupe — la perte se propage à un chiffre publié.
- `2026-08-20` <a id="parlementaire-en-mission-nest-pas-ministre"></a>[Le `label` d'un mandat `MINISTERE` ne dit pas si c'est un maroquin (#474)](decisions/parlementaire-en-mission-nest-pas-ministre.md) — le `label` d'un mandat `MINISTERE` ne dit pas si c'est un maroquin : un parlementaire en mission n'est pas ministre.
- `2026-08-20` <a id="perimetre-controle-perte"></a>[Le périmètre du contrôle de perte : ce qu'il couvre, ce qu'il ne couvre pas (#470)](decisions/perimetre-controle-perte.md) — les cinq couches publiées et les deux index partagés, pas seulement les profils.
- `2026-08-20` <a id="publication-dun-job-annule"></a>[Un préfixe de flux est valide, un préfixe de profil est faux (#460)](decisions/publication-dun-job-annule.md) — un préfixe de flux est valide, un préfixe de profil est faux : ce qu'un job annulé a le droit de publier.
- `2026-08-20` <a id="refonte-inputs-workflow"></a>[Le formulaire de lancement disait pourquoi, pas quoi](decisions/refonte-inputs-workflow.md) — un formulaire de lancement dit quoi, pas pourquoi : 575 mots de description ramenés à des libellés.
- `2026-08-20` <a id="syceron-acteur-ref-nu-510"></a>[Syceron publie l'identifiant d'orateur NU, et n'a donc jamais rien indexé (#510)](decisions/syceron-acteur-ref-nu-510.md) — la source primaire des interventions n'avait donc jamais rien indexé.
- `2026-08-20` <a id="test-adosse-au-corpus-vivant"></a>[Un test d'acceptation adossé au corpus vivant rougit quand la donnée s'améliore (#457)](decisions/test-adosse-au-corpus-vivant.md) — un test d'acceptation adossé au corpus vivant rougit quand la donnée s'améliore : fixtures figées.
- `2026-08-20` <a id="workers-fige-a-1"></a>[`workers` retire du formulaire : un bouton qui ne pouvait que nuire](decisions/workers-fige-a-1.md) — `workers` retiré du formulaire : un paramètre documenté comme nuisible reste un piège.
- `2026-08-19` <a id="cache-amendements-existence-nest-pas-conformite"></a>[L'existence d'un cache n'est pas la preuve de son contenu — et #447 n'avait pas de seconde cause](decisions/cache-amendements-existence-nest-pas-conformite.md) — l'existence d'un répertoire de cache ne prouve rien de son contenu : vérifier aussi le format de clé.
- `2026-08-19` <a id="controle-de-perte-avant-commit"></a>[Le contrôle de perte était écrit, documenté, et branché sur rien (#460)](decisions/controle-de-perte-avant-commit.md) — le contrôle de perte était écrit, documenté, et branché sur rien : il tourne maintenant avant le commit.
- `2026-08-19` <a id="normalisation-amendements"></a>[Normaliser les amendements : le coût n'est pas l'amendement, c'est sa liste de cosignataires (#431)](decisions/normalisation-amendements.md) — mapping par membre, index partagé par législature.
- `2026-08-19` <a id="normalisation-votes"></a>[Normaliser les votes : une liste partagée, un mapping, et deux invariants devenus des jointures (#432)](decisions/normalisation-votes.md) — liste partagée, mapping par membre, deux invariants devenus des jointures.
- `2026-08-19` <a id="overwrite-profiles-sans-purge-cache"></a>[`overwrite_profiles` : écraser les profils sans purger le cache](decisions/overwrite-profiles-sans-purge-cache.md) — écraser les profils sans purger le cache : deux axes distincts, déjà.
- `2026-08-19` <a id="publication-scopee-artifacts"></a>[Un artifact = la contribution d'un job : ce qu'on publie décide de ce qu'on peut corriger (#450)](decisions/publication-scopee-artifacts.md) — republier la ligne de base défait `--no-merge` et fait entrer les shards en collision.
- `2026-08-19` <a id="resolution-legislature-deux-mecanismes-432"></a>[Résoudre la `legislature` d'un vote : deux mécanismes, pas un seul (#432)](decisions/resolution-legislature-deux-mecanismes-432.md) — résoudre la `legislature` d'un vote demande deux mécanismes — jumeau étiqueté puis calendrier — jamais un défaut.
- `2026-08-19` <a id="resolution-legislature-votes"></a>[Où vit la liste dédupliquée des scrutins : un fichier partagé, pas un par entité (#432)](decisions/resolution-legislature-votes.md) — la liste dédupliquée des scrutins vit dans un fichier partagé, pas un par entité.
- `2026-08-19` <a id="restauration-interventions"></a>[Restaurer 789 interventions sans revenir sur le reste du schéma (#460)](decisions/restauration-interventions.md) — restaurer 789 interventions champ par champ, sans revenir sur le reste du schéma.
- `2026-08-19` <a id="signal-uid-partiel"></a>[Couverture `uid` partielle : ce qui manquait n'était pas un verrou, c'était un signal (#447)](decisions/signal-uid-partiel.md) — ce qui manquait sur la couverture `uid` partielle n'était pas un verrou, c'était un signal.
- `2026-08-19` <a id="telechargement-an-prefixe-valide-443"></a>[Téléchargement AN : trois modes de défaillance, un seul principe — ne jamais jeter un préfixe valide (#443)](decisions/telechargement-an-prefixe-valide-443.md) — trois modes de défaillance du téléchargement AN, un seul principe : ne jamais jeter un préfixe valide.
- `2026-08-19` <a id="telechargement-an-trois-modes-defaillance"></a>[Régénérer l'existant : `--refresh-existing`, l'inverse de `--skip-existing` (#445)](decisions/telechargement-an-trois-modes-defaillance.md) — `--refresh-existing`, l'inverse de `--skip-existing` : régénérer l'existant sans sauter les profils à corriger.
- `2026-08-19` <a id="volumetrie-arbre-de-travail-nest-pas-depot"></a>[Volumétrie : l'arbre de travail n'est pas le dépôt, et la photo n'est pas le coût](decisions/volumetrie-arbre-de-travail-nest-pas-depot.md) — l'arbre de travail n'est pas le dépôt, et une photo instantanée n'est pas un coût.
- `2026-08-18` <a id="amendements-cle-uid"></a>[Amendements : la clé du store est l'`uid`, jamais le `numero` (préalable à #431)](decisions/amendements-cle-uid.md) — la clé du store d'amendements est l'`uid` AN, jamais le `numero` qui repart à 1.
- `2026-08-18` <a id="amendements-zero-pas-de-hard-fail"></a>[Quality gate : « 0 amendement collecté » reste non bloquant, mais cesse d'être discret (#378)](decisions/amendements-zero-pas-de-hard-fail.md) — « 0 amendement collecté » reste non bloquant, mais cesse d'être discret.
- `2026-08-18` <a id="audit-rapport-perimetre-candidats"></a>[Rapport d'audit pivot : détail réservé aux candidats déclarés, indicateurs de distribution retirés](decisions/audit-rapport-perimetre-candidats.md) — le détail du rapport d'audit est réservé aux candidats déclarés ; indicateurs de distribution retirés.
- `2026-08-18` <a id="cache-cle-amendements-separee"></a>[Cache CI : clé propre aux amendements, et chemins énumérés pour les jobs AN (#424)](decisions/cache-cle-amendements-separee.md) — deux jobs qui partagent une clé se privent mutuellement de sauvegarde.
- `2026-08-18` <a id="concurrence-shards-extraction-412"></a>[Jobs d'extraction de `generate-data.yml` : résilience au *skip*, concurrence des shards, factorisation (#412)](decisions/concurrence-shards-extraction-412.md) — résilience au *skip*, concurrence des shards d'extraction, factorisation du préambule.
- `2026-08-18` <a id="couverture-dossiers-hors-couverture-vs-zero"></a>[Couverture des dossiers : « hors couverture de la source » ≠ « réellement à zéro » (#399)](decisions/couverture-dossiers-hors-couverture-vs-zero.md) — « hors couverture de la source » n'est pas « réellement à zéro » : le warning cesse d'affirmer un défaut de données.
- `2026-08-18` <a id="deploy-pages-declencheur-donnees"></a>[Publication du site après un run de données : le commit du bot n'émet aucun événement `push` (#416)](decisions/deploy-pages-declencheur-donnees.md) — un commit poussé par le `GITHUB_TOKEN` n'émet aucun événement `push` : la publication du site doit être déclenchée autrement.
- `2026-08-18` <a id="dossiers-multi-archives-origine-document"></a>[Dossiers législatifs : ingestion multi-archives, origine par document déposé, statut `promulgue` (#400)](decisions/dossiers-multi-archives-origine-document.md) — ingestion multi-archives des dossiers, origine déterminée par document déposé, statut `promulgue` ajouté.
- `2026-08-18` <a id="gouvernement-premier-ministre-portefeuille"></a>[`gouvernement_profile` : `premier_ministre` et `portefeuille` câblés depuis les mandats `MINISTERE` (#398)](decisions/gouvernement-premier-ministre-portefeuille.md) — deux taux à zéro documentés comme limites de source ne l'étaient plus.
- `2026-08-18` <a id="gouvernement-textes-fam-codes-archives"></a>[`gouvernement_textes` : 3 derniers `fam_code` mappés ; `TSORTF02` tranché sur données réelles (#402)](decisions/gouvernement-textes-fam-codes-archives.md) — les 3 derniers `fam_code` mappés, `TSORTF02` tranché sur données réelles plutôt que sur un libellé ambigu.
- `2026-08-18` <a id="gouvernement-textes-fam-codes-manquants"></a>[`gouvernement_textes` : 3 `fam_code` manquants excluaient 42 % des textes ; `adopte_cmp` ajouté à la nomenclature (#397)](decisions/gouvernement-textes-fam-codes-manquants.md) — 3 `fam_code` manquants excluaient 42 % des textes ; `adopte_cmp` ajouté à la nomenclature.
- `2026-08-18` <a id="gouvernement-textes-initiateurs"></a>[Profils de gouvernement : le lien ministre → texte (#435)](decisions/gouvernement-textes-initiateurs.md) — le lien ministre → texte : le pendant manquant de `role_signataire` pour les textes de gouvernement.
- `2026-08-18` <a id="gouvernement-textes-non-ecrasement"></a>[Profils de gouvernement : ne jamais réécrire sur une collecte incomplète, et cache dossiers dédié (#427)](decisions/gouvernement-textes-non-ecrasement.md) — ne jamais réécrire un profil de gouvernement sur une collecte incomplète ; cache dossiers dédié.
- `2026-08-18` <a id="merge-and-pivot-budget-permissions-413"></a>[`merge-and-pivot` : garde-fou #390 hors `main`, entrées de configuration, budget de temps mur, permissions (#413)](decisions/merge-and-pivot-budget-permissions-413.md) — `merge-and-pivot` : garde-fou de code périmé hors `main`, entrées de configuration, budget de temps mur, permissions scopées.
- `2026-08-18` <a id="profils-json-compact"></a>[Profils écrits en JSON compact, groupes et gouvernements indentés (#433)](decisions/profils-json-compact.md) — profils écrits en JSON compact (35 % du volume n'était que de l'indentation) ; groupes et gouvernements restent indentés.
- `2026-08-18` <a id="retry-inputs-appariement-prefixe"></a>[`retry-generate-data.yml` : reconstruction des inputs réparée par appariement de préfixe, collecte de jobs unifiée (#414)](decisions/retry-inputs-appariement-prefixe.md) — le shardage a rendu les noms de jobs dynamiques : la reconstruction des inputs s'apparie sur le préfixe.
- `2026-08-18` <a id="revue-workflows-ci-342"></a>[Revue transversale des workflows GitHub Actions : ce qui est gardé, ce qui est corrigé (#342)](decisions/revue-workflows-ci-342.md) — revue transversale des workflows : ce qui est gardé, ce qui est corrigé, et par quelles sous-issues.
- `2026-08-18` <a id="shardage-extract-roster-groupes"></a>[Shardage de `extract-roster-groupes` en 8 tranches, découpées par modulo (#394)](decisions/shardage-extract-roster-groupes.md) — `extract-roster-groupes` shardé en 8 tranches par modulo.
- `2026-08-18` <a id="votes-multi-legislature"></a>[Votes : agrégation des législatures 14 à 17, index dédupliqué, 14/15/16 figées (#403)](decisions/votes-multi-legislature.md) — 14/15/16 figées et committées.
- `2026-08-18` <a id="workflows-claude-securite"></a>[Workflows Claude : garde d'auteur, asymétrie de sandbox levée, marketplace non épinglé (#415)](decisions/workflows-claude-securite.md) — garde d'auteur sur `issue_comment`, asymétrie de sandbox levée, marketplace non épinglé.
- `2026-08-17` <a id="amendements-zero-silencieux-acteur-ref"></a>[Zéro amendement silencieux quand l'acteurRef est introuvable (#265, fix 5)](decisions/amendements-zero-silencieux-acteur-ref.md) — un acteurRef introuvable ne rend plus zéro amendement en silence.
- `2026-08-17` <a id="budget-roster-mesure"></a>[Budget CI de `extract-roster-groupes` : mesure réelle (#376)](decisions/budget-roster-mesure.md) — budget CI de `extract-roster-groupes` mesuré, à la place d'un `timeout-minutes` provisoire.
- `2026-08-17` <a id="cache-amendements-forme-dedupliquee"></a>[Cache amendements stocké et lu sous forme dédupliquée (#377)](decisions/cache-amendements-forme-dedupliquee.md) — le cache amendements est stocké et relu sous forme dédupliquée : correctif de l'OOM.
- `2026-08-17` <a id="freshness-timestamps-groupes-gouvernements-partis"></a>[Extension de la stabilité des horodatages aux profils groupe/gouvernement/parti (#343, complet)](decisions/freshness-timestamps-groupes-gouvernements-partis.md) — stabilité des horodatages étendue aux profils groupe, gouvernement et parti.
- `2026-08-17` <a id="index-amendements-sharde-par-acteur"></a>[Index amendements shardé par acteur (#392)](decisions/index-amendements-sharde-par-acteur.md) — 93 % du coût d'extraction d'un membre était la relecture de l'index.
- `2026-08-17` <a id="mandats-officiels-an-369"></a>[Mandats commission/groupe_amitie/extra_parlementaire sourcés depuis l'AN, fetch_identity NosDéputés rendu conditionnel (#369, complet), watchdog générique sur tous les téléchargements zip (#370, complet)](decisions/mandats-officiels-an-369.md) — mandats commission/groupe d'amitié/extra-parlementaire sourcés depuis l'AN ; watchdog générique sur tous les téléchargements zip.
- `2026-08-17` <a id="ne-jamais-committer-un-build-perime"></a>[Ne jamais committer un build produit avec du code périmé (#390)](decisions/ne-jamais-committer-un-build-perime.md) — ne jamais committer un build produit avec du code périmé : le checkout d'un job de fusion date du déclenchement.
- `2026-08-17` <a id="nettoyage-archive-brute-amendements"></a>[Suppression de l'archive brute `amendements.zip` après construction de l'index (#264)](decisions/nettoyage-archive-brute-amendements.md) — l'archive brute `amendements.zip` est supprimée après construction de l'index, succès comme échec.
- `2026-08-17` <a id="normalisation-fonction-mandats-agreges"></a>[Normalisation de `par_fonction` dans `mandats_agreges`, et requalification du défaut « catégorie commission » (#379)](decisions/normalisation-fonction-mandats-agreges.md) — normalisation de `par_fonction` : deux référentiels aux conventions typographiques différentes comptaient à part.
- `2026-08-17` <a id="oom-lecture-amendements-par-candidat"></a>[OOM persistant : lecture per-candidat de l'index amendements, tentative de mémoïsation revertée](decisions/oom-lecture-amendements-par-candidat.md) — OOM par relecture de l'index amendements à chaque candidat ; mémoïsation tentée puis revertée (fuite entre tests).
- `2026-08-17` <a id="oom-reconstruction-amendements-figees"></a>[OOM lors de la relecture d'un index amendements figé déjà en cache (exécution locale)](decisions/oom-reconstruction-amendements-figees.md) — OOM à la relecture d'un index figé déjà en cache, en exécution locale.
- `2026-08-17` <a id="parallele-oom-local-runner-ci"></a>[Parallèle RAM entre l'exécution locale et les runners GitHub Actions hébergés, diagnostic ajouté](decisions/parallele-oom-local-runner-ci.md) — la RAM d'une exécution locale et celle d'un runner hébergé se comparent : le diagnostic vaut pour les deux.
- `2026-08-17` <a id="purge-mandats-dupliques-prudence"></a>[Purge des mandats hérités dupliqués : appariement prudent (#387)](decisions/purge-mandats-dupliques-prudence.md) — purge des mandats hérités dupliqués par appariement prudent : la fusion additive ne remplace jamais.
- `2026-08-17` <a id="resilience-roster-decision"></a>[Résilience de `extract-roster-groupes` : le sharding reste nécessaire (#347)](decisions/resilience-roster-decision.md) — le sharding de `extract-roster-groupes` reste nécessaire, chiffres à l'appui.
- `2026-08-17` <a id="resolution-an-prenom-compose-et-gel-runner-etape0"></a>[Bug de résolution AN pour les prénoms composés, et gel runner déplacé sur l'étape 0 (run #47)](decisions/resolution-an-prenom-compose-et-gel-runner-etape0.md) — bug de résolution AN sur les prénoms composés, et gel runner déplacé sur l'étape 0.
- `2026-08-17` <a id="retry-push-merge-and-pivot-bash-e"></a>[La boucle de retry du push ne rebouclait jamais (`bash -e`) (#389)](decisions/retry-push-merge-and-pivot-bash-e.md) — la boucle de retry du push ne rebouclait jamais, faute de `defaults: shell` (`bash -e`).
- `2026-08-17` <a id="scission-cache-ci-ecartee"></a>[Scission du cache CI `.cache` par sous-répertoire : écartée (#374, fermée non planifiée)](decisions/scission-cache-ci-ecartee.md) — scinder le cache CI par sous-répertoire : écartée, avec les chiffres qui l'écartent.
- `2026-08-17` <a id="taxonomie-mandats-typeorgane-an"></a>[Taxonomie des mandats : exploitation des `typeOrgane` AN non mappés (#382, option « mixte »)](decisions/taxonomie-mandats-typeorgane-an.md) — la moitié du référentiel était inexploitée.
- `2026-08-17` <a id="verification-bout-en-bout-legislatures-figees"></a>[Vérification de bout en bout des législatures figées 15/16 (#273, clôture de l'epic #268)](decisions/verification-bout-en-bout-legislatures-figees.md) — vérification de bout en bout des législatures figées 15/16 : aucun changement de code nécessaire.
- `2026-08-16` <a id="bascule-identite-an-primaire"></a>[`fetch_identity` : identité (bio) des députés basculée sur l'AN comme source primaire, mandats/groupe restent sur NosDéputés (#355)](decisions/bascule-identite-an-primaire.md) — l'identité (bio) des députés bascule sur l'AN comme source primaire ; mandats et groupe restent sur NosDéputés.
- `2026-08-16` <a id="identite-acteurs-amo30"></a>[`_build_acteur_identite_index` : couvrir les élu⋅e⋅s dont le mandat est terminé via `AMO30`, pas en combinant `AMO20` par législature (#354)](decisions/identite-acteurs-amo30.md) — couvrir les élu·es dont le mandat est terminé via `AMO30`, pas en combinant `AMO20` par législature.
- `2026-08-16` <a id="mandats-agreges-famille-1"></a>[`mandats_agreges` : agrégation catégorielle sur `mandats[]`, famille 1 (#361, sous-issue de #349)](decisions/mandats-agreges-famille-1.md) — `mandats_agreges` : agrégation catégorielle sur `mandats[]`, première famille.
- `2026-08-16` <a id="matrix-extract-an-par-candidat"></a>[`extract-an` en matrix strategy par candidat, pour isoler la perte en cas de shutdown signal runner (#344)](decisions/matrix-extract-an-par-candidat.md) — `extract-an` en matrix par candidat, pour isoler la perte en cas de `shutdown signal` runner.
- `2026-08-16` <a id="mode-extraction-leger-roster"></a>[Mode d'extraction léger pour `extract-roster-groupes` (#357, sous-issue 6/6 de #351)](decisions/mode-extraction-leger-roster.md) — mode d'extraction léger pour `extract-roster-groupes`, une fois l'identité résolue côté AN.
- `2026-08-16` <a id="organe-index-organeref"></a>[`_build_organe_index` : résoudre `organeRef` via `AMO30` (historique) sans filtrage par `codeType` (#353)](decisions/organe-index-organeref.md) — résoudre `organeRef` via `AMO30` historique, sans filtrage par `codeType`.
- `2026-08-16` <a id="pivot-freshness-timestamps-stables"></a>[`genere_le`/`synchro_le` des pivots ne doivent avancer que si le contenu change réellement (#343)](decisions/pivot-freshness-timestamps-stables.md) — `genere_le`/`synchro_le` n'avancent que si le contenu change réellement.
- `2026-08-16` <a id="resilience-generate-data-shutdown-signal"></a><a id="dossiers-legislatifs-nosdeputes-vs-an-officiel"></a><a id="get-payload-retry"></a>[Résilience de `generate-data.yml` face aux `shutdown signal` runner : continue-on-error généralisé, watchdog réseau, retry générique sur `_get_payload`, retry `retry-generate-data.yml` non-régressif, et appels NosDéputés morts pour les députés (dossiers, votes)](decisions/resilience-generate-data-shutdown-signal.md) — continue-on-error généralisé, watchdog réseau, retry générique, appels NosDéputés morts retirés.
- `2026-08-16` <a id="retrait-fetch-activity-synthesis"></a>[Retrait de `fetch_activity_synthesis` (#356)](decisions/retrait-fetch-activity-synthesis.md) — `fetch_activity_synthesis` retiré : plus rien n'en dépendait.
- `2026-08-16` <a id="retry-generate-data-best-effort-non-bloquant"></a>[`retry-generate-data.yml` : le step best-effort d'extraction des inputs ne doit pas pouvoir bloquer le retry (#336)](decisions/retry-generate-data-best-effort-non-bloquant.md) — un step best-effort d'extraction des inputs ne doit pas pouvoir bloquer le retry.
- `2026-08-15` <a id="audit-pipeline-gouvernement"></a>[`audit_pipeline.py` : intégration du rapport gouvernement (#321, sous-issue 5/6 de #316)](decisions/audit-pipeline-gouvernement.md) — le rapport gouvernement entre dans `audit_pipeline.py`, à parité avec profils et groupes.
- `2026-08-15` <a id="audit-plages-temporelles"></a>[Épic #316 — tableaux croisés des plages temporelles (#317/#318/#320/#321) : bilan et décisions transverses](decisions/audit-plages-temporelles.md) — tableaux croisés des plages temporelles : bilan de l'épic et décisions transverses.
- `2026-08-15` <a id="plage-dates-groupes"></a>[Tableau croisé des plages temporelles par groupe (#318, sous-issue 2/6 de #316)](decisions/plage-dates-groupes.md) — tableau croisé des plages temporelles par groupe.
- `2026-08-14` <a id="direction-artistique-empreinte"></a>[Direction artistique de `web/UI_finale` : brief, itérations et alternatives écartées](decisions/direction-artistique-empreinte.md) — direction artistique de `web/UI_finale` : brief, itérations, et les alternatives écartées.
- `2026-08-14` <a id="gouvernement-ci-integration"></a>[Intégration de `generate_gouvernement_profiles.py` dans `generate-data.yml` (#215)](decisions/gouvernement-ci-integration.md) — `generate_gouvernement_profiles.py` branché dans `merge-and-pivot`.
- `2026-08-14` <a id="gouvernement-doc-cloture"></a>[Documentation upkeep de clôture, vue Gouvernement (#214, plan #184)](decisions/gouvernement-doc-cloture.md) — passe documentaire de clôture de la vue Gouvernement, sans anticiper de fonctionnalité non livrée.
- `2026-08-14` <a id="gouvernement-profile-rattachement"></a>[`gouvernement_profile.py` : rattachement des textes par `date_depot`, exclusion silencieuse des dossiers non classifiables (#211)](decisions/gouvernement-profile-rattachement.md) — rattachement des textes par `date_depot`, et exclusion des dossiers non classifiables.
- `2026-08-14` <a id="gouvernement-roster-desambiguisation"></a>[`gouvernement_roster.py` : désambiguïsation par libellé exact + garde-fou de période, pas l'inverse (#209)](decisions/gouvernement-roster-desambiguisation.md) — désambiguïsation par libellé exact puis garde-fou de période, jamais l'inverse.
- `2026-08-14` <a id="gouvernement-textes-statut-210-version-initiale"></a><a id="gouvernement-textes-statut"></a>[`gouvernement_textes.py` : filtre de statut par décision de séance, pas par `codeActe`/`fam_code` seul (#210)](decisions/gouvernement-textes-statut-210-version-initiale.md) — version initiale de la décision ci-dessus, laissée en double dans le fichier d'origine — `TSORTF24` y est encore non résolu.
- `2026-08-14` <a id="gouvernement-textes-statut-49-3-rejete"></a>[`KNOWN_STATUTS_TEXTE_GOUVERNEMENTAL` : ajout de `rejete_49_3` (#208, réouverte)](decisions/gouvernement-textes-statut-49-3-rejete.md) — `rejete_49_3` ajouté à la nomenclature fermée : le 49.3 n'est pas qu'une voie d'adoption.
- `2026-08-14` <a id="gouvernement-textes-statut"></a>[`gouvernement_textes.py` : filtre de statut par décision de séance, pas par `codeActe`/`fam_code` seul (#210)](decisions/gouvernement-textes-statut.md) — le statut d'un texte se lit sur la décision de séance, pas sur le `codeActe`/`fam_code` seul.
- `2026-08-14` <a id="licences"></a>[Audit des sources de données et de leurs licences, pour les Mentions légales (#288)](decisions/licences.md) — audit des sources et de leurs licences : ce que le partage à l'identique impose au corpus publié.
- `2026-08-14` <a id="pages-statiques-methodologie-mentions-legales"></a>[Pages Méthodologie et Mentions légales dans web/UI_finale (#289, plan #140)](decisions/pages-statiques-methodologie-mentions-legales.md) — pages Méthodologie et Mentions légales portées dans `web/UI_finale`, sur un composant partagé.
- `2026-08-14` <a id="quality-gate-gouvernements"></a>[`check_quality_gate.py` : section gouvernements (§5), couverture ministérielle proxy par `portefeuille` (#212)](decisions/quality-gate-gouvernements.md) — section gouvernements du quality gate : couverture ministérielle approchée par `portefeuille`.
- `2026-08-13` <a id="amendements-failed-legislature-marker-inter-jobs"></a>[Marqueur disque inter-jobs pour le cache d'échec amendements par législature (#246)](decisions/amendements-failed-legislature-marker-inter-jobs.md) — marqueur disque inter-jobs pour l'échec d'une législature d'amendements.
- `2026-08-13` <a id="amendements-index-budget-ci-cache-granularite"></a>[Spike : budget CI pour un job dédié `extract-amendements-an` et granularité de cache (#249)](decisions/amendements-index-budget-ci-cache-granularite.md) — spike : budget CI d'un job dédié amendements, et granularité de cache tranchée.
- `2026-08-13` <a id="amendements-index-cache-only-consumers"></a>[Bascule d'`extract-an`/`extract-roster-groupes` vers la lecture cache-only des amendements (#252)](decisions/amendements-index-cache-only-consumers.md) — `extract-an` et `extract-roster-groupes` lisent les amendements en cache seul.
- `2026-08-13` <a id="amendements-index-cache-only-split"></a>[Séparer téléchargement/construction et lecture cache-only dans `_build_acteur_amendement_index` (#250)](decisions/amendements-index-cache-only-split.md) — téléchargement/construction et lecture cache-only séparés en deux points d'entrée.
- `2026-08-13` <a id="amendements-index-job-dedie-ci"></a>[Job CI dédié `extract-amendements-an` : construction inconditionnelle des 3 index de législature (#251)](decisions/amendements-index-job-dedie-ci.md) — un job CI dédié construit inconditionnellement les 3 index de législature.
- `2026-08-13` <a id="amendements-index-non-regression-fraicheur"></a>[Non-régression sur échec de reconstruction d'un index amendements + indicateur de fraîcheur (#253)](decisions/amendements-index-non-regression-fraicheur.md) — un échec de reconstruction ne régresse pas l'index en place, et la fraîcheur devient un indicateur.
- `2026-08-13` <a id="amendements-index-quality-gate-fraicheur"></a>[Quality gate : distinguer un index amendements jamais construit d'un index périmé (#254)](decisions/amendements-index-quality-gate-fraicheur.md) — le quality gate distingue un index jamais construit d'un index périmé.
- `2026-08-13` <a id="amendements-legislatures-figees"></a>[Index amendements des législatures 15/16 : construction manuelle hors CI, committée](decisions/amendements-legislatures-figees.md) — construits hors CI, committés, jamais re-téléchargés.
- `2026-08-13` <a id="amendements-range-download-legislature-isolation"></a>[Téléchargement par plages (Range) + isolation par législature pour les amendements officiels (#241)](decisions/amendements-range-download-legislature-isolation.md) — téléchargement par plages (Range) et isolation par législature.
- `2026-08-13` <a id="amendements-retry-blocage-legislature"></a>[Le retry avec backoff des amendements (#225) transforme un échec instantané en blocage de plusieurs minutes par candidat (#239)](decisions/amendements-retry-blocage-legislature.md) — un retry avec backoff transforme un échec instantané en blocage de plusieurs minutes par candidat.
- `2026-08-13` <a id="pythonunbuffered-generate-data"></a>[`PYTHONUNBUFFERED` global sur `generate-data.yml` : stdout fiable en CI non-TTY (#259)](decisions/pythonunbuffered-generate-data.md) — CPython bufferise `stdout` dès que la sortie n'est pas un TTY.
- `2026-08-13` <a id="retry-generate-data-continue-on-error"></a>[Étendre `retry-generate-data.yml` aux échecs de job `continue-on-error` masqués par une conclusion de run `success` (#245)](decisions/retry-generate-data-continue-on-error.md) — un échec de job `continue-on-error` masqué par une conclusion de run `success` déclenche quand même le retry.
- `2026-08-13` <a id="retry-generate-data-detection-impossible"></a>[Distinguer erreur API et signature absente dans `retry-generate-data.yml` (#237)](decisions/retry-generate-data-detection-impossible.md) — distinguer une erreur d'API d'une signature absente : les deux ne se traitent pas pareil.
- `2026-08-13` <a id="retry-preemption-logs"></a>[`gh api .../logs` sans `--allow-escape-sequences` : cause racine de l'inefficacité du retry automatique sur les runs #26-28 (#236)](decisions/retry-preemption-logs.md) — `gh api .../logs` sans `--allow-escape-sequences` : cause racine de l'inefficacité du retry.
- `2026-08-12` <a id="ci-cd"></a>[Angle mort du `runner shutdown signal` sur `if: always()` et la sauvegarde de cache (#228)](decisions/ci-cd.md) — angle mort du `shutdown signal` runner sur `if: always()` et sur la sauvegarde de cache.
- `2026-08-12` <a id="concurrence-ci-roster"></a>[Réduction du pic de jobs concurrents `generate-data.yml` : séquencement + cache AN partagé](decisions/concurrence-ci-roster.md) — réduction du pic de jobs concurrents : séquencement et cache AN partagé.
- `2026-08-12` <a id="limit-sample"></a>[Déploiement progressif de l'extraction roster-driven : --limit vs --sample](decisions/limit-sample.md) — déploiement progressif de l'extraction roster-driven : `--limit` contre `--sample`.
- `2026-08-12` <a id="limit-skip-existing-roster-groupes"></a>[`--limit` + `--skip-existing` sur `extract-roster-groupes` : sélection progressive + rafraîchissement](decisions/limit-skip-existing-roster-groupes.md) — conquête de couverture et rafraîchissement ne sont pas le même besoin.
- `2026-08-12` <a id="retry-generate-data-preemption"></a>[Retry automatique de `generate-data.yml` sur signature de préemption runner (#230)](decisions/retry-generate-data-preemption.md) — retry automatique sur signature de préemption runner.
- `2026-08-12` <a id="senat-periode-debut"></a>[Groupes Sénat : ne pas renseigner `senat_periode_debut` dans `groupes_reels.json`](decisions/senat-periode-debut.md) — une valeur inventée vaut moins qu'une absence.
- `2026-08-12` <a id="seuil-couverture-groupe"></a>[Seuil de couverture de groupe (`--groupe-min-members`) : conservé faute de chiffres réels à pleine échelle](decisions/seuil-couverture-groupe.md) — `--groupe-min-members` conservé, faute de chiffres réels à pleine échelle.
- `2026-08-12` <a id="verification-billing-actions"></a>[Vérification quota/limite de dépense GitHub Actions (#221) : hypothèse infirmée](decisions/verification-billing-actions.md) — quota/limite de dépense GitHub Actions : hypothèse vérifiée et infirmée.
- `2026-08-10` <a id="provenance-pivot"></a>[Provenance des profils pivot : candidat_declare vs roster_groupe](decisions/provenance-pivot.md) — `meta.provenance` distingue `candidat_declare` de `roster_groupe`.
- `2026-08-08` <a id="web-v3-ui"></a>[Interfacer web/UI_finale (CONTRECHAMP) aux données réelles](decisions/web-v3-ui.md) — `web/UI_finale` câblé aux données réelles, à la place des mocks.
- `2026-08-07` <a id="syceron"></a>[Syceron : remplacement du scraping NosDéputés pour les débats en séance](decisions/syceron.md) — Syceron remplace le scraping NosDéputés pour les débats en séance.
- `2026-08-04` <a id="investigation-sources-ue"></a>[Données UE — investigation des sources](decisions/investigation-sources-ue.md) — trois pistes mesurées (ParlTrack, profils Europarl, HowTheyVote) sur le même échantillon de 3 candidats : les dumps ParlTrack sont la seule source qui attribue amendements et rapports à un député européen donné.
- `en continu` <a id="hors-perimetre"></a>[Deferred / out-of-scope investigations](decisions/hors-perimetre.md) — investigations menées puis écartées : Sénat open data, textes UE, portefeuille ministériel, agenda, maires…
